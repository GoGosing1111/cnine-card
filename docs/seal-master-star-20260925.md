# 봉인전 성공 보상 마스터의 별 20만 개

2026-09-25 사용자 지시: `봉인전 현 회차부터 봉인성공시 마스터의별 추가해라CMS포함 20만개 지급으로 설정해놔`.

## 적용

- 운영 사전 조회의 현재 회차는 49, 키 `seal-1790277293129-5d2eb5d7cb6e`, 상태 ACTIVE다. 참여자 162명, 완료 수령 0건이었다. 코인 5,000,000,000·카드 조각 0·최소 공격 20회, 운영 ON, 순위 보상은 그대로 유지한다.
- CMS의 봉인 완료 참여자 보상에 마스터의 별 입력(0~1,000,000 정수)을 추가했다. 요청대로 현재 회차와 다음 회차 설정을 200,000으로 저장한다. 구형 CMS가 새 필드를 생략한 저장도 기존 값을 유지한다.
- 새 회차는 CMS 수량을 별도로 고정한다. 일반 CMS 수정은 이미 시작한 회차에 소급되지 않는다. 이번 49회차만 명시적인 운영 작업으로 현재 회차에 반영한다. 이후 운영자가 변경한 수량을 재배포나 같은 운영 작업 재실행으로 덮어쓰지 않는다.
- 성공(CLEARED) 및 해당 회차 최소 공격 횟수를 만족해야 지급한다. 기존 완료 수령 버튼에서 코인·조각과 함께 별을 인벤토리로 지급하고 새 항목 수량·보상 로그·완료 영수증을 같은 트랜잭션에 기록한다. 실패 회차, 횟수 미달, 중복·동시 요청은 추가 지급하지 않는다. 참여 1회와 순위 보상은 변경하지 않는다.
- 유저의 현재/이전 회차 보상 안내와 수령 결과, CMS 현재 회차 요약에 실제 수량을 표시한다. 별이 0인 과거 회차에는 새 표시를 넣지 않는다. 게임·CMS 로더의 `masterStar=20260925` 캐시 키를 갱신했다.

## 저장 및 운영 작업

- `seal_battle_clear_rewards_v20260925(event_id,master_star)`에 회차별 수량을 저장한다. `seal_master_star_schema_20260925_v1` 독립 게이트로 기존 foundation 마커와 무관하게 준비하며 PostgreSQL은 `execSchema()`를 사용한다. 초기화 단계에서는 운영 설정·과거 회차·계정 재화를 변경하지 않는다. 스냅샷이 없는 과거 회차는 0이다. 조회는 기본 키 하위 쿼리로 기존 이벤트 조회에 결합하며 추가 왕복 요청이나 폴링을 만들지 않는다.
- 운영 예행 검사에서 기존 봉인전 테이블의 ALTER 소유권 제한을 확인했다. 기존 테이블 권한을 바꾸지 않고 운영 역할이 생성 가능한 독립 보상 테이블로 구현했다. 지급 수량은 기존 완료 영수증과 같은 거래에 쓰는 MASTER_STAR 인벤토리 로그(회차 키 연결)에 남긴다.
- `scripts/ops/seal-master-star-20260925.mjs`는 49회차의 정확한 키·ACTIVE·후속 회차 없음·기존 완료 청구 없음과 MASTER_STAR 카탈로그를 확인한다. 회차/청구 테이블·설정을 잠그고 한 트랜잭션에서 CMS 별 수량과 49회차 별 수량만 변경한다. 다른 필드 보존을 전후 비교하고 `admin_logs`에 이전 설정/회차를 보관한다.
- 작업 키 `ops:seal:49:master-star:20260925:v1`은 한 번만 적용한다. 재실행은 이전 완료 결과를 반환한다. 별도 인증·5분 만료 임시 운영 세션을 사용하며 공개 API·서비스 시작에 작업을 연결하지 않는다. 직접 계정 지급은 0건이며 정상 봉인 성공 후 수령으로 지급된다.

## 검증과 배포

- 새 회귀 7개 통과: SQLite/PostgreSQL 200,000개 지급·20회 경계·실패 미지급·동시/중복 차단·전체 거래 롤백·응답 유실 후 중복 방지, CMS 권한/수량 검증·구형 저장 보존, 회차 고정과 과거 0, 독립 PostgreSQL 스키마 초기화, 운영 설정 작업 예행/실패 롤백·필드 보존·재실행 차단, 플레이어/CMS 로더 연결.
- `tests/qa-seal-master-star-20260925.mjs`: 실제 봉인전/CMS 모듈 및 API 핸들러를 격리 DB에 연결했다. PC 1440×900·모바일 390×844에서 입력·저장·재조회·현재 회차 고정, 보상 표시·수령·결과 문구와 인벤토리 200,000개, 가로 넘침 없음을 확인했다. 페이지 오류 0건. 화면 증빙 `../qa-seal-master-star-20260925/`.
- 운영 예행 실행 `2026-09-25T09:29:15.874Z`에서 49회차·CMS 200,000개 및 다른 필드 보존을 확인한 후 전체 ROLLBACK했다(`committed:false`). 실제 적용은 새 서버 배포 후 실행한다.
- 새 보상 스냅샷 테이블이 포함되므로 변경 규모 정책에 따라 **전체 `release:gate`가 포함된 `npm run deploy:production`을 한 번 실행**한다. 별도의 사전 전체 게이트는 중복 실행하지 않는다. 깨끗한 범위 커밋·origin/main 일치·출시/캐시/Hyperdrive 검사도 유지한다.
- 직전 실제 운영 빌드는 `26f7629573583f7f50393a84ddc36abe8773fab4`, Pages `https://e81eee04.cnine-card.pages.dev`다. 배포 후 변경 파일만 확인하고 49회차·CMS 운영 설정을 적용·재조회한다. 완료 결과는 아래에 추가한다.
- 운영 DB에서 새 보상 조회를 예행 `EXPLAIN ANALYZE`로 확인했다: 0.469ms 실행 / 0.556ms 계획, 기존 회차 49행 읽기·최종 1행 반환, 새 보상은 기본 키 인덱스 1행·1회 조회였다. 상태 API 추가 왕복 0회, 지급은 기존 배치에 2문장만 추가하며 별도 배치 0회다. 전체 유저 응답 시간이나 Worker CPU를 계측한 값은 아니다.
- 첫 전체 게이트는 기존 크라이베른 MS-049 등록이 V2103 일회 CMS 보정 도구와 범용 음원 테스트의 별도 스킬 목록에서 누락되어 중단됐다. V2097과 동일하게 승인된 MS-049를 보정 대상에서 제외하고, 전용 모션 스킬에는 범용 음원이 없음을 검사한다. 크라이베른 수치 보존·미등록 스킬 거부 검사도 유지했다. 게임 전투 수치·자산·운영 CMS는 변경하지 않았다. 수정 후 지정 배포 명령의 게이트를 다시 통과해야 한다.
- 다음 전체 실행에서는 봉인전 22개를 포함한 앞선 검사가 통과했지만, 기존 제작소 분리 검사의 직접 호출 문자열이 진입 시 요청 재사용 경로를 인식하지 못해 중단됐다. 검사를 현재 `consumeWorkshopEntryRead()` 및 `/workshop` 대체 호출 계약에 맞췄으며 실제 제작소 코드는 변경하지 않았다. 폐차장 조회 분리·세션/진입 버전 검사 조건은 유지한다.
