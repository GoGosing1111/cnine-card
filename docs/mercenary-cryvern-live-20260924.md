# 크라이베른 SSS 운영 반영 — 2026-09-24

## 승인과 범위

- 사용자: “어 커밋해서 올리고 확률은 균등 말고 0.1 … 설정해놔”, 확인 답변 “SSS 당첨 후 0.1% — 매우 희귀하게”.
- 이름 크라이베른, 칭호 없음, SSS 기본 전투력 180,000. 승인 원화 V3·SD V7·134프레임 모션/FX와 라그니엘 근소 우위 준비 수치를 사용한다. 승인 원본은 재생성·변형하지 않았다.
- 최신 main의 흑월 V-048 / MS-048을 보존하여 신규 V-049 / MS-049로 편입한다. 기존 운영 이름·등급·수치·스킬 배정은 덮어쓰지 않는다.
- 이전 준비의 라이브 보류는 이번 명시적 승인으로 해제했다. 다른 콘텐츠의 출시 플래그·획득 정책은 변경하지 않는다.

## 확률

- 인증된 CMS에서 확인한 현재 SSS 등급 확률은 **0.005%**다. 오래된 문서의 0.0001%를 운영 값으로 적용하지 않는다.
- 등급 당첨 후 오메가-X : 라그니엘 : 크라이베른 가중치를 **8991 : 999 : 10**으로 저장한다.
- SSS 안에서 89.91% / 9.99% / **0.1%**. 기존 두 용병의 9:1 상대 비율은 유지된다.
- 현재 전체 개봉 기준 크라이베른은 **0.000005%**다. SSS 전체 확률과 다른 보상·수량은 유지한다.
- 배포와 CMS 저장 사이에도 기본 가중치 1로 풀리지 않도록 동일한 0.1% 전환 가중치를 서버/CMS에 적용한다. 신규 코드의 명시적 저장값이 있으면 그 운영 값을 우선하며 이후 배포 때 초기화하지 않는다.
- 합성으로 SSS 결과가 정해진 경우에도 같은 용병 선택 가중치를 사용한다. 합성 승급 확률이나 개방 여부는 변경하지 않는다.

## 검수·배포 선택

- 기준 운영 배포: `d593081c-cb4b-4c1f-9290-040951a24b14`, 커밋 `15d5fe2625517325f7b6dcd3bda01c688b8c4c8f`.
- 최신 main `472ac1b8d4fac1ed775895382c91045a6c2c1814`에서 독립 작업 트리를 만들었다. 기존 dirty 작업 트리는 건드리지 않았다.
- 범위는 기존 용병 체계에 승인 캐릭터 1종과 가중치 정책을 연결하는 것으로 한정한다. 인증·권한·공통 DB·트랜잭션 기반·마이그레이션·런타임 의존성·인프라 변경은 없다.
- `npm run deploy:production -- --scoped` 사용. 용병 원본/SD/도감/CMS/스킬/전투/등급 질서, 정확한 10,000표 경계, SQLite·PostgreSQL 지급 원자성·중복/재시도, 합성, V3 그리드 해시·실제 번들/메인 로더 연결만 검사한다. 선택한 정확한 테스트 목록은 배포 명령의 SCOPED RELEASE 기록에 남긴다.
- 번들은 최신 main을 선택하고 원본에서 재생성했다. 순서는 CMS → 스킬 → 도감 → V3 그리드(마지막). 수동 번들 병합 없음.
- 로컬 격리 계정으로 실제 index의 PC 토벌 버튼 → 전장 → 결과 → 복귀, 모바일 390×844 랭크전 매칭 → 전장 → 결과 → 복귀를 확인했다. 승인 SD와 청색 아우라, 5장 카드와 별도 용병 편성 유지.
- 기존 보스 아틀라스 캐시 경고와 로컬 서비스워커 미지원 경고는 관찰했다. 신규 전투 오류는 없었으며 관련 없는 공통 경고를 이번 출시에서 수정하지 않는다.

운영 계정에 시험 개봉·재화 차감·용병 지급을 하지 않는다. 배포 후 신규 도감/리소스 반영과 CMS 확률 저장 결과만 확인한다.

## 완료 기록

- 출시 커밋 `36f0a330`, 연결 보완 커밋 `f529d080`을 origin/main에 반영했다.
- 최초 관련 검사에서 구형 프리뷰의 빈 칭호 검증, 과거 밸런스 도구의 스킬 확장 누락과 이전 추첨/캐시/소스 목록 전제를 발견했다. 검증을 삭제하지 않고 실제 연결과 해당 전제를 수정했다. 실패 및 인접 경로 재검사 **67개 통과**, Worker 검사·출시 플래그/캐시 검사·Hyperdrive 검사 통과 후 배포했다.
- Pages 배포 **0a54cb09**: https://0a54cb09.cnine-card.pages.dev . 지정 배포 명령의 clan-draft Worker 단계도 정상 완료했다.
- 운영 공개 도감 API: **49종**, V-049 크라이베른, 빈 칭호, SSS / 180,000 / MS-049 확인. 운영 WebP·SD·공통 확률 모듈을 로컬 승인본과 해시 비교하여 일치 확인했다.
- CMS 개봉 설정 **r14 → r15**, 2026-09-24 18:43 KST 저장 완료. 저장 결과 화면에서 **8991 : 999 : 10**, **등급 내 0.1% / 전체 0.000005%**, SSS 전체 **0.005%**, 유저 개봉 **ON** 확인했다. 기존 타 등급·재화 확률·수량은 유지했다.
- PC/모바일 도감의 원화 확대 및 SD 전환도 확인했다. 신규 자산을 이유로 기존 용병 원화·등급·전투 수치를 덮어쓰지 않았다.

이 완료 기록만 추가한 후속 커밋은 실행 파일 변경이 없으므로 게임을 다시 배포하지 않는다.
