# 군단토벌 입장 화면·실계정 편성 수정

## 문제와 수정

운영 PVE 버튼이 검수 프리뷰를 바로 열고 고정 원정대를 생성했다. 실제 저장 덱과 용병을 누락한 잘못된 연결이다. 사용자 후속 요청은 정상 입장 절차, 내 덱·용병, 완성된 진입/콘텐츠 UI다.

- 섬 배경과 승인된 포스터, 카드 원화 5장, 별도 용병 패널, 난이도 선택과 고정 입장 버튼으로 전용 입장 화면을 구성했다.
- 메뉴 열기·난이도 변경·결과에서 복귀는 대기 상태다. 명시적 입장 후에만 전투 페이지/렌더러/서버 원정을 생성한다. 중복 클릭과 닫힌 화면의 지연 응답은 출전하지 않는다.
- 서버의 기존 `loadScrapyardV3Snapshot` 및 `releasedMercenarySnapshot`을 연결한다. 출전 때 다시 읽은 저장 카드, 강화·고유특성·타입·장비·마법·궁극기와 용병/CMS 스킬을 사용한다. 일반 카드 5장과 용병 1장은 분리한다.
- 공용 PVE의 플레이어 팀 구성 함수를 그대로 재사용한다. 군단토벌 제한 시계는 슈트/스킬칩이 없는 계정에서도 작동한다. 나머지 PVE/PVP 결과는 이전 운영 기준과 바이트 단위 비교한다.
- 운영 전투 페이지는 검수 party 선택 UI가 없다. 전투 HUD, 중간 보스, 클릭 드랍, 결과/재도전/입장 화면 복귀를 사용한다. 기존 V3 진형·카드 도크·등급 프레임은 유지한다.
- OWNER 한정, 일반 공개 OFF, 실계정 보상 지급 OFF, 기존 CMS 후보와 드랍 정책을 유지한다.

## 검증과 배포 범위

- 직전 운영: `5a4bb4b5482764aadeda3f3c3b913ec82f7be2d4`, Pages `eafd77b8-0221-498f-a2a7-338fe245a9c3` (Wrangler 운영 배포 목록에서 확인).
- 이번 변경은 군단토벌의 누락된 기존 계정 로더 연결과 입장 UI 복구다. 인증/권한 기반, 공통 DB/트랜잭션, 마이그레이션, 경제 정책, 의존성·인프라 변경 없음. 공용 팀 구성은 동작을 바꾸지 않는 함수 추출이며 새 시계 분기는 제한 시간을 지정하는 군단토벌에만 적용된다.
- 작은 수정의 관련 경로 검사로 `npm run deploy:production -- --scoped` 사용. `SCOPED_DEPLOY_TESTS`: `tests/legion-hunt-entry-account-20260926.test.mjs`, `tests/legion-hunt-owner-cms-20260926.test.mjs`, `tests/sustained-hunt-v2.test.mjs`, `tests/pve-battlefield-entry-v2117.test.mjs`. 서버 변경으로 `check:worker` 포함.
- 신규 회귀 8개: 명시적 입장/중복 클릭/닫기, 저장 덱 변경 반영과 입력 위조 차단, 슈트·용병 없는 계정, 덱 누락, SQLite/PostgreSQL 실제 보유 용병/CMS 스킬 로딩과 미편성 해제, 운영 로더 연결.
- PC 1440×960: 입장 대기·지옥/보통 선택(전투 iframe 0개) → 명시 입장 → 5장+용병 배치 → 보통 37/37·보스 3/3 클리어 → 결과에서 대기 화면 복귀 확인.
- 모바일 390×844: 원화·용병·포스터·난이도 선택, 고정 입장 버튼, 스크롤/가로 넘침, 실제 전장 용병 배치·전투 완료·결과 복귀·PVE 복귀 확인. 결과 복귀 후 전투 iframe 0개, 콘솔 오류 0개.
- 관련 서버·로더 회귀 38개 통과. 최종 범위 커밋의 지정 배포 명령에서 필수 검사를 실행한다. 기존 전체 게이트는 반복하지 않는다.

## 운영 반영

- 소스 커밋 `907e4d37b71a639d3e42be6a6cb65bc90c9c5069`을 origin/main에 반영하고 지정 `npm run deploy:production -- --scoped` 완료.
- 최종 후보에서 관련 38개 검사, Worker 구문/배포 컴파일, 출시 플래그·캐시, Hyperdrive query cache OFF 검사 통과.
- Pages: [f87cc6cc 운영 배포본](https://f87cc6cc.cnine-card.pages.dev). 19개 파일 업로드, 10,011개 기존 파일 재사용.
- clan-draft Worker 버전 `a83a7688-7d96-4113-8340-3885b9b6d3ad`.
- 운영 기본 주소의 메인 셸·입장 모듈·입장/전투 CSS·전용 전투 페이지·전투 컨트롤러 6개 파일이 HTTP 200이고 로컬 후보와 일치한다. 신규 전투 페이지는 재검증 캐시 정책 확인. 비로그인 bootstrap은 HTTP 401 + no-store 유지.
- 이 결과 기록은 문서만 커밋하며 같은 게임 빌드를 다시 배포하지 않는다.

## 운영 실계정 확인 후 원화 경로 보정

- 운영 OWNER의 실제 저장 카드 5장과 SSS 크라이베른·극빙 왕관, Z-BODY 및 장착 무기가 입장 화면에 로드됨을 확인했다. 입장 전 전투 iframe 0개.
- 실제 덱의 아이젠 족스케 원화는 기존 게임에서 사용하는 GitHub HTTPS URL이다. 새 입장 화면의 로컬 경로 제한이 이를 누락한 문제를 발견해 HTTPS 원본을 보존하도록 보정했다. 로딩 중 용병 슬롯도 미편성으로 오해하지 않게 대기 문구로 표시한다.
- 이 후속은 입장 화면 원화 URL/로딩 문구만 변경한다. 실제 직전 배포 SHA `907e4d37b71a639d3e42be6a6cb65bc90c9c5069`를 기준으로 `tests/legion-hunt-entry-account-20260926.test.mjs` 9개를 선정한 scoped 배포를 사용한다. 서버·전투·CMS 검사는 앞서 통과한 동일 빌드이므로 반복하지 않는다.

## 원화 보정 배포와 최종 실계정 확인

- 소스 커밋 `307630de2f1a32e31657419657fa5616631383ef`을 origin/main에 반영하고 `npm run deploy:production -- --scoped` 완료. 선정한 입장/계정 검사 9개와 출시 플래그·캐시·Hyperdrive 검사 통과.
- Pages: [8d02e845 운영 배포본](https://8d02e845.cnine-card.pages.dev). 4개 파일 업로드, 10,026개 기존 파일 재사용. clan-draft Worker 버전 `b57567d8-38c9-4d6b-b7ba-513a275233ab`.
- 운영 기본 주소의 메인 셸과 입장 모듈 2개 파일이 HTTP 200이며 로컬 배포 후보와 일치한다.
- 운영 OWNER의 실제 저장 카드 원화 5장과 크라이베른 원화가 모두 정상 로드된다. 입장 대기 중 전투 iframe은 0개이며, 명시적 입장 후 실제 카드 5장·별도 크라이베른·Z-BODY로 전투하는 것을 확인했다. 3구간 26/37 처치·보스 1/3 진행에서 입장 화면으로 복귀해 전투 iframe이 제거되고 실제 편성이 다시 표시됨을 확인했다. 콘솔 오류 0개.
- OWNER 한정·일반 공개 OFF·실계정 보상 지급 OFF를 유지한다. 최종 확인 결과는 문서만 커밋하며 운영 빌드를 재배포하지 않는다.
