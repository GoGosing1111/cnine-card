# 장비 강화 부족 재료 안내 — 2026-09-24

## 요청과 변경

- 신고: 마스터의 별 184,958개·장비 보호권 1장 보유 상태에서 +10 보호 강화를 시도하면 `강화 재료 또는 보호권이 부족합니다`만 표시됨.
- 승인된 +10 비용은 마스터의 별 150,000개·150억 코인, 보호권 사용 시 3장이다. 보호권 2장 부족이 원인이며 정책·확률·소모량은 변경하지 않는다.
- 서버는 부족한 항목의 이름과 필요/보유/부족 수량을 반환한다. 기존 오류 코드와 차감 전 검증 순서, 공유 잠금, 원자적 차감, 동일 요청 영수증을 유지한다.
- 강화 견적과 계정 보유량을 비교해 부족 항목을 화면에 계속 표시한다. 예: `장비 보호권 부족: 3장 필요 / 보유 1장 / 2장 부족`.
- 부족해도 확률·단계·코인·마스터의 별 견적을 유지한다. 실행 버튼은 비활성화하고 `보유 수량 새로고침`을 제공한다. 보호 옵션을 자동으로 끄거나 재료를 지급하지 않는다.
- 보호권 단위를 `장`으로 통일하고 현재 도전 단계와 필요 수량을 함께 표시한다. 새 모듈/HTML/CSS 캐시 키는 `20260924-shortage`다.

## 관련 검수와 배포 범위

- 작은 수정: 강화 부족 안내와 같은 화면에 한정. DB 스키마·인증·거래 기반·연결 설정·출시 플래그 변경 없음.
- 직전 실제 운영 배포: `01bc09c065137b573fb65b73a025eab14a771363` (`https://ab79eb0e.cnine-card.pages.dev`). 그 이후 `06cac392`는 문서만 변경했다.
- 배포 명령: `npm run deploy:production -- --scoped`.
- `SCOPED_DEPLOY_TESTS`: `tests/equipment-forge-shortage-20260924.test.mjs`, `tests/equipment-forge-recovery-20260923.test.mjs`, `tests/equipment-forge-readiness-20260922.test.mjs`.
- 선정 이유: 실제 신고 수량의 SQLite/PostgreSQL 거절·무차감·동일 요청 재시도, 부족 재료별 안내, 보충 후 1회 차감과 영수증 재조회, 기존 +9/+10 보호 소모 및 실제 클라이언트 복구 흐름을 확인한다. 변경 없는 다른 게임 콘텐츠는 검사하지 않는다.
- `check:worker`와 깨끗한 커밋/원격 일치/출시 플래그/캐시/Hyperdrive 검사는 지정 배포 명령 안에서 한 번 실행한다.
- PC 1440×1000·모바일 390×844 실제 공개 화면 검수 완료: 부족 안내, 확률 10%, 150억·마스터의 별 150,000개 표시, 보호 옵션 유지, 수량 새로고침, 수동 보호 해제 시 위험 안내, 버튼 영역과 가로 넘침 없음. 강화/복구 POST 0회, 콘솔 오류 0개.
- UI 재현: `FORGE_READINESS_QA=1`, `FORGE_INVENTORY_QA=1`, `FORGE_SHORTAGE_QA=1`, `REPAIR_QA_PORT=8966`으로 `scripts/qa-pingdu-repair-coupon.mjs` 실행 후 `tests/equipment-forge-shortage-20260924.browser.mjs`. 격리 SQLite만 사용하며 운영 계정 재화는 사용하지 않는다.
- 화면 산출물: `C:/Users/User/AppData/Local/Temp/forge-shortage-LDOtRf/shortage-1440.png`, `shortage-390.png`.

배포 회귀 검사와 운영 반영 결과는 완료 후 기록한다. 원복 시 코드만 되돌리고 운영 견적·영수증·재화·CMS 정책은 삭제하거나 초기화하지 않는다.
