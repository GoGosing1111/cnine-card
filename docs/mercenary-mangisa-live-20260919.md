# 망이사 SS 운영 등록

## 승인

2026-09-19 사용자 **“승인 용병등록해라”**. 붉은 치파오 원화, 오른쪽 수평 조준 SD, 금란 연사 연속 프레임과 기준 수치를 승인한 후 운영 등록한다.

- V-045 / 망이사 / SS / 중거리 MARKSMAN / 기본 전투력 120,000.
- MS-045 / 금란 연사: 현재 행동에서 6발. 주 대상 320%, 마지막 탄에 주변 최대 2명 각각 40%. 주변 대상 부재·소멸로 남은 몫을 다른 대상에게 이전하지 않는다.
- PVP의 SS 피해·상한 계수 82%, 비용 25, 재사용 5행동. 각 탄이 한 행동의 상한을 분할 사용한다. 회피·보호막·방어 적용, 회피한 초탄 때문에 후속탄이 취소되지 않는다.
- 주 대상 조기 처치 시 남은 탄과 확산 취소. 부활 판정은 해당 행동 뒤에 처리하고 부활 대상에게 남은 탄을 다시 쏘지 않는다.
- 기존 SS 획득 정책과 등급 내 균등 확률을 사용한다. 전체 등급 확률·하이퍼팩 비용·다른 용병의 운영 수치는 바꾸지 않는다.
- 기존 일반 덱 5장과 별도 용병 1장 계약을 유지한다.

## 원본 및 연출

원화 SHA-256 `4203D153549D0C5D13F6712BD6ABA3424A4866C12963BE823154A78F09DF6313`.
SD SHA-256 `1445CDE43CCC0E88A050A891BFA799C229310124070732FFDCFB459E24F6060A`.
기존 파일을 재생성하거나 색 보정하지 않고 원본 그대로 등록한다. 도감은 원화, 전투장은 별도 알파 SD를 사용한다.

- 실제 PixiJS 연출: `preview/mercenary-mangisa-v1/source/MangisaSkillFX.js`.
- 라이브 GSAP 연결: `preview/project-v-v3/source/battle/MangisaCombatPlayback.js` 및 공용 `MercenaryCombatPlayback.js`.
- 설치 잠금 버전은 프로젝트 `package-lock.json`을 따른다. 별도 렌더러·라이브러리 복제를 추가하지 않는다.
- 6개의 사격 자세와 16개의 개화탄 프레임, 총구·발바닥 기준점은 승인 프리뷰 매니페스트를 그대로 사용한다.
- 충돌 시각: 0.495 / 0.635 / 0.775 / 0.915 / 1.055 / 1.305초. 총 2.85초. 라이브 전투 시계가 재생·배속·취소를 관리한다.
- 서버의 단일 `MERCENARY_VOLLEY` 기록에 고정 표적과 개별 HP/보호막 결과가 포함된다. 화면은 이를 각 충돌 시점에 반영하며 피해·대상·승패를 계산하지 않는다.
- 망이사만 승인 사격 자세를 사용한다. 운영자가 다른 용병에 스킬을 배정하더라도 외형을 망이사로 교체하지 않는다.
- 전투 시작 전에 모션·이펙트 자산을 로드한다. 2.85초 연출이 일반 행동의 2초 복구 제한에 끊기지 않도록 해당 이벤트만 6초 제한을 사용한다.
- 승인 시연과 동일하게 신규 스킬은 무음이다. 기존 녹음 기반 기본 공격음은 기존 경로를 사용하며 미승인 합성음을 추가하지 않는다.

## 등록 및 검증

현재 운영 커밋 `f3caf3c4384e8c360243e3afbb8d3abfb36ab46f`에서 분리한 작업 트리로 준비했다. 44개 용병·27개 스킬을 45개·28개로 확장하며 기존 CMS의 이름·등급·스킬 배정·수치를 보존한다. 정확한 이전 목록에만 읽기 확장을 적용하고, 이후 CMS 수정은 덮어쓰지 않는다.

등록 작업은 `ops-mangisa-ss-live-20260919-v1` 감사 ID와 사전 revision/hash 비교를 사용한다. config·catalog·감사 기록을 하나의 PostgreSQL 트랜잭션으로 저장하며, 개봉 정책은 저장 전후 같은 해시인지 확인한다. 재실행은 기존 감사 기록을 확인하고 재적용하지 않는다.

필수 검증: `tests/mercenary-mangisa-live.test.mjs`, 승인 자산 QA, 용병 도감·CMS·공동 전투 회귀, 실제 로컬 PVE/PVP 및 모바일 전장. 운영 배포는 깨끗한 범위 커밋과 `npm run release:gate` 통과 후 `npm run deploy:production`으로만 진행한다.

검수 결과: 망이사 전용 회귀 7개, 자산 QA 5개, CMS·기존 하이희야 등록/획득 검증 통과. 데스크톱 PVE와 양 팀 PVP, 390px PVP 재입장·결과·용병 지휘소 표시 확인. 390px 화면에서 가로 넘침 없이 캔버스 1개이며, 2초 제한 수정 후 금란 연사 타임아웃은 재발하지 않았다. 전체 출시 게이트는 배포 로그에 별도 보존한다.
