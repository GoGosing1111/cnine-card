# 숲켓랜드 상한 확대·블랙미라클 제외 — 2026-09-24

## 승인 범위와 운영 확인

- 사용자 지시: 코인 300억·마스터의 별 5만 개까지 확대, 블랙미라클 제외, 제외한 확률을 코인·별로 분배.
- 기존 최소 수량·추첨 단위 유지: 코인 1억~300억(1억 단위), 마스터의 별 1,000~50,000개(1,000개 단위). 구간 내 균등 추첨 유지.
- Hyperdrive 연결의 `BEGIN READ ONLY`로 `soopketland_settings_v2039` 한 항목만 조회. 기존 운영 가중치는 코인 9,916 / 슈퍼스타팩 1,500 / 별 9,916 / 블랙미라클 5,668 / 미스틱 3,000.
- 신규 유효 가중치: 코인 12,750(42.5%), 슈퍼스타팩 1,500(5%), 별 12,750(42.5%), 미스틱 3,000(10%). 합계 30,000 유지. 이용권·쿠폰 사용 인원·다른 보상은 변경하지 않는다.

## 구현 경계

- 신규 추첨 목록과 OWNER 입력은 4종만 노출한다. 블랙미라클 키는 새 설정으로 저장할 수 없다.
- 기존 저장값에 블랙미라클 키가 있는 경우만 코인·별의 기존 상대 비율에 따라 확률을 분배한다. 두 값이 0이면 균등 분배하며, 나누어떨어지지 않는 가중치 1단위는 별에 남겨 합계를 보존한다.
- 기존 하이퍼버닝/랜덤카드 설정 변환을 먼저 수행하고 블랙미라클을 마지막에 제거한다. 순수 읽기 변환으로 원본 운영 설정은 덮어쓰지 않는다. OWNER가 저장한 4종 설정에는 다시 분배하지 않는다.
- 기존 최대 40,000 가중치가 합쳐지는 경우도 재저장할 수 있도록 신규 입력 상한만 80,000으로 확대한다. 실제 운영 가중치와 확률은 위 표 그대로다.
- 기존 발급 블랙미라클 1~20개 쿠폰과 코인 50억/200억·별 30,000개 쿠폰은 당시 수량을 유지한다. 기존 당첨 요청 ID 재시도는 기존 영수증을 반환하며 다시 추첨하지 않는다.
- 기존 사용자/쿠폰/lot 잠금, 거래 가드, 재고·감사·메시지·영수증 원자성은 유지한다. DB 스키마·기반 트랜잭션·블랙미라클 사용 OFF 플래그는 변경하지 않는다.
- 운영/프리뷰 공용 번들을 다시 빌드했고 공용 PixiJS·GSAP vendor 번들은 변경되지 않았다. 메인 로더와 프리뷰 캐시 키는 `20260924-land-rewards`, 메인 앱 연결은 `landRewards=20260924`다.

## 범위 검수·배포

- 작은 콘텐츠 정책 변경: 숲켓랜드 범위에 한정. 직전 실제 운영 배포는 `0d03ec4a9f19e638fae218b3aa46503cec5037b1` (`https://1051e9e1.cnine-card.pages.dev`), 이후 `d1bd3ef9`는 문서만 변경.
- 지정 명령: `npm run deploy:production -- --scoped`. `SCOPED_DEPLOY_TESTS=["tests/soopketland-v2039.test.mjs"]`, `SCOPED_DEPLOY_CHECKS=[]`(서버 변경으로 `check:worker` 자동 추가).
- 관련 회귀: 300억·50,000개 상한/단위/쿠폰 지급, SQLite·PostgreSQL BIGINT/감사/영수증, 메시지·감사 실패 롤백, 재시도/동시성, 이전 쿠폰 보존, 확률 변환·재저장·블랙미라클 재편입 차단. 배포 과정에서 한 번 실행한다.
- PC 1440×1080·모바일 390×844 화면 검수 완료: 4종 목록·42.5/5/42.5/10%, 300억·50,000개 결과, OWNER 설정 4개 입력, 블랙미라클 선택 제거, 가로 넘침/콘솔 오류 없음. `SOOPKETLAND_REWARDS_QA=1 node tests/soopketland-v2039.browser.mjs`로 변경된 보상 경로만 검사했다.
- 화면 산출물: `C:/Users/User/AppData/Local/Temp/soopketland-qa-IEwxur/`. 운영 이용권 발급·실제 추첨·쿠폰 지급은 실행하지 않았다.
- 운영 읽기 점검용 임시 Worker는 종료했다. 설정 쓰기, 계정 재화 변경, 기존 쿠폰 삭제·취소는 없다.

배포 결과는 완료 후 기록한다. 운영 코드 원복 시 이미 발급된 300억/50,000개 쿠폰의 수령 상한 호환을 유지해야 한다.
