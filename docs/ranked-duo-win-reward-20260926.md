# 랭크 듀오 승리 보상 5천만 — 2026-09-26

- 사용자 지시: `랭크듀오 승리보상 1회당 5천만으로 해라 제한도 풀고 지금 시즌부터 바로 적용`.
- 듀오 승리 코인의 서버·CMS 금액 상한 1,000만을 제거한다. 0 이상의 안전한 정수와 잔액 오버플로 검증은 유지한다. 듀오에는 별도의 일일 승리 보상 횟수/누적액 상한이 없다.
- 현재 시즌과 다음 시즌 운영 정책의 `rewards.winCoin`을 **50,000,000**으로 변경한다. 행동력을 사용한 공격자에게 승리마다 지급한다.
- 보상은 경기 접수 시 확정하는 기존 거래를 따른다. 설정 적용 이후 접수된 경기부터 5천만이 적용되며 이미 완료되거나 접수된 경기 영수증은 재정산하지 않는다.
- 일정·참가자·팀·점수·행동력·티어 보상·일반 랭크전 정책은 보존한다. 진행 중인 현재 시즌 수정은 이번 명시적 요청의 일회 적용이며 CMS의 일반적인 현재 시즌 수정 조건을 바꾸지 않는다.

## 관련 검증

- `tests/ranked-duo-win-reward-20260926.test.mjs`: 정책 금액 제한 해제/잘못된 숫자 차단, SQLite·PostgreSQL pipeline의 CMS 현재 적용 및 서로 다른 승리마다 전액 지급, 재시도 중복 차단, 지급 오류의 잔액/승패 롤백, 일회 운영 반영의 ROLLBACK·영수증·후속 설정 보존과 감사 오류/오래된 스냅샷 차단 등 5개 통과.
- 처음 운영 도구 테스트에서 기존 fixture의 `admin_logs.id` 생략을 확인해 운영 스키마와 맞춘 뒤 해당 검사 2개를 다시 통과했다. 나머지 3개는 동일 코드에서 이미 통과했다.
- 실제 CMS 모듈과 격리된 로컬 API로 데스크톱(1440×1000 요청)·모바일(390×844)에서 5천만 입력 → 현재 시즌 저장 → 재조회 확인. 입력 유효, 저장된 현재/다음 값 일치, 가로 넘침 없음. 숫자 입력과 기존 레이아웃 가독성을 직접 확인했다.
- 신규 운영 쿼리는 정책/시즌 기본키 잠금과 단일 감사·영수증 기록으로 제한한다. 보상 지급 경로에 조회·왕복·집계나 추가 잠금을 넣지 않는다.

## 범위 배포와 운영 반영 순서

- 직전 운영 Pages: `38e046b4-18be-4033-83f6-c207bd5d200d`, `SCOPED_DEPLOY_BASE=2024a2c76229b19403e4801839e1b61dab9ee846`(배포 목록에서 확인). 그 이후 선행 커밋은 운영 도구/검사/문서뿐이다.
- 작은 변경: 기존 validator/CMS 입력 상한과 CMS 모듈 캐시만 수정한다. 공통 거래·스키마·인증·의존성 변경 없음.
- `npm run deploy:production -- --scoped`, `SCOPED_DEPLOY_TESTS=["tests/ranked-duo-weekly.test.mjs"]`, `SCOPED_DEPLOY_CHECKS=["check:worker"]`. 신규 관련 5개는 같은 코드에서 통과했으므로 중복 실행하지 않는다.
- Pages와 듀오 스케줄러 Worker의 상한 해제 배포가 끝난 후 운영값을 변경한다. 구 Worker가 5천만 설정을 거절하는 중간 상태를 만들지 않는다.
- 운영 도구 `scripts/ops/ranked-duo-win-reward-20260926.mjs`, 작업 키 `ops:ranked-duo:win-50000000:20260926:v1`. 검토한 시즌/정책 해시를 확인하고 현재 시즌과 다음 정책·감사 로그·영수증을 한 거래로 확정한다. 재시도는 덮어쓰지 않는다.
- 비공개 임시 Hyperdrive 세션의 5분 토큰과 지정 동작만 사용한다. ROLLBACK 예행 후 실제 반영, 독립 읽기 연결로 확인한 뒤 종료한다.

## 운영 반영 완료

- 코드 커밋 `d1172316a5c31f2b04210020cb8425fc4b7fe2f1`, `origin/main` 반영 후 지정 scoped 배포 완료. 신규 5개와 기존 듀오 경제 회귀 14개, 총 **19개** 통과. Worker 구문/컴파일, 출시 가드와 Hyperdrive 캐시 OFF/바인딩 일치도 통과했다.
- 첫 배포 사전 검사에서 임시 원격 도구가 만든 `.wrangler/cache/cf.json`을 감지했다. 도구를 종료하고 캐시를 배포 트리 밖으로 옮긴 뒤 진행했으며, 이 차단 시점에는 관련 회귀나 운영 업로드가 실행되지 않았다.
- Pages: https://e5ea3889.cnine-card.pages.dev . `cnine-card-clan-draft` 버전: `34e217c0-c83e-4800-a252-7160fbf0b84a`.
- 운영 적용: **2026-09-26 23:08:28 KST** (`2026-09-26T14:08:28.227Z`). 현재 `duo-weekly-20260925-134825`, **ACTIVE**, 현재 시즌과 다음 정책의 승리 코인 **250,000 → 50,000,000**, 정책 버전 **0 → 1**.
- 감사 로그 **36231** (`DUO_WIN_REWARD_POLICY`), 고정 작업 완료 영수증을 함께 COMMIT했다. 사전 ROLLBACK 및 별도 읽기 전용 연결에서 현재/다음 정책과 감사·완료 기록 일치를 검증했다.
- 전투 시작 `2026-09-26T13:48:25.000Z`, 종료 `2026-10-03T13:48:25.000Z`와 나머지 설정을 보존했다. 운영 계정 대신 대전을 실행하지 않았으며 5천만 실제 지급·재시도는 격리 DB의 실제 라우트에서 검증했다.
- 운영 기본 주소의 관리자 HTML·듀오 CMS 모듈·공용 정책 모듈이 모두 HTTP 200이고 배포 파일 SHA-256과 일치했다. 로컬 QA와 임시 운영 세션을 종료하고 임시 토큰 파일을 정리했다.
- 결과 기록 후속 커밋은 문서만 변경하므로 게임 재배포는 반복하지 않는다.
