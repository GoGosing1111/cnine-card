# 군단토벌 · 잊혀진 섬 OWNER 공개 / CMS — 2026-09-26

## 사용자 범위

- `OWNER 공개로 추가해 PVE에 일반유저는 OFF`
- `드랍아이템도 CMS에서 설정,추가 가능하게 해두고`
- 콘텐츠 이름 **군단토벌**, 맵 이름 **잊혀진 섬**.
- 일반 유저 공개와 실계정 보상 지급은 OFF. 후속 지시에 따라 실제 계정의 저장된 PVE 카드 5장과 편성 용병 1장, 장착 슈트·무기를 사용한다. 이전 고정 검수 원정대 연결은 사용자 지적으로 폐기했다.
- 후속 요청으로 간단한 소개 포스터를 제작했다. 상단 ‘숲’ 오류를 실제 한글 폰트 참조로 교정한 [최종 V3](../preview/sustained-hunt-v2/assets/posters/legion-hunt-forgotten-island-v3.png)만 저장한다.

## 운영 연결

- PVE의 OWNER 전용 군단토벌 버튼은 독립 입장 화면을 연다. 승인 포스터·섬 배경·실제 카드 원화 5장·용병 전용 슬롯·4개 난이도를 확인하고 **토벌 입장**을 눌러야 전장이 열린다.
- 메뉴 열기와 난이도 선택은 세션/렌더러를 생성하지 않는다. 입장 클릭 이후 전용 `pve/legion-hunt/` 전투 페이지가 공용 V3 전장·원화 도크·아트 어댑터 및 군단토벌 확장을 사용한다. 프리뷰의 검수 원정대 선택창은 운영 화면에 없다.
- bootstrap은 기존 PVE 계정 스냅샷 로더와 출시된 용병 로더를 사용한다. start에서도 계정 편성을 다시 읽어 저장 변경을 반영한다. 브라우저의 카드·전투력·용병 코드·검수 party 지정은 받지 않는다.
- `/api/legion-hunt/bootstrap|start|begin|reveal|claim|finish|cancel` 및 `/api/admin/legion-hunt`는 서버 인증 후 정확한 OWNER 역할만 허용한다. USER·ADMIN·미로그인 모두 차단하며 공개 ON을 요청 본문으로 전달할 수 없다.
- 기존 인증·사용자 잠금을 사용하고, 현재 원정의 축약 상태를 기존 app_meta의 계정별 키 하나에 저장한다. Worker 메모리에 세션을 보관하지 않는다. 전투 시뮬레이션은 출전 때 한 번만 수행한다.
- 현재 원정 키는 계정당 하나이며 명시적인 새 출전으로 교체된다. 30분 만료. 새로고침·입장 화면 복귀 후에는 대기 상태이며 다시 입장해야 새 원정을 만든다.
- 처치에는 보상을 지급하지 않는다. 서버가 발급한 드랍의 토큰·좌표·소멸 시간과 계정별 원정을 검증한 클릭만 검수 획득 기록에 반영한다.
- app_meta compare-and-swap으로 클릭·재시도·정산을 저장한다. 원정 스냅샷에는 카탈로그 전체·렌더링 페이로드 대신 전투 이벤트의 최소 필드와 드랍 상태만 보관한다.
- OWNER 화면의 드랍 생성·클릭·정산 요청은 순서대로 보내 같은 계정 잠금과 경쟁하지 않는다. 잠금 사용 중 응답만 같은 본문으로 최대 두 번 재시도한다.

## CMS 사용

**CMS → 군단토벌 → 필드 드랍 설정**

1. 현재 활성·공개 카탈로그의 아이템·장비·이동수단·SUPERSTAR/ZENITH/FUR 카드를 검색하고 후보로 추가한다.
2. 신규 후보의 기본값은 사용 OFF. 사용 여부, 선택 가중치, 최소·최대 수량을 지정한다. 후보 최대 100종.
3. 보통·어려움·악몽·지옥 각각 일반 몬스터 확률, 보스 확률, 필드 소멸 시간(3~30초)을 설정한다.
4. 저장하면 다음 원정부터 적용된다. 진행 중인 원정은 시작 당시 설정을 유지한다.
5. 드랍 성공 시 활성 후보 중 가중치 비율로 1종을 선택하고 수량 범위에서 추첨한다. 화면에 후보별 조건부 선택 확률을 표시한다.

최초 후보는 빈 상태다. 검수용 프리뷰의 임시 세 종류를 실제 아이템 정책으로 자동 승계하지 않는다. 후보를 선택·활성화·저장해야 OWNER 필드 드랍이 생성된다. 로컬 단독 프리뷰(8958)는 종전 검수용 세 종류를 그대로 사용한다.

설정 저장은 리비전 검사 + CAS + 관리자 감사 로그를 같은 DB batch에서 처리한다. 오래된 탭의 덮어쓰기, 감사 기록 실패 후 부분 저장을 차단한다. 카탈로그 조회는 유형당 1회, 최대 1,001행이며 한도 초과 시 잘라 저장하지 않고 오류를 반환한다. 기존 통합 드랍풀·블랙미라클·다른 콘텐츠 정책은 변경하지 않는다.

## 검수

- `tests/legion-hunt-owner-cms-20260926.test.mjs`: SQLite와 실제 PostgreSQL 호환 계층/PGlite에서 역할 차단, 카탈로그, 추가/제외/활성화, 확률·수량 검증, CAS 충돌, 감사 실패 롤백, 설정 스냅샷, 드랍 만료, 클릭 재전송·응답 유실·저장 실패, 사용자 분리, 실계정 미지급.
- `tests/sustained-hunt-v2.test.mjs`: 난이도별 클리어/실패, 시간 초과, 중간·최종 보스, 랜덤 위치, 직접 클릭/만료, 공용 PVE·PVP 결과 동일성, V3 번들/시계.
- 실제 브라우저 1440×960 / 390×844: CMS 후보 추가·제외·수량 변경·저장·재조회, OWNER 메뉴 입장, USER 메뉴 없음, PVE 복귀 확인.
- OWNER PC 플레이: 37/37, 보스 3/3 클리어. 9개 드랍 클릭 → CMS 지정 수량 3개씩 총 고출력 엔진 27개 집계.
- 모바일 무클릭 플레이: 37/37, 보스 3/3 클리어, 직접 획득 0개, 수집품 없음. 메뉴 겹침과 전장 우측 스크롤바 잘림 수정 후 재검수.
- 검수 서버 `node scripts/serve-legion-hunt-owner-review.mjs`: 127.0.0.1:8959에만 바인딩, 테스트 DB와 역할 쿠키를 사용하는 개발용 harness. 운영 인증을 우회하는 라우트는 배포 함수에 없다.

## 배포 선택

- Wrangler 4.125.0 운영 배포 목록으로 확인한 직전 운영 커밋: **60acb717dd501240937faad54b4c2f36a9e288fe**.
- 직전 배포: **058a3107-9d31-4302-a83a-8bc03d6463ae**.
- 신규 다중 몬스터 전투·PVE OWNER 진입·서버 세션·CMS 저장을 함께 연결하는 기능 추가이므로 전체 게이트 경로를 선택한다.
- 배포 명령: `npm run deploy:production`. 최종 후보를 커밋·origin/main 반영한 뒤 명령 내부에서 `release:gate`를 한 번 실행한다. 별도 사전 전체 게이트는 중복 실행하지 않는다.
- 신규 마이그레이션·인증 기반·런타임 의존성·Wrangler 바인딩 변경 없음. 일반 공개·실계정 지급 OFF, 기존 출시 플래그 그대로 유지.
- 1차 전체 게이트는 기존 grid 검사에 남은 소스 47개 고정값에서 중단됐다. 현재 운영판에 이미 포함된 저격수 오리꿍 입력 3개를 누락한 검사였으며, 50개와 해당 3개 파일의 명시적 검증으로 바로잡았다. 아홉 번들과 모든 입력 SHA 검사는 통과했고 게임 리소스 재생성은 하지 않았다. 수정 후보에서 지정 배포 명령을 다시 실행한다.
- 2차 게이트의 V3 공동 검사는 184개 중 기존 용병 수 고정값 두 곳에서 실패했다. 이미 출시된 MS-050 에메랄드 대물저격을 포함해 음원 프로필은 28개, CMS 스킬은 33개다. 해당 수와 MS-050의 음원 존재·별도 승인 밸런스 보존을 명시적으로 검증하도록 수정했다. 음원 해시·충돌 시점·스킬 정책 검증은 유지하며 운영 데이터와 전투 로직은 바꾸지 않았다.
- 3차 게이트는 V3 184개를 통과한 뒤 기존 용병 연결 검사에서 중단됐다. V-050 확정 SS 등급·가중치 추첨·배너 어댑터를 누락한 테스트 픽스처를 현재 승인 데이터에 맞췄다. 같은 원인의 인접 도감·진형·자산 개수와 과거 크라이베른 초안 마이그레이션 검사도 함께 갱신했다. 원본 해시·원자 지급·재시도 검증은 유지한다.
- 관련 119개 회귀 중 116개 통과 후, 옛 읽기 전용 도감 프리뷰가 새 역할 문구 `최상위 원거리 저격`의 위치를 해석하지 못한 실제 오류를 확인했다. 해당 표시 문구만 후열/저격으로 해석하도록 어댑터를 보정했고, 남은 세 검사(명단·필터·150개 WebP 해시)가 모두 통과했다. 운영 용병 이름·등급·확률·보상·전투 데이터는 변경하지 않았다.
- 해당 프리뷰 보정도 PC 1440×960 / 모바일 390×844에서 후열·저격 필터와 오리꿍 카드 표시를 확인했다.
- 4차 게이트는 용병 도감까지 통과한 뒤 강화 인벤토리 테스트의 오래된 API 의존성 주입에서 중단됐다. 이미 운영 API에 추가된 `ensureEmperorEnergyCatalog`를 두 테스트가 전달하지 않아 생긴 ReferenceError다. 실제 초기화 함수를 주입하고 SQLite/PostgreSQL의 보호권·쿠폰 조회 네 검사를 통과했다. 인벤토리 운영 코드는 변경하지 않았다.

## 운영 반영 완료

- 2026-09-26 17:31 KST 확인. 사용자 후속 `포스터는 됐으니까 나머지나 빨리 정리하고 업로드하고 끝내`에 따라 V3 포스터 승인과 OWNER 공개 배포를 완료했다.
- 운영 소스: `1e0fdfe96da7cd677da7ad3f53a2319897c375ef`. 검사 중 원격에 추가된 제작소 진입/이미지 성능 수정도 보존해 통합했으며, 이 최종 후보에서 지정 `npm run deploy:production`을 완료했다.
- 전체 게이트: 2,689개 중 2,681개 통과, 기존 조건부 건너뛰기 8개, 실패 0개. 군단토벌 관련 23개 모두 통과. 깨끗한 후보·origin/main 일치·출시 플래그·캐시 검사와 Hyperdrive 쿼리 캐시 OFF 확인.
- Pages 배포: `44a0e952-a151-4218-bdcc-1e0a53a0d472`, [운영 배포본](https://44a0e952.cnine-card.pages.dev). 새 파일 142개 업로드, 기존 9,884개 재사용.
- 지정 명령의 clan-draft Worker 배포도 완료: `c45d15fa-d8de-41bf-a808-80fef5f8f0aa`.
- 운영 기본 주소에서 PVE/CMS 진입·스타일·전투 앱과 함께 반영된 제작소 파일 등 텍스트 11개가 로컬 후보와 일치함을 확인했다. 최종 포스터 V3도 HTTP 200 및 SHA-256 일치.
- 운영 비로그인 `/api/legion-hunt/bootstrap`, `/api/admin/legion-hunt`는 HTTP 401 + `Cache-Control: no-store`. OWNER/일반 유저 역할 구분과 실제 플레이·CMS 조작은 앞서 로컬 실제 핸들러 및 PC/모바일에서 확인했다.
- 일반 유저 공개·실계정 보상 지급은 계속 OFF. 최초 CMS 드랍 후보는 비어 있으며 OWNER가 추가·활성화·저장한 항목부터 다음 검수 원정에 적용된다.
- 이 완료 기록은 문서만 커밋·원격 반영하며 게임을 다시 배포하지 않는다.
