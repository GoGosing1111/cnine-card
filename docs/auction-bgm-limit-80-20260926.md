# 경매장 공용 BGM 80개 확대 — 2026-09-26

관리자 화면의 추가·저장 한도와 서버의 저장·조회 한도 안내를 50개에서 80개로 올린다. 관리자 스크립트 캐시 키는 `20260926-bgm-80`으로 변경한다. 기존 BGM 구간·음량·재생시간, 일반 입찰 알림 제외와 대형 입찰 처리는 그대로 유지한다. 운영 BGM 데이터를 덮어쓰는 작업이나 DB 마이그레이션은 없다.

## 관련 검증

- 변경 전 `tests/auction-bgm-limit-80-20260926.test.mjs`로 80개를 저장해도 서버가 50개만 보존하는 제한을 재현했다 (`50 !== 80`).
- 같은 회귀는 기존 50개 보존, 80개 저장·재조회, 공개 발동표 80번째 표시와 해당 금액의 BGM 이벤트 예약을 검증한다. 최종 실행은 범위 배포 과정에서 한 번 수행한다.
- 실제 관리자 스크립트·스타일과 로컬 경매 API/SQLite를 연결한 격리 화면에서 PC 1440×1000, 모바일 390×844 편집 영역을 확인했다. 30개 추가, 80개 저장·불러오기, 기존 50개 보존, 81개째 추가 차단과 브라우저 오류 없음을 확인했다.
- 음원 메타데이터는 검사 객체로 대체했고 운영 API 저장은 하지 않았다. 모바일 검수는 BGM 편집 영역을 대상으로 했다. 축약 CMS 셸의 기존 sticky 사이드바가 조작을 가려 셸을 제외한 격리 화면에서 검사했다.
- 검수 스크립트와 스크린샷은 배포 디렉터리 밖 `../qa-auction-bgm-80.mjs`, `../qa-auction-bgm-80/`에 보관한다. 수정 JS 문법과 diff 공백 검사도 확인했다.

## 범위 배포

- 분류: 관리자 BGM 등록 한도에 한정된 작은 변경. DB 구조·인증·의존성 변경 없음.
- 확인한 직전 운영 배포: `a180e0c6-0435-4e97-976a-39ec6556402f`, 기준 SHA `22bb77f4f4470378d173b906f6819c7bab34814f`.
- 선택 테스트: `tests/auction-bgm-limit-80-20260926.test.mjs`.
- 선택 검사: `check:worker`와 지정 배포 도구의 기본 출시 플래그·캐시·Hyperdrive 검사.
- 선정 이유: 한도 확대의 저장/조회/마지막 구간 발동만 영향받으므로 관련 회귀와 관리자 편집 영역을 검사한다. 무관한 전체 게이트는 실행하지 않는다.
- 깨끗한 범위 커밋을 `origin/main`에 반영하고 `npm run deploy:production -- --scoped`로 배포한다. 배포 후 관리자 스크립트·캐시 키 반영을 짧게 확인한다.
