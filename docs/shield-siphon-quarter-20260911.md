# 강탈의 성배 성능 1/4 조정

2026-09-11 사용자 지시: `성배 1/4 성능으로 낮춰`.

## 적용

- 운영 `magic_cards`의 `V2_SHIELD_SIPHON` 강탈률: **60% → 15%**. 카드 설명도 15%로 맞춘다. 일반 PVE와 PVP 모두 적용한다.
- 아포칼립스 성배 1회 상한: **일반 타격 하한 1회 → 1/4회**. 다른 마법카드 상한은 1회로 유지한다.
- 강탈률과 상한을 모두 줄여 상한에 걸리는 구간도 실제 효과가 1/4이 된다. 정수 보호막은 반올림하며 기존 최소 1 규칙을 유지한다.
- 발동률, 강화 단계, 최대 발동 횟수, 보유 수량, 카드 고유효과와 배틀슈트 화력은 이번 조정 대상이 아니다.

코드 배포와 운영 카탈로그 변경이 한 작업이다. 카탈로그는 `ops:shield-siphon-quarter:20260911` 영수증과 `MAGIC_CARD_SAVE` 운영 로그를 남기는 일회성 트랜잭션으로 반영한다. 반복 조회나 배포 시 CMS 설정을 덮어쓰는 자동 마이그레이션을 추가하지 않는다.

## 검증

- PVP 및 일반 몬스터: 남아 있는 보호막의 15%를 빼앗아 동일 수량을 자신의 보호막에 더한다.
- 하시라마 저전투력 조건: 1회 강탈량 **342,866 → 85,717**. 강탈률만 15%로 바꾸면 기존 상한에 계속 걸리는 조건이다.
- 보호막이 적은 아포칼립스 조건: 15%를 그대로 적용하고 추가로 1/4을 곱하지 않는다.
- 기존 아포칼립스 난이도·배틀슈트 관통 검증을 통과했다. 전용 회귀 테스트를 `test:apocalypse`와 `release:gate`에 포함했다.
- 조사 당시 운영 스냅샷의 동일 500개 시드(`1..500 × 7919`) 비교: CR7 포함 덱 **482승 → 0승**, 다른 두 덱 **473승 → 93승**, **363승 → 0승**. 이는 세 가지 특정 조합의 재현 결과이며 전체 계정의 승률을 뜻하지 않는다.

운영 DB 확인과 재현 자료는 로컬 `tmp/ronaldo-apocalypse-audit-20260911/` 및 `tmp/grail-quarter-20260911/`에 보존한다. 계정 스냅샷과 임시 접근 키는 배포 파일에 포함하지 않는다.
