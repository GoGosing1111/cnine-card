# 용병 투사체 가독성 운영 수정 · 2106

사용자 요청: 공격·체력·추가 행동 상향부터 운영 반영한 뒤 나머지 용병을 함께 맞추고, 잘 보이지 않는 화살·총알을 수정한다.

2105의 용병 43종 상향은 2026-09-14 00:14 KST 운영 확인 완료다. 배포 `4ffeda72`, 커밋 `7f47197e`. 아군 일반 카드 1회 행동마다 용병 추가 행동 1회, 등급별 공격력·실제 최대 체력 연동을 적용했다. 상향표는 `mercenary-action-hp-2105.md`를 따른다.

## 2106 변경

- 실제 공용 V3 `MercenaryRoleAttackFX.js`의 기본 원거리 비행을 충돌 전 구간의 28%에서 80%로 늘린다. 기본 사격의 비행 구간은 0.0672초에서 0.192초, 저격은 0.1008초에서 0.288초다. 충돌 시각·총 연출 길이는 유지한다.
- 승인된 역할별 16프레임은 유지한다. 비행 중 표시 배율은 30%에서 55%로 키우고, 작은 화면에서도 최소 36 화면 픽셀을 확보한다.
- 총알에 밝은 탄두와 외곽 대비가 있는 궤적을, 화살에 화살촉·깃·곡선 궤적을 추가한다. 보조 선은 기본 3.5 화면 픽셀, 화살 스킬은 최소 3 화면 픽셀로 화면 축소에 대응한다.
- 공용 `RenderAuthoredSkill.js`의 사격 스킬은 최소 0.26초 비행, `RenderSSkill.js`의 삼중 화살은 0.42초 비행이다. V3 이벤트 재생 시작점도 화살의 비행 시작에 맞춰 중간 지점에서 나타나지 않게 한다.
- 준비·비행·타격·소멸은 기존 PixiJS 8.20.0 / GSAP 3.13.0의 동일 타임라인을 사용한다. 서버 피해·표적·승패·충돌 기준점, 승인 아틀라스와 사운드는 유지한다.
- 기본 투사체는 충돌·대상 소멸·취소·종료 때 비우거나 소유 Graphics를 제거한다. 스킬 궤적도 매 렌더 시 비우고 충돌 시각 이후 표시하지 않는다.
- 실제 앱과 공용 V3 로더의 캐시 키에 `projectiles=2106`을 추가한다. 9개 공용 V3 소비 번들은 기존 빌드 스크립트로 재빌드한다.

## 확인 기록

로컬 실제 앱의 `ensureFeatureResources('battleV2')` 및 `ProjectVPixiBattle.mountForBattle`을 사용했다. 독립 프리뷰 페이지를 실제 앱으로 대체하지 않았다.

- 1440×1000 / 390×844: 베스페라 총알·마리엘 화살의 비행 위치, 화면 두께, 충돌 위치 확인.
- 모바일 실제 서버 스킬 이벤트: MS-004 정밀 사격 / MS-040 삼중 화살 확인.
- 6개 사례: 충돌 후 선 0개, 취소 후 소유 노드 0개, 2배속 완료 후 활성 이펙트 0개.
- 좌우 공격과 화면 축소의 종점·가독성 회귀 검사 추가.
- 자료: 저장소 밖 `qa/mercenary-action-hp-2105/projectile-browser-report.json` 및 동일 폴더의 `projectile-*.png`.

배포는 깨끗한 범위 커밋에서 `npm run deploy:production`으로 실행하며, 포함된 `release:gate`를 통과해야 한다.
