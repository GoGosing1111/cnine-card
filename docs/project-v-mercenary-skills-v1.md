# 용병 독립 스킬 17종 · 2026-09-11 검수 초안

사용자 지시: “용병스킬 최소 15개 만들어봐라 특색있게 만드는 기준 준수하고”. 현재 7개 효과 분류의 **17개 스킬**을 독립 목록으로 관리한다. 후속 “스킬 목록을 따로 뽑아놓고 어떤 용병에 적용할지는 내가 정한다” 지시에 따라 기존 제안 배정과 전용 잠금을 모두 해제했다. 각 스킬은 동작 규칙·유효 상황·약점 공략·보스 예외와 독립된 효과 소재를 가진다.

검수 화면: `/preview/project-v-mercenary-system-v1/skills`.

2026-09-11 후속 지시로 **종언의 사건지평선(MS-021)**을 추가했다. 당시 오메가 전용 제안은 후속 지시로 철회했고 오메가 SSS 등급만 유지한다. 현재 총 17종·51개 대표 상황·개별 16프레임씩 총 272프레임이다. 최초 V1의 정지 이미지 트윈 16종은 사용자 반려 이력이며, 현재 자산과 재생 기준은 `preview/project-v-mercenary-system-v1/SKILLS-V2-REVIEW.md` 및 `OMEGA-REVIEW.md`를 따른다. 등급 체계·전용기 설계·기존 CMS 저장본 변환은 `docs/project-v-mercenary-omega-ranks-v1.md`에 기록한다.

## 용병과 독립된 스킬 목록

| 스킬 ID | 스킬 | 핵심 규칙 | 상대의 대응 |
| --- | --- | --- | --- |
| MS-021 | 종언의 사건지평선 | 전열에 균열 후 남은 표식만 두 번째 검격으로 폭발, 사용 뒤 과부하 | 타격 사이 정화·제압, 보호막 |
| MS-003 | 황금 구명선 | 위기 아군의 단일 피해 1회 분담, 피해 총량 보존 | 광역·지속 피해, 호위자 제압 |
| MS-004 | 루비 최후통첩 | 시작 공격력 기준 후열 위협 고정 저격 | 긴 조준 방해, 보호, 표적 상실 유도 |
| MS-006 | 흑철 해체 | 방호 소모 후 방어 약화 창 생성 | 접근 차단, 약화 정화 |
| MS-005 | 탄착 교정 | 동일 표적 연속 명중의 마지막 탄 강화 | 표적 전환·회피로 교정 초기화 |
| MS-013 | 일제 사격 명령 | 각 아군의 다음 기본 공격 1회 보조 | 공격 전 지휘 해제·행동 방해 |
| MS-015 | 독사 그림자 | 침투 접촉 후 귀환, 지연 독 표식 | 침투 방해, 귀환 전 공격, 독 정화 |
| MS-016 | 황혼 봉인 | 전열의 다음 공격 스킬 위력 감쇠 | 장막 정화, 기본 공격으로 대기 |
| MS-018 | 생명선 봉합 | 지속 피해 정화 후 지연 단일 회복 | 회복 억제·완료 전 처치 |
| MS-023 | 푸른 응수 | 자신을 공격한 근접 적에게만 받아치기 | 사격·지속 피해·기다리기 |
| MS-025 | 백색 정적 | 무피격 준비 뒤 전투 1회 첫 저격 강화 | 조준 중 약한 견제라도 명중 |
| MS-027 | 폐쇄 사선 | 전체 피해 예산을 나눈 전열 진입 억제 | 준비 차단, 후열·고정 사격 |
| MS-028 | 연화 결계 | 전체 보호막 예산을 전열에 분배 | 보호막 제거·관통·지속 압박 |
| MS-032 | 마지막 약실 | 충돌 시 저체력 검사, 처치 실패 시 재장전 부담 | 충돌 직전 회복·방호 |
| MS-038 | 업무 종료 | 현재 준비 중인 중단 가능 기술 1개 차단 | 기본 공격, 빠른 발동, 중단 면역 |
| MS-042 | 현행범 체포 | 동일 적의 반복 직접 공격 기록으로 다음 기본 공격 약화 | 공격 패턴 전환, 기록 정화 |
| MS-043 | 퇴근길 앙코르 | 첫 탄 명중 뒤 동일 대상 후속탄, 실패하면 취소 | 초탄 회피, 두 탄 사이 제압 |

기본 공격의 전열 우선 규칙을 유지한다. 기습·저격의 후열 우회는 해당 스킬의 명시적 표적 정책에 한정한다. 일반 카드 5장에 용병을 넣거나 배틀슈트까지 지원 효과 대상으로 확대하지 않는다.

## 특색 검수 기준

1. 이름·색·피해 배율만 바꾼 기술은 별개의 고유 스킬로 세지 않는다.
2. 발동 조건, 표적 정책, 상태 변화, 시간 흐름, 실패 비용, 대응법을 명시한다. 현재 17종은 서로 다른 `mechanic`과 `visual.motion`을 가진다.
3. 강점 하나를 선명하게 둔다. 호위와 회복, 선제 방호와 응수, 즉시 차단과 다음 공격 감쇠를 섞어 만능 스킬로 만들지 않는다.
4. 보호 대상·고정 표적을 잃었을 때 재선정 여부를 명시한다. 처치·추가타·반격에 의한 재귀 발동을 금지한다.
5. 범위 피해·보호막은 전체 예산을 나눈다. 대상 수에 비례해 단일 수치를 무한 복제하지 않는다.
6. 보스의 이동·중단·상태 면역과 고유 기믹을 존중한다. 면역으로 막힌 효과를 임의의 스턴·즉사·고정 피해로 보상하지 않는다.
7. 효과는 방벽·조준탄·금속 균열·명령 전달·독사·장막·봉합·응수·설빙·사선·연화·탄실·건틀릿·구속·두 박자처럼 실루엣과 동작으로 구분한다.

## 구현 및 수치의 범위

- `shared/mercenary-skills-v1.mjs`: 17종의 단일 설계 기준, 발동·효과·약점·보스·연쇄 정책, CMS 초안 검증.
- `preview/project-v-mercenary-system-v1/skill-rehearsal.mjs`: 검수 전용 결정적 시뮬레이션. 기술마다 정상·약점·보스의 3개 상황을 미리 확정된 이벤트로 만든다.
- 100 HP, 검수 피해·회복·보호막 수치는 동작 차이를 확인하기 위한 예시다. 운영 피해 계수·쿨다운·비용은 `null / PENDING_REVIEW`다. 예시 숫자를 운영 정책으로 이관하지 않는다.
- 중첩 상한·지속 턴·모드별 저항·적중·자원·성장 계수는 실제 서버 밸런스 구현 전에 확정해야 한다. 검수 실행기는 이들 전체를 구현한 라이브 전투 엔진이 아니다.
- 지원의 정화·감쇠, 전열 억제, 초탄 조건, 고정 표적과 보스 면역 등의 대표 차이를 51개 시나리오에서 실행한다. 모든 가능한 시간 순서·상태 조합을 검수한 것으로 보고하지 않는다.

## PixiJS·GSAP과 리소스

실제 구현은 `source/skills-lab.src.js`와 `source/MercenarySkillFX.js`다. 기존 `project-v-pixi-battle.src.js`, `BattleEngine`, `BattleCharacter`, `battle-v3-live.js`와 원본 카드 도크·아트 어댑터를 재사용한다. 독립 프리뷰 번들 한 개에 잠금 **PixiJS 8.20.0 / GSAP 3.13.0**을 한 번만 포함한다. 라이브 번들은 수정하지 않는다.

일반 카드 5명은 승인 로스터의 실제 ID·원화·SD로 만든 정적 검수 편성이다. 상대도 같은 승인 카드로 구성한 모의 표적이며 실제 사용자 덱 스냅샷이 아니다. 별도 용병 `BattleCharacter`는 일반 카드 배열 밖에 둔다. 용병 슬롯은 검수용 빈 타일만 사용하며 기존 5인 진형·도크·배틀슈트 타일을 옮기지 않는다. 실제 전투 슬롯 구현·서버 편성 승인을 뜻하지 않는다.

현재 17개 스킬의 개별 연속 시트를 내장 ImageGen으로 각각 생성했다. 원본 PNG는 `skill-assets-v2/source/`에 무가공 보존하고 16프레임씩 분리한 무손실 WebP 아틀라스를 사용한다. 원본·변환본 해시, 알파 검수, 전체 프롬프트와 생성 방식은 `skill-assets-v2/manifest.json`에 남긴다.

반려된 V1은 정지 이미지 트윈 이력으로 보존한다. 현재 V2는 **각 스킬의 준비·접촉·파열·소멸이 실제로 변화하는 개별 연속 스프라이트**이며 GSAP 단일 시각에서 프레임 UV와 보조 계층을 함께 재생한다. 단순 이동·색상 변경을 새 스프라이트로 세지 않는다.

- 시간 단위: 초. 각 기술의 `visual.windup`, `visual.impacts`, `visual.duration`이 예고·충돌·완료 기준이다.
- 기준점: 캐릭터 발바닥 원점, 충돌은 캐릭터 높이·실제 스케일을 반영한 몸통 54% 위치. SD 발끝은 승인 `footAnchor`를 사용한다.
- 시간 소유자: V3 `simpleTimelines`에 등록한 GSAP 타임라인 1개. 정지·배속·탐색 모두 이 시계만 변경한다. Pixi 자동 프레임 재생·별도 충돌 타이머는 없다.
- 피해·상태는 사전 확정된 검수 이벤트를 샘플링한다. 이펙트가 피해·승패·발동 횟수를 재계산하지 않는다.
- 취소·화면 이탈·선택 변경은 타임라인 등록을 해제하고 용병을 원래 타일로 되돌린다. 선택 변경·종료는 소유한 표시 객체를 제거한다. 공유 V3 텍스처를 파괴하지 않는다.
- 사운드: **무음 검수**. 새 사운드를 제작·연결하지 않았다. 효과음 완료 또는 음향 ±20ms 검증을 주장하지 않는다.

## CMS 준비 계약

포지션 초안과 별개인 `PROJECT_V_MERCENARY_SKILL_DRAFT_V1`을 사용한다. 브라우저 저장 키는 `cnine.mercenarySkills.draft.v1`이다. 17종의 스킬 이름·검토 상태·의견을 편집, 저장, 재로드, JSON 가져오기·내보내기 할 수 있다.

스킬 검토본 version=3은 용병 code·rank·exclusivity와 로스터 버전을 갖지 않는다. 목록 버전, 중복·누락, 길이, 수정 번호와 OFF 상태를 검사한다. 과거 version=1/2의 검토 이름·메모·수정 번호는 보존하고 당시 제안 code만 제거한다. 다른 화면의 수정 번호가 바뀌면 덮어쓰지 않는다. `REVIEWED`는 메모용 검토 표시이며 사용자 출시 승인이나 운영 플래그가 아니다.

실제 선택은 `skill-assignments.html`의 별도 `PROJECT_V_MERCENARY_SKILL_ASSIGNMENTS_V1` 초안으로 관리한다. 저장 키는 `cnine.mercenarySkillAssignments.draft.v1`이며 43종 모두 빈 `skillIds`로 시작한다. 사용자가 체크한 연결만 저장하고 여러 스킬·여러 용병 선택을 허용한다. 장착 슬롯 수·무기 적합성·등급 제한을 운영 규칙으로 확정한 것은 아니다. 시연용 용병 선택은 저장되지 않는다. 배정 UI·형식과 전체 표는 `docs/project-v-mercenary-skill-catalog-v1.md`를 따른다.

향후 운영 CMS에서 추가할 항목은 기존 포지션 설계 계약을 따른다. 서버 권한·revision 충돌 검사·감사 로그·검수 전후 차이·이전 버전 복구·사용자 확정 등급·계수·쿨다운·대상 수 상한을 함께 구현한다. 자유 입력 문자열을 실행 코드로 평가하지 않는다.

## 출시 상태

검수 화면 공개만 포함한다. 준비 로스터에서 오메가-X는 SSS, 나머지 42종은 미정이며, 용병 획득·일반 덱·운영 편성 API·실제 전투·CMS 운영 DB에는 연결하지 않는다. 사용자의 등급 확정과 시스템 연결 지시 뒤 별도 통합 검증을 진행한다.

검증 명령: `npm run build:mercenary-skills`, `npm run test:mercenary-skills`, `npm run test:mercenary-codex`. 운영 프리뷰 공개 전 `npm run release:gate`와 `npm run deploy:production`을 사용한다. 실제 실행 결과는 프리뷰 README에 기록한다.
