# 베르칸 2명 동시 타격 조정 — 2026-09-27

사용자 지시: 스킬 한 번으로 2명을 맞히는 장점을 만들고, SSS 안에서 크라이베른 동급 또는 살짝 아래로 맞춘다.

## 적용

- V-055 / MS-055만 새 `GILDED_STARFALL` 판정을 사용한다. 살아 있고 지정 가능한 서로 다른 적을 후열 우선·전투 시작 공격력 순으로 최대 2명 선택하며 후열이 부족하면 전열로 채운다.
- 총 피해 배율 5.6을 대상 수로 나눈다. 두 명이면 각각 2.8, 단일 보스면 5.6 한 번이다. 비용 35 / 재사용 5턴, 회피·방어·보호막·제압 처리를 유지한다. 회피한 몫의 이전, 추가 행동, 중복 발동은 없다.
- PVP 단일 충돌 상한 계수 1.7은 크라이베른의 전열 2단 타격과 정식 엔진 비교로 조정했다. 수치 하나의 대소를 전투 우열로 해석하지 않는다. PVE는 크라이베른과 같은 일반 피해 경로를 사용하여 기존 저격수의 몬스터 피해 하한 보너스를 중복 적용하지 않는다.
- 두 화살·파열은 승인된 연속 스프라이트를 공유하고 같은 GSAP 시계에서 2.08초에 표시한다. 기본 공격은 계속 단일이다. 기존 오라·사각 잔상 수정과 Sprite 풀 40개를 유지한다. 서버 이벤트 하나에 두 결과를 담으며 렌더러가 피해를 계산하지 않는다.
- 저장된 예전 MS-055 단일 정의는 기존 CMS 읽기 경로에서 순수 변환한다. 이전 기본값에 해당하는 수치만 새 기본값으로 바꾸고 운영자가 수정한 수치, 배정, 다른 용병, 획득 가중치는 보존한다. 새 정의로 저장된 뒤의 CMS 변경을 덮어쓰지 않는다. DB 조회·DB 쓰기·마이그레이션·폴링은 추가하지 않는다.

## 관련 검증

`tests/mercenary-berkan-twin-starfall-20260927.test.mjs` 6건 통과: 두 표적 선택/전열 보충/단일/회피/보호막/동시 처치/제압/0배율, 비용 1회, 기존 CMS 보존, 실제 GSAP의 두 결과 1회 적용 및 중단, 이전 단일 전투 재생, 밸런스 비교. 이 검사는 구현 중 통과했으며 같은 실행 코드에서 배포 전 중복 실행하지 않는다.

동일 덱·양 진영 교대·4종 덱·4개 전투력 구간·두 시드 구간 8,192전: 베르칸 4,094승, **49.98%**. 구간별 50.27% / 49.68%. 단일 보스 평균 스킬 피해는 크라이베른의 **96.06%**. 운영 통계가 아닌 로컬 정식 전투 엔진의 비교이며 다른 편성과 상성의 승률을 보장하지 않는다. 전체 결과는 `preview/mercenary-berkan-sss-v1/balance-report.json`.

기존 관련 회귀와 실제 Pixi 두 화살·두 파열·회피별 표시, 공용 번들·메인 로더 검사는 `docs/mercenary-berkan-twin-release-tests-20260927.json`으로 지정 배포에서 실행한다. 승인 원화와 116개 프레임을 재생성하지 않았다.

## 배포 선택

실제 직전 운영 배포는 Pages `2933fd2b-931f-4c4b-a837-54e9b9997000`, 소스 `9aaf6b2adfd7726c603bf9309540c627bf7ef8ee`로 Pages 목록에서 확인했다. 기존 용병 한 종의 스킬 대상·피해 예산·연출 연결에 한정되므로 `npm run deploy:production -- --scoped`를 사용한다. 인증·공통 트랜잭션·DB 구조·의존성·인프라 변경은 없다. Worker 컴파일, 출시 플래그, Hyperdrive 캐시 OFF, 깨끗한 범위 커밋과 origin/main 일치를 유지한다.

실제 Codex 브라우저에서 PC와 390px 모바일의 두 화살·두 파열을 확인했다. `preview/mercenary-berkan-sss-v1/qa/twin-desktop.png`, `twin-mobile.png`에 보존했다. 격리 계정으로 실제 메인 PVE 토벌 시작 → 성공 → 복귀, PVP 매칭 → 승리 → 복귀를 확인했고 브라우저 오류 로그는 없었다. 배포 결과는 완료 후 기록한다.
