# SS 흑월 라이브 등록

2026-09-22 사용자 **“용병 이름 흑월로 승인 라이브 배포해”** 승인으로 원화·투명 SD·12개 검격 동작·16개 이펙트 프레임을 연결한다.

- 이름 **흑월**, 용병 **V-048**, **SS**, 전열 **VANGUARD**, 기본 전투력 **120,000**(기존 SS 공통 기준).
- 전용 스킬 **흑월 삼연참(MS-048)**: 내려베기 → 올려베기 → 횡베기. 기존 SS 단일 대상 기준인 전체 4.2배·비용 25·재사용 5행동으로 등록하며 CMS에서 조정할 수 있다.
- 피해는 30%·30%·40%로 나눈다. 한 행동의 피해 상한도 같은 비율로 나누므로 3번의 상한을 중복 사용하지 않는다. 회피·방어·보호막·단일 피해 호위를 적용한다. 대상 사망 또는 시전자 제압 시 남은 타격은 취소되고, 부활은 행동 종료 후 처리한다.
- 서버가 세 충돌의 피해·HP·보호막을 확정한다. 공용 V3 PixiJS 8.20.0 / GSAP 3.13.0 시계가 **0.72 / 1.24 / 1.92초**에 결과를 표시한다. 클라이언트가 피해를 다시 계산하지 않는다.
- 기본 공격도 승인한 내려베기 동작을 사용한다. 다른 용병에 스킬을 재배정하면 그 용병의 외형을 유지한다. 새 사운드는 추가하지 않았다.
- 하이퍼팩 SS 등급 내 균등 추첨에 포함한다. 등급 확률·개봉 가격은 변경하지 않는다. 일반 카드 5장과 별도 용병 1장 계약을 유지한다.

## 원본 및 CMS 보존

원화 SHA-256 `42853BDCB6C1843832C7050F5B0FB64D008F60372C90C5397D208D72E82D3230`, SD SHA-256 `8855D70472C08A154A956D32549BE53E34E72C2183D0460D4B9BA8726C0593F8`를 보존한다. 원화와 전투 SD는 서로 다른 경로를 사용한다. 도감용 경량 파일과 CMS 프레임 보기는 별도 파생 자산이다.

정확한 기존 전체 CMS 문서만 48종·31스킬로 확장한다. 기존 용병·스킬·확률·배정·메모를 덮어쓰지 않는다. 흑월이 이미 등록된 문서는 다시 기본값을 강제하지 않으며 운영자의 후속 스킬 해제·밸런스 변경을 보존한다. 운영 계정 재화·보유품을 별도로 지급하거나 수정하지 않는다.

## 전투 로딩과 검증

런타임 식별자 **20260922-heukwol-ss-v1**을 실제 번들과 로더에 함께 반영한다. 공용 V3 소비 번들 9개를 동일 소스로 빌드하고, 일반 PVE/PVP·카우방·폐차장·탑의 진입 로더와 캐시 키를 갱신한다. 기존 하이퍼팩 JPEG 복구 수정과 최신 운영 변경을 유지한다.

흑월 전용 서버·획득·편성 검사 8개, 승인 자산·Pixi 좌표·취소 검사 6개를 통과했다. 스킬·CMS 관련 129개 검사와 기존 전투 회귀를 확인했다. 실제 공유 V3 브라우저 재생을 PC 1440px / 모바일 390px에서 PVE와 PVP 양 진영에 실행해 캔버스 1개·독립 용병 슬롯·삼연참·취소 후 정리·가로 넘침 없음·리소스 실패 없음·JavaScript 오류 없음을 확인했다.

배포 후보는 운영 `faf8b66f`에서 분리한 뒤 최신 클랜 일정 수정 `7d8de7b4`를 보존해 통합했다. 로컬 QA 계정과 캡처·로그는 배포 디렉터리 밖에 보존한다.

## 운영 배포 결과

- 2026-09-22 KST, 배포 코드 `878717075aa73d23ea8d4541a56c6ce74cc4b440`을 깨끗한 후보에서 `npm run deploy:production`으로 배포했다.
- 같은 명령의 필수 `npm run release:gate`: **2,241 통과 / 실패 0 / 선택적 외부 PostgreSQL 연결 검사 7개 생략**. 흑월의 SQLite·PostgreSQL 호환 트랜잭션, 편성, PVE/PVP 검사는 통과했다. 새 소스 5개와 관리자 캐시 키에 맞게 기존 검사 목록을 갱신했다.
- Pages 배포: `https://e3a8c381.cnine-card.pages.dev`; 운영 도감 직접 보기 `https://cnine-card.pages.dev/mercenary-codex/?view=all#V-048`(기본 도감은 보유 용병 목록이므로 전체 보기 지정).
- 공통 배포 스크립트의 클랜 드래프트 Worker 버전: `ac21e32e-1b1b-426a-a183-9b295f60b402`. Hyperdrive 쿼리 캐시 OFF와 연결 일치를 통과했다.
- 운영 도감 API에서 **48종·흑월 SS·120,000·MS-048 사용 가능**을 확인했다. 기존 47종의 공개 정보는 배포 전과 완전히 동일하다. 운영 CMS 저장 문서나 유저 재화를 별도로 변경하지 않았다.
- 원화·SD·도감 파생 이미지·동작/이펙트·실제 전투 로더/번들 **13개 경로**를 운영에서 내려받아 후보 파일과 SHA-256 일치를 확인했다.
- 운영의 실제 V3 파일로 PC 1440px·모바일 390px PVE/PVP 4개 조합을 재생했다. 기본 검격, 삼연참, PVP 반대 진영, 중간 취소와 정리 모두 통과했다. 네 조합 모두 리소스 실패·JavaScript 오류·가로 넘침이 없었다. 전투 payload는 로컬 검수용이며 운영 계정의 전투나 보상을 실행하지 않았다.
- 같은 두 화면 폭에서 운영 도감의 흑월 이름·전투력·삼연참 설명·원화 로딩·전투 SD 탭 전환도 확인했다.
