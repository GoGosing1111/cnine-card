# 나무늘봉순 리페어권 1장 지급 — 2026-09-25

- 사용자 요청: `나무늘봉순 리페어권 1장 지급해`
- 운영 조회 대상: 나무늘봉순, user ID `5426`, ACTIVE. 공백 정규화 검색에서도 단일 계정이다.
- 지급 아이템: `PINGDU_REPAIR_COUPON` / 핑두 리페어 쿠폰, 인벤토리 직접 지급 1장.
- 고정 작업 키: `ops:bongsoon-repair-coupon:20260925:v1`.
- 실행 함수: `scripts/ops/bongsoon-repair-grant-20260925.mjs`의 `grantBongsoonRepairCoupon`.

## 검증 및 실행 범위

- 대상 계정 잠금, 아이템 검증, 인벤토리·미확인 수량 +1, 지급 원장, 관리자 감사 로그와 완료 영수증을 한 트랜잭션으로 처리한다.
- 동일 작업 키 재시도는 기존 영수증을 반환한다. 영수증 없이 지급 원장만 있으면 지급을 중단한다.
- 관련 PostgreSQL 회귀: `node --test tests/bongsoon-repair-grant-20260925.test.mjs` — 3개 테스트 통과. 기존/없는 인벤토리, 중복 재시도, 원장·감사·영수증 실패 롤백과 복구 후 재시도, 잘못된 대상·아이템 거부를 확인했다.
- 기존 Hyperdrive 바인딩을 사용하는 단기 토큰·액션 제한 원격 실행기에서 처리한다. 쿼리 캐시 OFF를 확인했다. DB 계정·데이터베이스 검증과 연결 종료를 적용한다.
- 게임 런타임·스키마 변경이 없는 일회성 운영 지급이다. 관련 검사·범위 커밋·원격 반영과 운영 DB 적용으로 완료하며 게임 재배포·전체 검사는 실행하지 않는다.

## 운영 결과

- 완료: **2026-09-25 22:35:09 KST** (`2026-09-25T13:35:09.135Z`).
- 인벤토리 보유 수량 **0 → 1장**, 미확인 수량 **0 → 1장**.
- 지급 원장 `37593871`, 관리자 감사 로그 `35958`, 완료 영수증 `COMPLETED`.
- 별도 읽기 전용 연결에서 영수증·지급 원장 1건·감사 로그 1건 및 현재 보유 1장을 확인했다.
- 적용·검증 실행기는 모두 종료했고 DB 연결도 닫았다.
