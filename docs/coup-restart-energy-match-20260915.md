# 쿠데타 재시작 · 행동력 · 매칭 · 출격 보상

2026-09-15 사용자 요청을 반영한다.

- 기본 행동력은 최대 10회, 출격당 1회 소모, 2분당 1회 충전이다. 지도 위에 잔량과 다음 충전을 표시하고 참가 전에도 정책을 표시한다. 서버 시각 기준으로 화면의 충전을 갱신하며 서버가 실제 소모를 검증한다. 결집 50회 및 원폭 회복 차단은 기존 정책을 따른다.
- 출격자는 현재 PVP 활성 덱·장비·용병을 사용한다. 매칭 후보는 저장된 전투력으로 가까운 8명을 우선 조회하고, 후보의 최신 편성 전투력을 공용 계산식으로 다시 산출한다. 공통 고유효과·마법·용병 자료를 묶어서 읽어 대기시간을 줄인다. 후보 캐시는 최대 5초, 실제 전투에 들어가는 양쪽 편성은 다시 읽는다.
- 근접 전투력 범위(기본 15%, 가장 가까운 상대와의 차이 + 본인 전투력 10% 중 큰 값) 안에서 최근 12회 대전 상대를 분산한다. 다른 근접 상대가 있으면 직전 상대는 제외하고, 최근 대전 횟수가 적은 상대를 우선한다. 상대가 한 명뿐이면 재대전할 수 있다.
- 완료된 출격마다 승리·무승부 2,000만, 패배 1,000만 코인을 지급한다. 공격자의 행동력·대기시간·전선 피해·코인·불변 전투 영수증을 한 트랜잭션으로 처리한다. 재조회나 동일 요청 재전송은 추가 지급하지 않는다.
- 실제 V3 렌더러와 버전, 라이브 준비 게이트는 변경하지 않는다. 영토전의 기존 편성 산출 함수를 추가로 내보내서 사용하며, 같은 V3 서버 전투와 재생 경로를 유지한다.

## 이번 회차 시범 운영

운영 회차 `07306811-48cf-43ef-8cc3-58a810fdd2e3`에만 `settings_json.rebelTrial = {roundId, prisonHours:3}`을 저장했다. 이 ID와 일치할 때만 반란군 패배 처리를 코인 차감에서 반란군 전원 3시간 수감으로 바꾼다. 족장팀 패배의 수감·재판 규칙은 기존과 같다. 다음 회차 생성은 전역 설정의 허용된 필드만 사용하므로 예외를 상속하지 않는다.

정산 시 수용동과 수감 명단을 회차 정산과 한 번에 기록한다. 재시도는 중복 수감이나 형기 연장, 석방자 재수감을 일으키지 않는다. 포로수용소에서도 반란군으로 표시한다.

## 운영 재시작 기록

- 작업 영수증: `ops-coup-restart-3500000-trial-3h-20260915-01`
- 적용 시각: 2026-09-15 18:18:13 KST
- 기존 팀: 족장팀 59명, 반란군 98명, 총 157명 유지
- 전선: 황궁 광장(중앙, index 2)
- 양 진영 현재·최대 HP: 3,500,000
- 재시작 시각부터 기존 설정 180분 적용, 종료 21:18:13 KST
- 단일 회차 행 잠금, revision 갱신, 명단 지문 비교, 사전·사후 값과 작업 영수증을 한 트랜잭션에 기록했다. 같은 운영 명령의 재실행은 다시 리셋하지 않는다.

## 검증

SQLite와 PostgreSQL 호환 계층에서 3시간 예외·다음 회차 비상속·기존 코인 페널티, 행동력 경계, 최신 덱·장비 반영, 상대 분산, 승패별 보상·중복 요청·동시 출격·강제 오류 롤백을 검증한다. 실제 JS/CSS를 사용한 1440px·390px·320px 화면에서 정책/잔량·충전 후 버튼 활성화·규정 팝업·누락 응답을 검수한다. 운영 DB에서는 전투를 실행하지 않고 읽기 전용으로 후보 준비 시간을 측정한다.

운영 DB 읽기 전용 측정에서 본인 최신 편성 조회 1.97초, 근접 후보 8명 준비 2.72초였다. 후보 공통 자료를 묶기 전의 11.61초보다 단축됐다. 전투 시뮬레이션과 네트워크 전체 시간을 나타내는 값은 아니다.
