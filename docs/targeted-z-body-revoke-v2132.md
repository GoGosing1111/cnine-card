# 진짜디임 Z-BODY 일회 회수 · V2132

2026-09-21 사용자 요청에 따라 활성 계정 `진짜디임`의 `BATTLE_SUIT_Z_BODY` 보유 인스턴스를 전량 회수한다.

- 닉네임이 정확히 하나의 활성 계정으로 식별될 때만 실행한다.
- Z-BODY 장착을 먼저 해제하고, 해당 인스턴스의 강화 상태를 정리한 뒤 보유 인스턴스를 삭제한다.
- 다른 배틀슈트, 다른 장비, 다른 계정의 Z-BODY, 재화와 스킬칩은 변경하지 않는다.
- 회수 전 인스턴스·획득 경로·장착 여부·강화 상태는 `targeted_equipment_revoke_v2132_audit`에 보존한다.
- 전체 변경은 단일 배치 트랜잭션으로 처리하며 보유 수가 사전 조회 뒤 바뀌면 전부 롤백한다.
- 완료 마커 `targeted_equipment_revoke_v2132_jinjja_diem_z_body`로 중복 실행을 차단한다. 완료 뒤 새로 획득한 Z-BODY를 자동으로 반복 회수하지 않는다.
- 운영 health에는 계정 식별자 없이 회수 수량과 최종 부재 검증만 노출한다.
