# 2026-09-21 지정 보상 지급

- `블랙캐슬`: 운영 용병 CMS에서 지급 시점에 `S` 등급으로 확정된 후보 중 암호학적 난수로 1종을 선택해 1장을 지급한다. 기존 보유 카드가 선택되면 정상 중복 1장으로 기록한다.
- `구와함`: 숲켓몬 코인 `300,000,000,000`을 지급한다.
- `공단`: 활성 공개 카탈로그의 `BATTLE_SUIT_H_BODY` (`H-BODY`) 인스턴스 1개를 지급한다.

세 계정의 활성·유일성, 활성 OWNER, 용병 등급, H-BODY 카탈로그, 지급 전 잔액·보유량을 확인한다. 세 지급과 코인 로그·용병 획득 영수증·장비 요청 ID·관리자 감사 로그·완료 마커는 하나의 트랜잭션으로 처리한다. 일부만 성공하는 상태는 허용하지 않으며 같은 작업을 재실행해도 추가 지급하지 않는다.

운영 완료 마커는 `targeted_reward_grant_20260921_blackcastle_guwaham_gongdan_v1`이다.
