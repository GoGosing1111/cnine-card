# 영토전 종료 후 세력전 로딩 복구 — 2026-09-25

사용자 신고: 영토전 종료 후 세력전 진입이 `중단된 세력전의 영토전 종료 기록을 확인하지 못했습니다.`로 막힘.

## 원인과 수정 범위

- 시즌 5의 `2026-09-23:2` 회차가 영토전 58 시작 시각 `2026-09-23T14:16:15.267Z`에 중단됨. 당시 회차 번호 없이 시각만 저장됨.
- 같은 영토전의 클랜 재배정·재시작이 `starts_at`을 `14:43:50.842Z`로 변경해 기존 중단 시각을 포함하는 종료 구간이 사라짐. 영토전 58은 9월 24일 08:49:12 UTC에 종료됐지만 세력전 정산/진입이 409로 중단된 상태였음.
- 중단 기록에 `territoryRoundIds`를 저장하고 같은 회차가 재시작돼도 저장된 중단 경계를 유지함. 겹친 영토전은 모두 종료된 뒤 재개하며, 최근 종료 32개에서 빠진 참조 회차만 추가 일괄 조회함. 정상 경로의 조회 수·폴링 주기는 늘리지 않음.
- 확인 가능한 구형 중단 기록은 정상 조회 시 번호를 보완함. 근거가 없는 중단 기록을 다른 영토전으로 추정하거나 삭제하지 않음.
- 서버 세력전 회차 모듈만 변경. 공통 DB·인증·경제 정책·CMS·전투 엔진 변경 없음.

## 운영 데이터 복구 근거

- 관리자 감사 35272 `TERRITORY_CLAN_RANK_RESTART`가 가리키는 `ops:territory-58-rank-restart-20260923:before` 원본의 영토전 시작 시각이 저장된 세력전 중단 시각과 정확히 일치함.
- `scripts/ops/faction-pause-repair-20260925.mjs`는 해당 시즌·회차·revision·감사·원본·종료 상태를 모두 확인한 뒤 중단 기록에 `[58]`만 추가함. 원본 백업, 수정, 감사, 완료 영수증은 단일 트랜잭션. 재실행은 영수증 반환만 수행함.
- 실제 DB에서 dry-run 후 rollback 완료. 운영 상태를 복사한 로컬 모의 실행에서는 점령 25개와 참여 62명 보존, 기존 정책에 따른 `2026-09-23:2` 종료 시각 9월 24일 09:12:56.733 UTC, 정상 보상 62건(각 300억 코인 메시지) 정산 및 재실행 시 추가 지급 0건 확인.
- 9월 25일 영토전 60 승리팀 추가 보상은 이미 별도 지급 완료된 기록으로 이 작업에서 변경하지 않음.

## 관련 검증과 배포 범위

- 수정 전 SQLite·PostgreSQL에서 같은 오류 재현. 수정 후 재시작, PREPARING 유예, 최근 32개 밖 종료 기록, 겹친 영토전, 근거 없는 구형 중단 차단, 상태 쓰기 실패 rollback, 남은 시간·전투 타이머·점령·참여자 보존, 재시도 보상 중복 방지를 회귀검사함.
- 복구 작업 PostgreSQL 검사: dry-run rollback, 감사 저장 실패 시 전체 rollback, revision/원본 불일치 차단, 1회 수정·재실행 안전성 통과.
- 실제 index/메인 메뉴/세력전 UI를 사용하는 로컬 합성 계정으로 1440×1000 / 390×844에서 진입→중단→동일 영토전 재시작→종료→재개→새로고침 재진입 통과. 지도 25개, 점령 상태, 남은 시간 보존 및 가로 넘침/브라우저 예외 없음. 스크린샷 직접 확인.
- 화면 검사: `tests/clan-faction-pause-recovery.browser.mjs`; 결과 `C:/Users/User/AppData/Local/Temp/faction-pause-recovery-mXwqLc/`.
- 직전 실제 운영 기준: `05ff3a511093ac7b6642a05ab104c645800235a3` / Pages `537a8ea3-0a9a-423f-83da-d9daa1e90188`. Wrangler 운영 배포 목록으로 확인. 그 이후 기존 커밋은 개별 운영 도구·검사·기록으로 게임 실행 코드 변경 없음.
- 작은 수정: `npm run deploy:production -- --scoped`. 선정 검사: `tests/clan-faction-sessions.test.mjs`, `tests/clan-faction-sessions-policy.test.mjs`, `tests/clan-faction-v1.test.mjs`, `tests/faction-pause-repair-20260925.test.mjs`. 선택 이유: 회차 복구와 해당 거래의 원자성·재시도, 연결된 세력전 경로에 한정된 수정. Worker·출시 플래그·캐시·Hyperdrive 검사는 지정 배포 도구에서 유지.

운영 반영 결과는 배포 및 실제 복구 확인 후 이 문서에 추가한다.
