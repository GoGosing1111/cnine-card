# 대회 성황리 기념 — 2차 인증자 코인·마스터의 별

- 사용자 지시: `2차인증자 전체 1500억코인에 마별 150만개 지급해 메세지 내용은 대회 성황리 기념`.
- 대상: 발송 시점 `user_second_verifications` 기록을 가진 ACTIVE 계정 전체. 인증된 USER·ADMIN·OWNER를 포함한다.
- 계정당 보상 메시지 2통: COIN **150,000,000,000(1,500억)**, MASTER_STAR **1,500,000(150만)**. 두 메시지의 제목과 본문은 모두 **대회 성황리 기념**이다.
- 기존 메시지당 단일 보상/원자적 수령 경로를 사용한다. 발송 시 잔액이나 별 재고를 직접 변경하지 않는다. 신규 대회 사은품 상자는 이번 발송과 별개다.
- 명시적으로 지정한 이번 수량에 한정한 일회성 발송이며 일반 CMS 발송 한도, 인증 상태, 게임 코드와 미래 지급 정책은 변경하지 않는다.

## 실행·중복 방지

- 운영 도구: `scripts/ops/tournament-celebration-gift-20260926.mjs`.
- 고정 작업 키: `ops:tournament-celebration:verified:20260926:v1`.
- 캠페인: `tournament-celebration-coin-20260926-v1`, `tournament-celebration-star-20260926-v1`.
- 고정 대상 해시, 기존 동일 지급 유무, 계정별 메시지/보상 행수와 금액을 확인한다. 두 캠페인·감사 로그·완료 영수증을 단일 REPEATABLE READ 트랜잭션으로 확정하며 실패하면 모두 롤백한다. 동일 키 재시도는 기존 결과만 검증한다.
- 운영 실행기는 배포 트리 밖에 두고 5분 만료 임시 토큰과 지정 동작만 허용한다. `cnine` 주 DB와 `cnine_migrator` 역할, Hyperdrive 캐시 OFF 및 Pages/Worker 바인딩 일치를 확인했다. 작업 후 원격 프리뷰를 닫는다.
- 대상 한도 5,000명, 잠금 대기 5초, 문장 20초 제한. 대상/보상은 일괄 조회·삽입하며 계정별 N+1 조회를 추가하지 않는다.

## 관련 검증

- `node --test --test-concurrency=1 tests/tournament-celebration-gift-20260926.test.mjs`: 실제 PostgreSQL(PGlite) 7개 통과. 모든 역할/정상 인증 대상, 정확한 두 보상, 재시도/대상 고정, 감사·두 번째 보상·행수·완료 기록 오류의 전체 롤백, COMMIT 응답 유실, 다른 캠페인의 중복 지급 차단.
- `node --test --test-name-pattern='1500억 COIN|대회 성황리 기념' tests/message-gift-rewards-v2044.test.mjs`: 기존 수령 함수 4개 통과. 코인/별 전액, 동시 재시도 1회 지급, 실패 롤백, 일괄 수령에서 별 실패 후 코인 재지급 없이 복구.
- 처음 검사에서 운영 도구 반환 메타데이터까지 감사 로그와 비교하던 검증 오류를 발견해 저장 영수증 필드만 비교하도록 수정했다. 최종 관련 검사 총 11개 통과.
- 운영 사전 조회: **487명**(USER 483, ADMIN 2, OWNER 2), 동일 지급 및 완료 기록 없음.
- 대상 해시: `49282cbc53e26266bee31f3b0c5188e9121a6683f984d8f8c016928407eb696e`.
- 총액: **73,050,000,000,000 COIN**, **730,500,000 MASTER_STAR**. 계정당 두 통, 메시지/보상 각 **974건**.
- 운영 ROLLBACK 예행에서 두 보상 974건의 대상·수량·미수령 상태를 검증했고, 별도 조회로 발송/완료 기록이 남지 않았음을 확인했다.
- 일회 도구·검사·문서만 변경하므로 범위 커밋/원격 반영 후 실행한다. 게임 전체 검사와 운영 빌드 재배포는 하지 않는다.

## 운영 발송 결과

- **2026-09-26 21:38:01 KST** (`2026-09-26T12:38:01.038Z`) 발송 완료. 실행 커밋 `08ad6f5d10c2e3e6820354af2a02b17c584c62c9`은 깨끗한 범위 커밋으로 `origin/main`에 반영한 뒤 실행했다.
- 고정 대상 **487명**에게 COIN 메시지 487건과 MASTER_STAR 메시지 487건, 총 메시지/보상 **974건**을 한 트랜잭션으로 COMMIT했다.
- 별도 읽기 전용 DB 연결에서 수신자 전체·두 제목/본문·보상 종류와 정확한 수량·완료 영수증·감사 로그 일치를 확인했다. 누락 **0**, 중복 **0**.
- 감사 로그 ID **36165**, 액션 `VERIFIED_REWARD_MESSAGE_SEND`. 첫 검증 시 두 보상 모두 미수령 487건씩으로, 기존 메시지함에서 수령 가능하다.
- 운영 지갑/별 재고를 직접 변경하거나 대신 수령하지 않았다. 임시 원격 프리뷰를 종료했으며 게임/영구 관리 Worker 재배포는 없다.
- 원격 반영 중 선행된 군단 토벌 화면 변경을 rebase로 보존했다. 이번 지급 도구와 수령 서버 코드에는 영향을 주지 않는다.
