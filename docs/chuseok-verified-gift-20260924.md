# 추석 명절 기념 선물 — 2026-09-24

- 사용자 승인: 2차 인증 전체 유저에게 1인당 **1,000억 코인(100,000,000,000)**, 제목과 본문은 모두 **추석 명절 기념 선물**. 기존 CMS 한도와 무관하게 이번 지급을 진행하라는 재확인에 따라 일회성 운영 발송으로 처리했다.
- 발송 완료: **2026-09-24 03:39:31 KST**, 운영 DB `cnine`, 실행 역할 `cnine_migrator`. 실행 코드 커밋 `96a64db2`.
- 대상: 발송 시점 `user_second_verifications`에 등록된 활성 계정 **479명**. 일반 유저 475명, 관리자 2명, OWNER 2명으로 인증된 전체 역할을 포함했다. 미인증·비활성 계정은 제외했다.
- 지급 방식: **메시지함 수령**, 계정당 메시지 1건과 코인 보상 1건. 발송만으로 지갑 잔액을 직접 변경하지 않는다. 총 보상액은 **47,900,000,000,000 코인(47조 9,000억)**이다.
- 캠페인: `chuseok-verified-1000eok-20260924-v1`.
- 완료 영수증: `ops:chuseok-verified-1000eok:20260924:v1`, 상태 `COMPLETED`.
- 감사 로그: `VERIFIED_REWARD_MESSAGE_SEND`, ID **35432**. 요청 승인, `SYSTEM_OPS` 작업 주체, 금액, 제목·본문, 확정 대상과 대상 해시를 저장했다. 개인별 대상 목록은 운영 영수증에만 보관하고 Git에는 넣지 않는다.
- 대상 해시: `b477ffa358e947e40303ece8710cc104c23a40252199fbd16542480c9b7229ba`.

## 거래 안전 및 확인

- 사전 조회에서 동일 제목·본문·금액의 기존 발송이 없음을 확인했다. 조회 당시의 대상 해시를 실제 실행에서 대조해 대상이 바뀌면 발송을 중단한다.
- 메시지, 보상, 감사 로그와 완료 영수증을 하나의 반복 읽기 트랜잭션으로 기록했다. 유일한 작업 키가 재시도를 직렬화하며, 완료 후 재실행은 기존 발송만 검증하고 추가 발송하지 않는다. 나중에 인증한 계정을 같은 작업 재실행으로 추가하지 않는다.
- 로컬 PGlite 관련 검사 **5개 통과**: 모든 활성·인증 역할과 금액/본문, 재시도 무중복, 기존 지갑 보존, 대상 변경 차단, dry-run 무변경, 보상·감사 실패 전체 롤백 및 실패 후 재시도, 다른 캠페인의 중복 발송 차단.
- 운영 연결에서 먼저 전체 거래 dry-run 후 롤백했다. 실발송 후 별도 읽기 연결로 **메시지 479건 / 보상 479건 / 중복 0건**, 확정 대상별 1,000억 및 제목·본문 일치를 확인했다.
- 기존 수령 로직은 안전한 정수 보상을 처리하며 생성 화면의 CMS 상한을 수령 시 적용하지 않음을 확인했다. 전역 CMS 한도·기능 플래그·인증 상태·확률은 변경하지 않았다.
- Cloudflare/Workers/Wrangler 절차에 맞춰 기존 Hyperdrive 연결을 사용한 임시 비공개 실행으로만 처리했다. 매 실행 일회용 인증값과 5분 만료, 허용 작업 제한을 적용하고 연결 및 실행 서버를 종료했다.
- 실행 스크립트·관련 검사·기록만 변경했다. 게임 런타임·UI 변경이 없어 범위 커밋 및 원격 반영으로 마무리하며 전체 게임 검사나 운영 재배포를 하지 않는다.
