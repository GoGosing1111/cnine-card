# v7 FIX2 검증/수정

- 관리자 설정 화면에 `등급별 돌파 설정` 실제 포함 확인
- admin.css / admin.js 캐시 버전 파라미터 추가
- Cloudflare Pages `_headers`로 `/admin/*` 캐시 방지
- 설정 화면에 `v7 FIX2` 빌드 배지 추가
- 돌파 설정 API 로딩 성공/오류 상태를 화면에 직접 표시
- 등급 변경 시 이전 등급 입력값이 새 등급에 덮어써지던 버그 수정
- 저장 버튼 중복 클릭 방지 및 D1 저장 완료 상태 표시

배포 후 관리자 설정 화면에서 `v7 FIX2` 배지가 보이면 최신 파일이 적용된 것입니다.
