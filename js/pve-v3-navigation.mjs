const destinations=new Set(['tower','scrapyard','cow-room','idle-dungeon']);
const feature=fetch('/api/pve/v3/feature',{cache:'no-store'}).then(r=>r.ok?r.json():{enabled:false}).catch(()=>({enabled:false}));
globalThis.PveV3Runtime={async tryOpen(content){if(!destinations.has(content)||(await feature).enabled!==true)return false;location.assign(`/pve-v3/?content=${encodeURIComponent(content)}`);return true;}};
feature.then(value=>{if(value.enabled!==true)return;const add=()=>{const nav=document.querySelector('.pve-mode-tabs');if(!nav||nav.querySelector('[data-v3-cow-entry]'))return;const button=document.createElement('button');button.className='pve-mode-btn';button.dataset.v3CowEntry='';button.type='button';button.textContent='카우방';button.onclick=()=>void globalThis.PveV3Runtime.tryOpen('cow-room');nav.append(button);const mercenary=document.createElement('a');mercenary.className='pve-mode-btn';mercenary.href='/mercenary-hangar/';mercenary.textContent='용병 지휘소';nav.append(mercenary);};add();const observer=new MutationObserver(add);observer.observe(document.body,{childList:true,subtree:true});addEventListener('pagehide',()=>observer.disconnect(),{once:true});});
