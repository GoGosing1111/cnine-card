/* V1388 VEHICLE DRAW UI */
(()=>{const esc=v=>typeof escapeHtml==='function'?escapeHtml(String(v??'')):String(v??'').replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));const rarity={NORMAL:'일반',MAGIC:'고급',RARE:'희귀',EPIC:'영웅',LEGENDARY:'전설',MYTHIC:'신화'};
async function open(owned=0){const modal=document.getElementById('modal');if(!modal)return;modal.className='modal show vehicle-draw-modal-v1388';modal.innerHTML='<div class="modal-panel vehicle-draw-panel-v1388"><div class="vehicle-draw-loading-v1388"><i></i><b>이동수단 시스템 연결 중</b><span>뽑기 설정을 확인하고 있습니다.</span></div></div>';try{const d=await apiRequest('vehicle-draw/config',{}, {ttl:0}),s=d.settings||{},pool=(d.vehicles||[]).filter(x=>x.drawEnabled&&x.isActive&&x.isPublic);modal.innerHTML=`<div class="modal-panel vehicle-draw-panel-v1388"><button type="button" class="icon-close" id="vehicleDrawCloseV1388">×</button><header><small>${esc(s.drawTitle||'VEHICLE ACQUISITION')}</small><h2>이동수단 뽑기</h2><p>${esc(s.drawCopy||'새로운 이동수단을 획득합니다.')}</p></header><div class="vehicle-draw-stage-v1388"><span class="ring a"></span><span class="ring b"></span><div class="vehicle-draw-ticket-v1388">${s.ticketImage?`<img src="${esc(s.ticketImage)}" alt="">`:'<b>CNINE</b>'}<em>VEHICLE DRAW</em></div></div><div class="vehicle-draw-meta-v1388"><span>보유 뽑기권 <b>${Number(d.ticketQuantity||owned).toLocaleString()}개</b></span><span>등록 풀 <b>${pool.length}종</b></span></div><div class="vehicle-draw-bonus-v1388"><b>MASTER STAR BONUS</b><span>이동수단 결과와 별도로 ${Number(s.masterStarChance||0)}% 확률 판정</span></div><button type="button" class="btn vehicle-draw-confirm-v1388" id="vehicleDrawConfirmV1388" ${Number(d.ticketQuantity||0)>0&&pool.length?'':'disabled'}>${Number(d.ticketQuantity||0)>0?(pool.length?'1회 뽑기':'뽑기 풀 준비 중'):'뽑기권 없음'}</button></div>`;const close=()=>{modal.className='modal';modal.innerHTML=''};document.getElementById('vehicleDrawCloseV1388').onclick=close;const btn=document.getElementById('vehicleDrawConfirmV1388');if(btn)btn.onclick=async()=>{const panel=modal.querySelector('.vehicle-draw-panel-v1388'),requestId=crypto?.randomUUID?.()||`${Date.now()}-${Math.random().toString(36).slice(2)}`;btn.disabled=true;btn.textContent='이동수단 호출 중';panel.classList.add('opening');try{const r=await apiRequest('vehicle-draw/open',{method:'POST',body:JSON.stringify({requestId})});clearApiCache('inventory');clearApiCache('character');clearApiCache('shell/summary');await new Promise(x=>setTimeout(x,1200));panel.classList.remove('opening');panel.classList.add('revealed');panel.innerHTML=`<div class="vehicle-result-v1388 rarity-${esc(String(r.vehicle.rarity||'NORMAL').toLowerCase())}"><small>${esc(r.duplicate?'DUPLICATE VEHICLE':'NEW VEHICLE')}</small><h2>${esc(r.vehicle.name)}</h2><div class="vehicle-result-image-v1388">${r.vehicle.image?`<img src="${esc(r.vehicle.image)}" alt="${esc(r.vehicle.name)}">`:'<b>NO IMAGE</b>'}</div><strong>${esc(rarity[r.vehicle.rarity]||r.vehicle.rarity)}</strong><p>${esc(r.vehicle.description||'새로운 이동수단을 획득했습니다.')}</p><div class="vehicle-result-rewards-v1388">${r.duplicate?`<span>중복 환산 <b>카드 조각 +${Number(r.shardsGained||0).toLocaleString()}</b></span>`:'<span>차고지 등록 <b>신규 획득</b></span>'}${Number(r.masterStarsGained||0)>0?`<span>보너스 <b>마스터의 별 +${Number(r.masterStarsGained).toLocaleString()}</b></span>`:''}</div><button type="button" class="btn" id="vehicleResultDoneV1388">인벤토리로 돌아가기</button></div>`;document.getElementById('vehicleResultDoneV1388').onclick=()=>{close();renderShell('inventory')}}catch(e){panel.classList.remove('opening');btn.disabled=false;btn.textContent='1회 뽑기';alert(e.message)}}}catch(e){modal.innerHTML=`<div class="modal-panel vehicle-draw-panel-v1388"><div class="vehicle-draw-error-v1388"><b>이동수단 뽑기를 열 수 없습니다.</b><span>${esc(e.message)}</span><button type="button" class="btn" id="vehicleDrawErrorCloseV1388">닫기</button></div></div>`;document.getElementById('vehicleDrawErrorCloseV1388').onclick=()=>{modal.className='modal';modal.innerHTML=''}}}
window.VehicleDrawV1388={open};})();
