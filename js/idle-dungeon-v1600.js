(()=>{let data,selected='NORMAL';const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),n=v=>Number(v||0).toLocaleString();function ensure(){const nav=document.querySelector('.pve-mode-tabs');if(!nav)return;let btn=nav.querySelector('[data-pve-mode="idle"]');if(!btn){btn=document.createElement('button');btn.type='button';btn.className='pve-mode-btn';btn.dataset.pveMode='idle';btn.textContent='방치형 원정';nav.appendChild(btn)}let view=document.getElementById('pveIdleDungeonView');if(!view){view=document.createElement('section');view.id='pveIdleDungeonView';view.className='idle-dungeon-view';nav.parentElement.appendChild(view)}if(!btn.dataset.idleBound){btn.dataset.idleBound='1';btn.onclick=async()=>{document.querySelectorAll('[data-pve-mode]').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('#pveHuntView,#pveTowerView,#pveIdleDungeonView').forEach(x=>x.style.display=x===view?'block':'none');view.classList.add('active');await load()}}}async function load(){const view=document.getElementById('pveIdleDungeonView');view.innerHTML='<div class="idle-dungeon-shell">원정 기록을 불러오는 중...</div>';try{data=await apiRequest('idle-dungeon/status',{}, {ttl:0});selected=data.progress.difficulty||'NORMAL';render()}catch(e){view.innerHTML=`<div class="idle-dungeon-shell"><h3>진입할 수 없습니다</h3><p>${esc(e.message)}</p></div>`}}function render(){const v=document.getElementById('pveIdleDungeonView'),p=data.progress,m=data.monster||{},d=data.settings.difficulties.find(x=>x.id===selected)||data.settings.difficulties[0],ratio=Math.min(100,p.dailyCoin/Math.max(1,p.dailyCap)*100);v.innerHTML=`<div class="idle-dungeon-bg"></div><div class="idle-dungeon-shell"><header class="idle-dungeon-head"><div><small>SOOPKETMON IDLE EXPEDITION</small><h2>달빛 성채 자동 원정</h2><p>저장된 실제 PVE 덱으로 자동 등반합니다. 패배하면 1층부터 재등반합니다.</p></div><div class="idle-power"><small>현재 PVE 전투력</small><b>${n(data.deckPower)}</b><span>최종 봉쇄 ${n(data.finalBarrierPower)}</span></div></header><nav class="idle-difficulties">${data.settings.difficulties.map(x=>`<button data-idle-diff="${x.id}" class="${selected===x.id?'active':''}" ${x.unlocked?'':'disabled'}><b>${esc(x.name)}</b><br><small>6시간 ${n(x.dailyCap)}</small></button>`).join('')}</nav><div class="idle-stage"><div class="idle-arena"><div class="idle-floor"><span>${esc(d.name)} 원정</span><b>${p.currentFloor}F / ${d.maxFloor}F</b><span>${esc(m.name||'몬스터 탐색 중')}</span></div><div class="idle-runner"><img src="assets/ui/idle-dungeon/runner-party-cycle-clean-v4.png" alt="원정대"></div><img class="idle-monster" src="${esc(m.image||'assets/ui/idle-dungeon/moon-citadel-v1.png')}" alt="${esc(m.name)}" onerror="this.src='assets/ui/idle-dungeon/moon-citadel-v1.png'"></div><aside class="idle-panel"><small>EXPEDITION RECORD</small><h3>${p.running?'자동 등반 진행 중':'원정 대기 중'}</h3><div class="idle-stat"><span>최고 도달</span><b>${p.highestFloor}층</b></div><div class="idle-stat"><span>재등반</span><b>${n(p.resets)}회</b></div><div class="idle-stat"><span>정산 대기</span><b>${n(p.pendingCoin)} 코인</b></div><div class="idle-stat"><span>오늘 획득</span><b>${n(p.dailyCoin)} / ${n(p.dailyCap)}</b></div><div class="idle-progress"><i style="width:${ratio}%"></i></div><div class="idle-actions"><button class="primary" id="idleStart">${p.running?'선택 난이도로 재출정':'자동 원정 시작'}</button><button id="idleClaim" ${p.pendingCoin?'':'disabled'}>누적 코인 수령</button><button id="idleRefresh">진행 갱신</button></div></aside></div><div class="idle-note">일반 100만 · 심연 130만 · 종말 150만이 6시간 기준 일일 상한입니다. 최종 봉쇄층은 실제 최고 PVE 기록보다 높게 자동 산정됩니다.</div></div>`;v.querySelectorAll('[data-idle-diff]').forEach(b=>b.onclick=()=>{selected=b.dataset.idleDiff;render()});v.querySelector('#idleRefresh').onclick=load;v.querySelector('#idleStart').onclick=async()=>{await apiRequest('idle-dungeon/start',{method:'POST',body:JSON.stringify({difficulty:selected})});await load()};v.querySelector('#idleClaim').onclick=async()=>{const r=await apiRequest('idle-dungeon/claim',{method:'POST',body:JSON.stringify({requestId:crypto.randomUUID()})});const t=document.createElement('div');t.className='idle-toast';t.textContent=`${n(r.rewardCoin)} 코인을 수령했습니다.`;document.body.appendChild(t);setTimeout(()=>t.remove(),2400);await load()}}new MutationObserver(ensure).observe(document.documentElement,{childList:true,subtree:true});addEventListener('DOMContentLoaded',ensure);setInterval(()=>{if(document.getElementById('pveIdleDungeonView')?.classList.contains('active'))load()},60000)})();
