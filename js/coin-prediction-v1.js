(()=>{
  const MIN_BET=100000,USER_MAX_BET_PER_EVENT=30000000;
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const fmt=v=>Number(v||0).toLocaleString('ko-KR');let state=null,timer=0,pollTimer=0,busy=false,termsAccepted=false;
  const api=(p,o={},c={})=>window.apiRequest(p,o,c);
  const eventStatus=e=>e.status==='OPEN'?'참여 중':e.status==='CLOSED'?'진행 중':e.status==='SETTLED'?'정산 완료':'무효·환불';
  const eventStatusDetail=e=>e.status==='CLOSED'?'<em class="cp-status-detail">(베팅 마감)</em>':'';
  function predicted(e,o){const pool=Number(e.total_pool||0),mine=Number(o.total_bet||0);if(!pool||!mine)return'집계 중';return `예상 ${(pool*.9/mine).toFixed(2)}배`}
  function timeParts(ms){const seconds=Math.max(0,Math.ceil(ms/1000));return{hours:String(Math.floor(seconds/3600)).padStart(2,'0'),minutes:String(Math.floor(seconds%3600/60)).padStart(2,'0'),seconds:String(seconds%60).padStart(2,'0')}}
  function orderedEvents(events,now=Date.now()){
    return (Array.isArray(events)?events:[]).map((event,index)=>{const closeAt=Date.parse(event?.closes_at||''),active=event?.status==='OPEN'&&(!event?.closes_at||closeAt>now);return{event,index,active,closeAt:Number.isFinite(closeAt)?closeAt:Number.MAX_SAFE_INTEGER}}).sort((a,b)=>{
      if(a.active!==b.active)return a.active?-1:1;
      if(a.active&&a.closeAt!==b.closeAt)return a.closeAt-b.closeAt;
      if(!a.active){const idDiff=Number(b.event?.id||0)-Number(a.event?.id||0);if(idDiff)return idDiff}
      return a.index-b.index;
    }).map(x=>x.event);
  }
  function clockMetrics(e,remain){const end=Date.parse(e.closes_at||''),start=Date.parse(e.created_at||''),total=Number.isFinite(start)&&Number.isFinite(end)&&end>start?end-start:Math.max(remain,1),ratio=Math.max(0,Math.min(1,remain/total));return{ratio,offset:(301.593*(1-ratio)).toFixed(2),level:remain<=300000?'is-critical':remain<=3600000?'is-warning':''}}
  function optionShare(e,o){const pool=Number(e.total_pool||0);return pool>0?Math.max(0,Math.min(100,Number(o.total_bet||0)/pool*100)):0}
  const wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));
  async function betRequest(payload){let lastError=null;const body=JSON.stringify(payload);for(let attempt=0;attempt<3;attempt++){try{return await api('coin-prediction/bet',{method:'POST',body},{timeoutMs:30000})}catch(error){lastError=error;const retry=!Number(error?.status)||Number(error?.status)>=500||/처리 중/.test(String(error?.message||''));if(!retry||attempt===2)throw error;await wait(350*(attempt+1))}}throw lastError}
  function bettorLedger(e,o){const bettors=Array.isArray(o.bettors)?o.bettors:[],key=`${e.id}:${o.id}`;return `<details class="cp-bettors" data-cp-bettors="${key}"><summary><span>참여자별 베팅 현황</span><b>${fmt(bettors.length)}명</b></summary><div class="cp-bettor-list">${bettors.length?bettors.map((b,i)=>`<div class="cp-bettor-row"><i>${String(i+1).padStart(2,'0')}</i><b title="${esc(b.nickname)}">${esc(b.nickname)}</b><em>${fmt(b.amount)} COIN</em></div>`).join(''):'<p>아직 참여자가 없습니다.</p>'}</div></details>`}
  function card(e){
    const open=e.status==='OPEN'&&state?.settings?.enabled!==false,mine=e.myBet,selected=Number(mine?.option_id||0),result=Number(e.result_option_id||0),ownerUnlimited=state?.settings?.ownerUnlimited===true,currentAmount=Number(mine?.amount||0),walletCoin=Math.max(0,Number(state?.walletCoin||0)),policyMax=Number(e.max_bet||USER_MAX_BET_PER_EVENT),max=ownerUnlimited?currentAmount+walletCoin:policyMax,remaining=ownerUnlimited?walletCoin:Math.max(0,policyMax-currentAmount),remain=e.closes_at?Math.max(0,Date.parse(e.closes_at)-Date.now()):0,image=String(e.image_url||'').replace(/^\/+/,''),canAdd=remaining>=MIN_BET,clock=clockMetrics(e,remain),parts=timeParts(remain);
    const optionCards=(e.options||[]).map((o,i)=>{const share=optionShare(e,o),isSelected=selected===Number(o.id),isWinner=result===Number(o.id);return `<div class="cp-option-block" style="--cp-share:${share.toFixed(2)}%"><button class="cp-option ${isSelected?'selected':''} ${isWinner?'is-winner':''}" data-cp-option="${o.id}" aria-pressed="${isSelected?'true':'false'}" ${open&&(!mine||isSelected)?'':'disabled'}><i class="cp-option-index">${String(i+1).padStart(2,'0')}</i><span class="cp-option-copy"><b>${esc(o.label)}</b><small>${fmt(o.bet_count)}명 · ${fmt(o.total_bet)}코인${isWinner?' · 적중 결과':''}</small><span class="cp-option-meter"><i></i></span></span><em><small>EXPECTED</small>${predicted(e,o)}</em></button>${bettorLedger(e,o)}</div>`}).join('');
    return `<article class="cp-event ${open?'is-open':''}" data-cp-event="${e.id}" data-cp-current="${currentAmount}" data-cp-max-total="${max}" data-cp-unlimited="${ownerUnlimited?'1':'0'}" data-cp-status="${esc(e.status||'')}">${image?`<div class="cp-event-cover"><img src="${esc(image)}" alt="" onerror="this.parentElement.remove()"></div>`:''}<div class="cp-event-head"><div class="cp-event-copy"><div class="cp-event-kicker"><span class="cp-live-dot"></span><small>${eventStatus(e)}${eventStatusDetail(e)}</small><span>${fmt(e.participant_count)}명 참여</span></div><h2>${esc(e.title)}</h2><p>${esc(e.description||'결과를 예측하고 게임 코인으로 참여하세요.')}</p><div class="cp-event-meta"><span>10% FEE</span><span>100,000 MIN</span><span${ownerUnlimited?' class="owner-unlimited"':''}>${ownerUnlimited?'OWNER UNLIMITED':'30,000,000 MAX'}</span></div></div><div class="cp-clock ${clock.level}" data-cp-end="${esc(e.closes_at||'')}" data-cp-start="${esc(e.created_at||'')}" data-cp-open="${open?'1':'0'}"><div class="cp-clock-ring" style="--cp-ring-offset:${clock.offset}"><svg viewBox="0 0 112 112" aria-hidden="true"><circle class="cp-clock-track" cx="56" cy="56" r="48"></circle><circle class="cp-clock-progress" cx="56" cy="56" r="48"></circle></svg><div class="cp-clock-face"><small>${open?'CLOSES IN':'EVENT STATUS'}</small><b data-cp-time>${open?`${parts.hours}:${parts.minutes}:${parts.seconds}`:eventStatus(e)}</b><em>${open?'KST LIVE':'SOOPKETMON'}</em></div></div><div class="cp-pool-readout"><small>LIVE COIN POOL</small><strong>${fmt(e.total_pool)}</strong><span>COIN · ${fmt(e.participant_count)} PLAYERS</span></div></div></div><div class="cp-options">${optionCards}</div>${mine?`<div class="cp-my-bet"><span>MY PREDICTION</span><b>${esc((e.options||[]).find(o=>Number(o.id)===selected)?.label||'')} · 누적 ${fmt(mine.amount)}코인</b><em>${mine.status==='SETTLED'?`수령 ${fmt(mine.payout)}코인`:mine.status==='REFUNDED'?`환불 ${fmt(mine.payout)}코인`:canAdd?`추가 가능 ${fmt(remaining)}코인`:ownerUnlimited?'보유 코인 부족':'최대 참여 완료'}</em></div>`:''}${open&&canAdd?`<div class="cp-entry"><div><button data-cp-quick="100000">10만</button><button data-cp-quick="500000">50만</button><button data-cp-quick="1000000">100만</button><button data-cp-quick="${remaining}">${ownerUnlimited?'보유 전액':'잔여 전액'}</button></div><input type="number" min="100000" max="${remaining}" step="100000" placeholder="${ownerUnlimited?'OWNER 자유 베팅':mine?'추가 참여':'참여'} 코인 입력"><button class="cp-submit" data-cp-submit="${e.id}" disabled>${mine?'추가 참여':'예측 확정'}</button></div>`:''}</article>`;
  }
  function formatTime(ms){if(ms<=0)return'마감';const s=Math.ceil(ms/1000),h=Math.floor(s/3600),m=Math.floor(s%3600/60),x=s%60;return`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(x).padStart(2,'0')}`}
  function updateClocks(){document.querySelectorAll('.cp-clock[data-cp-end]').forEach(clock=>{const end=Date.parse(clock.dataset.cpEnd||''),start=Date.parse(clock.dataset.cpStart||''),time=clock.querySelector('[data-cp-time]'),ring=clock.querySelector('.cp-clock-ring');if(!time||!Number.isFinite(end)||clock.dataset.cpOpen!=='1')return;const remain=Math.max(0,end-Date.now()),total=Number.isFinite(start)&&end>start?end-start:Math.max(remain,1),ratio=Math.max(0,Math.min(1,remain/total));time.textContent=formatTime(remain);ring?.style.setProperty('--cp-ring-offset',(301.593*(1-ratio)).toFixed(2));clock.classList.toggle('is-warning',remain>300000&&remain<=3600000);clock.classList.toggle('is-critical',remain<=300000);clock.classList.remove('is-tick');requestAnimationFrame(()=>clock.classList.add('is-tick'))})}
  function championMarkup(king){return king?`<span class="cp-champion-label"><i>♛</i> KST DAILY HIT KING</span><div class="cp-champion-main"><small>오늘의 적중왕</small><b title="${esc(king.nickname)}">${esc(king.nickname)}</b></div><strong>+${fmt(king.netProfit)} <small>COIN</small></strong><em>오늘 순수익 · 적중 ${fmt(king.hitCount)}건</em>`:`<span class="cp-champion-label"><i>♛</i> KST DAILY HIT KING</span><div class="cp-champion-main"><small>오늘의 적중왕</small><b>아직 없음</b></div><strong class="is-empty">정산 대기</strong><em>KST 자정 이후 정산 기준</em>`}
  function view(){return `<section class="cp-shell"><div class="cp-hero"><div><small>SOOPKETMON · COIN PREDICTION</small><h1>코인 승부예측</h1><p>게임 속 승부를 예측하고 적중자들과 코인 풀을 나눕니다.</p></div><section class="cp-daily-champion" id="cpDailyChampion" aria-live="polite">${championMarkup(null)}</section><aside id="cpWalletPanel"><span>MY COIN</span><b id="cpWallet">—</b><small id="cpBetLimit">수수료 10% · 최소 10만 · 최대 3,000만</small></aside></div><div id="cpEvents" class="cp-events"><div class="cp-loading">승부예측 현황을 불러오는 중...</div></div><div class="cp-rule-note"><span>숲켓몬 코인은 현금·환전 가치가 없는 게임 내 가상 재화입니다.</span><button data-cp-terms>이용 규정</button></div></section>`}
  async function load(){try{state=await api('coin-prediction/state',{}, {replaceInflight:true});render()}catch(e){const root=document.getElementById('cpEvents');if(root)root.innerHTML=`<div class="cp-empty">${esc(e.message)}</div>`}}
  function render(){const root=document.getElementById('cpEvents');if(!root||!state)return;const opened=new Set([...root.querySelectorAll('[data-cp-bettors][open]')].map(x=>x.dataset.cpBettors));const wallet=document.getElementById('cpWallet'),walletPanel=document.getElementById('cpWalletPanel'),betLimit=document.getElementById('cpBetLimit'),champion=document.getElementById('cpDailyChampion'),todayChampion=state.settings?.todayChampion||null,ownerUnlimited=state.settings?.ownerUnlimited===true,events=orderedEvents(state.events);if(wallet)wallet.textContent=`${fmt(state.walletCoin)} COIN`;walletPanel?.classList.toggle('owner-unlimited',ownerUnlimited);if(betLimit)betLimit.textContent=ownerUnlimited?'이벤트 운영 모드 · 베팅 상한 없음':'수수료 10% · 최소 10만 · 최대 3,000만';if(champion){champion.classList.toggle('has-winner',Boolean(todayChampion));champion.innerHTML=championMarkup(todayChampion)}root.innerHTML=state.settings?.enabled===false?'<div class="cp-empty"><b>OPERATION PAUSED</b><span>현재 승부예측 참여가 일시 중지되었습니다.</span></div>':events.length?events.map(card).join(''):'<div class="cp-empty"><b>NEXT PREDICTION</b><span>현재 진행 중인 승부예측이 없습니다.</span></div>';root.querySelectorAll('[data-cp-bettors]').forEach(x=>{if(opened.has(x.dataset.cpBettors))x.open=true});bindCards()}
  function bindCards(){
    document.querySelectorAll('.cp-event').forEach(el=>{
      let option=Number(el.querySelector('.cp-option.selected')?.dataset.cpOption||0);
      const input=el.querySelector('input'),submit=el.querySelector('.cp-submit'),current=Number(el.dataset.cpCurrent||0),max=Number(el.dataset.cpMaxTotal||USER_MAX_BET_PER_EVENT),unlimited=el.dataset.cpUnlimited==='1';
      el.querySelectorAll('.cp-option:not(:disabled)').forEach(button=>button.onclick=()=>{
        option=Number(button.dataset.cpOption);
        el.querySelectorAll('.cp-option').forEach(x=>x.classList.toggle('selected',x===button));
        if(submit)submit.disabled=!Number(input?.value);
      });
      el.querySelectorAll('[data-cp-quick]').forEach(button=>button.onclick=()=>{
        input.value=Math.min(Number(button.dataset.cpQuick),max-current);
        if(option)submit.disabled=false;
      });
      input?.addEventListener('input',()=>{if(submit)submit.disabled=!option||!Number(input.value)});
      submit?.addEventListener('click',async event=>{
        if(busy)return;
        const button=event.currentTarget,eventId=Number(button.dataset.cpSubmit),amount=Number(input.value);
        if(!option)return alert('예측 항목을 선택하세요.');
        if(!Number.isSafeInteger(amount)||amount<MIN_BET||amount+current>max)return alert(unlimited?`최소 10만 코인이며 보유 코인 내에서 참여할 수 있습니다.\n현재 보유: ${fmt(state.walletCoin)}코인`:`최소 10만 코인이며 이벤트 누적 최대는 3,000만 코인입니다.\n현재 참여: ${fmt(current)}코인`);
        if(!termsAccepted&&!await showTerms(true))return;
        if(!confirm(`${fmt(amount)}코인을 ${current?'추가로 ':''}참여할까요?\n최초 선택 항목은 변경하거나 취소할 수 없습니다.`))return;
        busy=true;
        button.disabled=true;
        try{
          const result=await betRequest({eventId,optionId:option,amount,requestId:crypto.randomUUID()});
          if(result.state){state=result.state;render()}else await load();
        }catch(e){alert(e.message)}finally{
          busy=false;
          if(button.isConnected)button.disabled=false;
        }
      });
    });
  }
  function showTerms(confirmMode=false){return new Promise(resolve=>{const modal=document.getElementById('modal');if(!modal)return resolve(false);modal.className='modal show cp-terms-modal';modal.innerHTML=`<section><button class="cp-terms-x">×</button><small>SOOPKETMON POLICY</small><h2>${esc(state?.terms?.title||'코인 승부예측 이용 규정')}</h2><div>${(state?.terms?.items||[]).map((x,i)=>`<p><b>${String(i+1).padStart(2,'0')}</b><span>${esc(x)}</span></p>`).join('')}</div><label><input type="checkbox"> 위 내용을 확인했으며 숲켓몬 코인에 현금·환전 가치가 없음을 이해했습니다.</label><button class="cp-terms-ok" ${confirmMode?'disabled':''}>${confirmMode?'동의하고 계속':'확인'}</button></section>`;const close=v=>{modal.className='modal';modal.innerHTML='';resolve(v)};modal.querySelector('.cp-terms-x').onclick=()=>close(false);const check=modal.querySelector('input'),ok=modal.querySelector('.cp-terms-ok');check.onchange=()=>ok.disabled=confirmMode&&!check.checked;ok.onclick=()=>{if(check.checked)termsAccepted=true;close(!confirmMode||check.checked)}})}
  function bind(){document.querySelector('[data-cp-terms]')?.addEventListener('click',()=>showTerms(false));load();clearInterval(timer);clearInterval(pollTimer);updateClocks();timer=setInterval(updateClocks,1000);pollTimer=setInterval(()=>{if(!document.hidden&&!busy&&!document.querySelector('.cp-shell input:focus')&&!document.querySelector('.cp-terms-modal'))load()},Math.max(15,Number(state?.settings?.pollSeconds||15))*1000)}
  window.coinPredictionView=view;window.bindCoinPredictionView=bind;window.stopCoinPredictionView=()=>{clearInterval(timer);clearInterval(pollTimer)};
})();
