# 코덱스 지시문 — v1936 커밋 (계열 개편 S1)

아래를 그대로 코덱스에 붙여넣으면 된다.

---

작업 트리에 v1936 계열 개편이 반영돼 있다. 검증하고 커밋해라. **배포는 하지 마라.**

## 무엇을 바꿨나

**총량 제한이 없던 계열 능력을 전부 유한 자원으로 바꿨다.** 계열 간 격차 68%p → 18%p.

스탯을 계열마다 완전히 똑같이 맞춰놓고 재도 계열별 승률이 55/73/91/94로 갈렸다.
격차는 스탯이 아니라 능력의 성격에서 나왔고, 무한 자원인 능력(방어형 무료 부활, 생명형 무한 회복)은
수치를 아무리 깎아도 다른 계열이 따라올 수 없었다. 레버 30개 측정, 전부 3%p 미만.

| 계열 | 전 | 후 |
|---|---|---|
| 방어 | 무료 부활 + 무조건 반격 | 팀 방벽(소모) + 반격은 방벽 있을 때만 |
| 생명 | 무료 부활 + 무한 회복 | 부활 삭제 + 전투당 회복 총량 |
| 속도 | 회피(무의미) | 속도 상향 + 방벽에 추가 피해 |
| 공격 | 조건부 능력뿐 | 관통·마무리 상향 + 적 부활 1회 봉인 |
| 신규 | — | 계열 종류 수 보너스 (2종 +4% / 3종 +9% / 4종 +15%) |

## 변경 파일

    functions/_battle_v2_preview.js   (이 파일 하나만 바뀐다)

모든 상수는 파일 상단 `S1` 객체에 모여 있다. 재조정은 여기만 만지면 된다.

    PATCH-NOTES-v1936.txt   (신규)
    TEST-REPORT-v1936.txt   (신규)

## ⚠️ 반드시 확인할 것 — 피해 모델은 PVP 전용이다

`hitResult` 의 `usePvpDamageModel` 게이트를 **절대 풀지 마라.**

방어 감소 공식(상수 600 → 공격자 공격력 비례)과 피해 상한(46% → 60%)은 PVP 에만 적용된다.
PVE 에 넣으면 승률은 100% 로 같은데 **보스전 카드 생존이 3.0장 → 1.2장**으로 떨어진다(측정).
PVE 는 이미 승률 100% 라 이 공식을 살릴 실익이 없고 난이도 체감만 올라간다.

이 게이트 덕분에 PVE 회귀가 사실상 0 이다. 게이트를 풀면 PVE 난이도가 통째로 바뀐다.

## 커밋 전 확인

1. 문법·회귀

       node --check functions/_battle_v2_preview.js
       npm run test:escort
       npm run test:raid-entry
       npm run check:worker

2. `test:escort` 를 특히 눈여겨봐라. 호송작전은 `createPveBattleV2` 경로라
   이번 변경의 영향을 받는다. 실패하면 기대값이 이번 개편으로 바뀐 것인지
   진짜 회귀인지 구분해서 보고해라. **임의로 기대값을 고치지 마라.**

3. `git status` 로 위 3개 파일 외에 딸려 들어간 게 없는지 확인해라.
   `tools/balance-harness-v1903/`, `NOTICE-SEASON-REWORK-DRAFT.md`,
   `UNIQUE-TRAIT-V2-AND-AWAKENING-DESIGN.md` 는 이 커밋에 넣지 마라.

## 커밋

    git add functions/_battle_v2_preview.js PATCH-NOTES-v1936.txt TEST-REPORT-v1936.txt

    git commit -m "balance(v1936): 계열 개편 - 무한 자원 능력을 유한 자원으로

스탯을 계열마다 완전히 똑같이 맞춰도 계열별 승률이 55/73/91/94 로 갈렸다.
격차는 스탯이 아니라 능력의 성격에서 나온다. 방어형 무료 부활,
생명형 무한 회복처럼 총량 제한이 없는 능력은 수치를 깎아도 따라올 수 없었다
(레버 30개 측정, 전부 3%p 미만).

- 방어형: 무료 부활 삭제 -> 소모되는 팀 방벽. 반격은 방벽이 남아 있을 때만
- 생명형: 무료 부활 삭제 -> 전투당 회복 총량. 지속·긴급 회복량은 상향
          연장전 회복 완전 차단 -> 절반 유지(연장전 진입 = 자동 패배 해소)
          단일 힐러 오라를 '1장에서만' -> 장수 곡선으로
- 속도형: 속도 프로필 상향, 방벽에 추가 피해, 처치 시 게이지 회복
- 공격형: 관통·마무리 상향, 적 부활 1회 봉인, 처치 시 연쇄
          프로필은 공격력↑ 방어↓ (HP↓ 로 주면 오히려 약해진다)
- 신규: 계열 종류 수 보너스(2종 4% / 3종 9% / 4종 15%), 같은 계열 중첩 체감
- 피해 모델(방어 감소 분모, 피해 상한, 행동 빈도 기저값)은 PVP 전용

측정: 계열 간 격차 68%p -> 18%p (공 19->42 / 방 87->59 / 속 60->53 / 생 33->46)
      조합 리그 1위가 2계열 -> 3계열, 2·3위가 4계열. 상위10 평균 계열 2.9 -> 3.1
      PVP 평균 전투 길이 56 -> 34행동
      PVE 승률 전 구간 100% 유지, 생존 카드 수 최대 -0.5장

상수는 전부 파일 상단 S1 객체에 모아 두었다."

## 배포

이 커밋으로 배포하지 마라. 사용자의 배포 지시를 기다려라.
배포할 때는 `npm run deploy:production` 만 쓴다.

이번 개편은 **유저 공지가 필수**다. 방어형·생명형의 자동 부활이 사라지는 건
명백한 너프로 체감된다. 공지 초안은 `NOTICE-SEASON-REWORK-DRAFT.md` 에 있다.

## 이 커밋에 없는 것

- 전술 전직 4종 (설계만 있고 미구현)
- 고유특성 effect_type 8종 규격화
- 스킬 이펙트 6종 결선 (S2, 네 담당)
