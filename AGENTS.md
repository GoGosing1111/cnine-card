# 숲켓몬 프로젝트 고정 컨텍스트

## 핑두 리페어 쿠폰

- 2026-09-22 ON 준비 검증 후속 확정: 복구 비용은 이전 코인 0 대신 **최신 CMS의 쿠폰 1장 + 1,000억 코인**을 유지한다. 직전 강화 단계·기한 없음은 유지한다. 보호권은 **+9 도전 1장, +10 도전 3장에만 사용**하며 +1~+8 도전에서는 사용 불가다. 이번 지시는 검증 및 ON 준비이고 실제 활성화 승인은 아니다.

- 2026-09-22 사용자 확정: `PINGDU_REPAIR_COUPON`(핑두 리페어 쿠폰)은 장비 강화 파괴 기록 복구용이다. 복구 1회에 1장, 파괴 직전 강화 단계 유지, 추가 코인 0, 복구 기한 없음으로 설정한다. 이미 복구한 기록은 재복구하지 않으며 소모했던 강화 재료는 반환하지 않는다.
- 이번 등록은 강화·복구 ON 승인이 아니다. 현재 OFF 및 공동 출시 게이트를 유지하고 지급 대상·판매처·보상풀은 임의 추가하지 않는다. 운영자 후속 편집을 초기화하거나 등록 스크립트를 반복 적용하지 않는다. 기록: `docs/pingdu-repair-coupon-20260922.md`.

## 하이퍼팩 용병별 추첨 가중치

- 2026-09-22 후속 사용자 지시: 라그니엘을 오메가-X보다 희귀하게 만들며 같은 등급 균등 추첨을 해제한다. SSS 전체 확률 0.0001%는 유지하고 용병별 가중치는 오메가-X(V-021) 9, 라그니엘(V-046) 1이다. 이전 균등 추첨 고정 지시보다 이번 지시를 우선한다. 이후 운영자가 CMS에서 저장한 가중치는 재배포로 덮어쓰지 않는다.
- 동일 등급 내부 비율과 전체 개봉 확률을 구분한다. 보유 여부 미반영·중복 수량 집계·기존 영수증 재추첨 금지는 유지한다. 상세 기준은 `docs/mercenary-sss-weighting-20260922.md`다.

## 랭크전 챌린저 기준

- 2026-09-21 사용자 지시: 챌린저는 현 시즌부터 최종 1~20위다. 실시간 티어 표시, 시즌 보상, 정산 스냅샷, `챌린저★★★★` 기간제 호칭 지급, 명함의 연속 챌린저 기록을 모두 같은 20위 기준으로 적용한다. 과거 정산에서 챌린저가 아니었던 기록은 소급 변경하지 않는다.

## 운영 DB 연결·초기화 게이트 재발 방지

- 2026-09-20 계정 이전 복구: 운영 Hyperdrive SQL 쿼리 캐시는 반드시 OFF다. 쓰기 직후 잔액·행동력·영수증·참가 상태를 읽기 때문에 캐시를 켜지 않는다. 계정 이전/재생성 시 기본 ON을 확인하고 `deploy:production`의 원격 캐시 OFF 검사를 우회하지 않는다. Pages와 clan-draft의 Hyperdrive ID도 같아야 한다.
- `safe_runtime_upgrade_v2121_*`, `v2120_territory_*` 등 영속 foundation fast-gate 뒤에 신규 초기화 단계를 추가하면 반드시 마커 버전을 올리거나 기존 마커 이전에 독립된 버전 게이트를 둔다. 기존 마커가 있는 운영 DB에서 새 단계가 누락되지 않는 회귀 테스트를 추가한다.
- 2026-09-20 후속 확인: Neon 운영 계정 `cnine_migrator`는 유지한다. OWNER·테스트 계정도 점검 시간이 아니면 현재처럼 행동력을 소모한다. 공격덱이 보유 카드 5장으로 유효하지 않으면 방어덱으로 자동 대체하지 않고 공격덱 수정을 요구한다.

- 2026-09-19 사용자 지시: 이미지 업로드에 전체 게임 기능 검증을 실행하지 않는다. 원화·장비창 등 자산과 등록 메타데이터만 바꾸는 배포는 `ASSET_DEPLOY_BASE`에 직전 운영 커밋을 지정하고 `npm run deploy:production -- --assets-only`를 사용한다. 이 경로는 전체 테스트와 변경 없는 클랜 워커 재배포를 생략한다. 게임 실행 코드·전투·경제 로직 변경은 기존 전체 출시 게이트를 유지한다.

- 2026-09-17 사용자 지시: **PVE·PVP 공용 V3에서 카드 사진 컷신·궁극기 영상 사전 재생을 표시하지 않는다.** 발동 멘트는 전투를 멈추지 않는 작은 안내로 표시한다. 실제 타격·서버 피해/승패와 하단 원화 카드 프레임은 유지한다. 결과창은 공통 네이비·라임 UI와 모바일 내부 스크롤/복귀 버튼을 사용한다. `docs/v3-fluid-combat-20260917.md`를 따른다.

## 전투 버전·로딩 재발 방지

- 2026-09-15 사용자 지시: **전투 버전 불일치로 PVE·PVP 입장을 다시 막지 않는다.** 하이희야 배포 때 엔진 버전은 갱신했지만 메인 앱의 준비 검사에 이전 값을 남긴 오류를 재발 방지 기준으로 삼는다.
- 메인 앱에 전투 버전 비교 문자열을 별도로 복제하지 않는다. 공용 V3 래퍼의 `ready()`·`ensureRuntime()`을 사용하고, 실제 배포 번들의 버전과 일치하는지 검사한다. 원화·SD 파일만 바뀔 때는 공통 엔진 코드를 불필요하게 수정하지 않는다.
- `tests/pve-battlefield-entry-v2117.test.mjs`의 **실제 번들 + 메인 기능 로더 연결 검사**와 구버전 엔진 갱신 검사를 `test:live-connections`·`release:gate`에 유지한다. 예상 버전을 테스트에 함께 하드코딩하거나 준비 판정을 강제로 참으로 만들어 검사를 통과시키지 않는다.
- 전투 코드·번들·버전·로딩 경로 변경은 실제 게임 `index.html`에서 **토벌 시작 버튼과 PVP 매칭 버튼 → 전투 → 결과 → 복귀**를 PC·모바일로 검수한다. 첫 로딩, 로딩 완료 후 재입장, PVE에서 PVP 전환 및 구버전 갱신을 확인한다. 독립 프리뷰나 전투 브리지를 직접 호출한 검사만으로 운영 입장이 검증됐다고 보고하지 않는다.
- 오류를 주입했을 때 안내와 복귀 버튼이 실제로 보여야 한다. 배경만 남거나 숨겨진 결과 영역에서 멈추는 상태를 통과 처리하지 않는다. 검수용 계정·API로 운영 계정 재화 사용을 피하고, 배포 후에는 실제 운영 파일로 같은 입장 경로를 재확인한 뒤 완료를 보고한다.
- 원인과 복구 기준은 `docs/battle-entry-runtime-2121-20260915.md`에 기록한다. 기존 깨끗한 범위 커밋·전체 출시 검사·지정 배포 명령 규칙을 그대로 적용한다.

- 2026-09-14 FUR 고급 강화 추가 승인: +13→+14는 중복 5장·마스터의 별 20,000개·성공 15%·연속 실패 천장 10회, +14→+15는 중복 8장·별 30,000개·성공 10%·천장 20회다. 기존 천장처럼 지정 횟수 연속 실패 후 다음 시도 확정 성공이며 대상 1장은 남긴다. 후속 `13보다 더 좋게` 정정에 따라 +14는 +13 전투력의 1.5배, +15는 +14의 2배로 연결한다. 일반 FUR 전투력은 124,800/249,600(기본 대비 +3,800%/+7,700%), 고유효과는 +200%/+500%, 단계별 퇴사 환급은 15,000/30,000개로 저장한다. 최종 `일단 14 강화는 off로 해놔` 지시를 우선해 `extendedEnabled:false`로 유지하며 현재 최대 +13까지만 강화한다. 별도 ON 지시 없이 신규 두 단계를 활성화하지 않는다. 기존 +11~+13 운영값은 보존한다. 프레임은 실제 alpha 검수 전 기존 +13 승인본을 유지한다. 기준은 `docs/fur-enhancement-v2114.md`다.

- 2026-09-14 후속 공통 메뉴 지시: 메시지함·카드상점 등 모든 게임 화면에서도 새 로비의 왼쪽 메뉴를 유지한다. 분류는 해당 항목만 열고 하단 전체 메뉴는 보존한다. 별도 장비 강화·용병도감·용병 지휘소·소원램프에도 공통 메뉴를 연결한다. BGM은 족장 이름 아래로 내려 메시지함·튜토리얼과 겹치지 않게 한다. 모바일 본문·팝업·하단 메뉴 잘림과 짧은 PC 창의 전체 메뉴 접근을 함께 검수한다. 기준은 `docs/shared-navigation-v2108-20260914.md`다.

- 2026-09-14 최종 로비 UI 지시: 왼쪽 메뉴와 행정부 분류를 유지하고, 메인은 이전 로비처럼 아바타 일러스트로 화면을 채운다. 메인에 바로가기·장비·용병·검색 목록을 중복 배치하지 않는다. 족장 이름을 작게 표시하고 콘텐츠 현황은 이전처럼 하단 가로 한 줄로 얇게 둔다. 왼쪽 분류는 해당 항목만 여는 별도 창으로 연결하고 검색도 그 분류 안에서만 작동한다. 하단 전체 메뉴 버튼은 그대로 유지하며 이 버튼에서만 전체 목록을 연다. 세부 항목은 각각 구별되는 아이콘을 사용한다. 튜토리얼을 원하면 “처음 오셨나요?”에서 맨처음 시안(첫 모험 길잡이·5단계 화면 안내)을 별도 창으로 연다. 용병 선택 화면의 신규 개봉 패널은 제거하고 편성·이전 요청 복구는 보존한다. 기준은 `docs/lobby-clarity-v2107-20260914.md`다.

- 2026-09-13 최신 하이퍼팩·도감 후속 지시: 결과에서 같은 수량으로 연속 개봉하는 버튼을 제공한다. 운영 확률·수량 저장은 사유 생략 시 변경 내역을 자동 기록하고 실패 원인을 버튼 옆에 표시한다. 도감은 장비 강화 화면의 디자인을 참고한 운영 UI로 구성하며, CMS에 저장한 등급·포지션·스킬 배정·효과·수치를 직접 조회한다. 과거 도감의 등급·스킬 미정 안내를 현재 운영 기준으로 쓰지 않는다. 기준은 `docs/hyper-cms-codex-2098.md`다.

## UI 디자인 품질 고정 기준

- 2026-09-15 사용자 고정 지시: **신규·개편 UI의 기준점은 현재 운영 용병도감·로비·장비강화다.** 세 화면의 실제 공통 메뉴, 네이비 패널·라임 강조색, 한글 타이포그래피, 정보 위계, 버튼과 선택/상세 구조를 먼저 확인하고 계승한다. 임의의 독립 홍보 페이지나 일반 정보 카드 나열을 게임 UI로 대신하지 않는다. 고급 전리품 상점도 같은 기준으로 재구성하고 PC·모바일 실제 화면과 핵심 조작을 검수한다. 상세 기준은 `docs/ui-fixed-reference-20260915.md`다.

- 2026-09-12 사용자 지시: 앞으로 이 프로젝트의 UI를 대충 만들지 않는다. 사진만 넣고 평범한 정보 박스를 나열하는 구성으로 완성됐다고 보고하지 않는다.
- 콘텐츠의 분위기와 사용 목적에 맞춰 화면 구도, 한글 타이포그래피, 정보 위계, 재질, 색, 버튼과 상호작용을 함께 설계한다. 필요한 경우 라이선스를 확인한 새 폰트와 전용 자산을 사용한다.
- 실제 화면에서 데스크톱·모바일의 가독성, 여백, 잘림, 스크롤과 핵심 조작을 직접 검수한다. 기능 테스트 통과와 시각 완성도는 별도로 확인한다.
- 포로수용소는 삭막하고 공포스러운 수용 시설의 분위기다. 승인된 배경 사진은 유지하며, 귀여운 죄수 캐릭터나 일반 대시보드 같은 UI로 대체하지 않는다.
- 2026-09-13 사용자 지시: 모든 V3 전투의 그리드 크기·행/열 간격·진영 간격을 공통 기준으로 맞춘다. `FormationLayout.mjs`와 `ViewportLayout.mjs`의 정수 그리드를 사용하고 콘텐츠별 수동 좌표나 슬롯별 배율을 다시 추가하지 않는다. 용병·슈트·단일 보스·호송도 같은 기준을 따르며 PC·모바일과 이동/복귀를 검수한다. 상세 기준은 `docs/project-v-common-grid-v1.md`다.

- 2026-09-13 후속 운영 UI 지시: 카우방·폐차장에 독립 프리뷰/원정 페이지를 운영 화면으로 연결하지 않는다. 카우방은 기존 게임 PVE 메뉴·계정 재화·공용 V3 전투·결과 흐름 안에 구현하며, 폐차장은 원래 운영 화면과 입장·결과 UI를 사용한다. 실제 PC·모바일 운영 화면을 검수한다.
- 같은 날 최종 정정 `UI는 유지하고 전투화면을 개편안으로 하라고`: 무한의탑·폐차장 입장 화면의 기존 UI를 보존하고, 도전/출발 후 전투만 공용 V3 연속 전투로 교체한다. 탑은 현재 CMS 층·수호자·보상·한 층씩 진행·최고층 완료 정책을 유지하며 재등반을 추가하지 않는다. 폐차장은 기존 3구역·출입증·일일 제한·CMS 보상을 유지한다. 기준은 `docs/pve-combat-only-2094-20260913.md`다.
- 같은 날 포탈 범위 재지시: 일반 PVE와 아포칼립스 토벌·소탕 완료 결과에서만 포탈을 판정·안내한다. PVP·영토전·랭크전과 로그인/화면 이동에서는 보관 포탈 자동 팝업을 띄우지 않는다. 아포칼립스의 기존 소탕 금지는 유지한다.

- 2026-09-13 하이퍼팩 지시: CMS ON 한 번으로 실제 개봉·코인 차감·용병/재료 지급이 시작되도록 준비하고 현재 운영은 OFF로 둔다. 개봉 스위치는 `hyper_pack_opening_v2093`이며 기존 용병 확률·등급·수량을 그대로 사용한다. 용병 전투/편성 활성화와 구분한다.
- 2026-09-13 최신 후속 `용병 PVE,PVP 덱 1장 편성 가능하게 다시한번 체크하고 하이퍼팩 개봉 ON으로 변경해`는 용병 편성·전투 및 하이퍼팩 개방 승인이다. 일반 덱 5장과 PVE·PVP 공통 용병 슬롯 1개를 분리하고, 기존 소유권·등급·배정·개봉 확률을 보존한다. `스킬별 수치 초안을 만들어 함께 적용` 답변에 따라 26종 수치를 검증 후 CMS에 명시적으로 적용한다. 이번 범위의 과거 OFF 보류보다 최신 지시를 우선하며 용병 업그레이드는 추후로 유지한다. 기준은 `docs/mercenary-deployment-hyper-2097.md`다.

## 작업 완료 기본 흐름

- 2026-09-13 후속 `재등반은 나중에 하고 카우방,무한의탑 연결해`, `무한의탑은 기존 정책대로 유지`, `아니 ON 시키라고`, 이어 `폐차장도 연결해라`는 카우방·기존 무한의탑·폐차장의 유저 ON 지시다. 이 세 콘텐츠는 이전 공동 출시 대기보다 최신 지시를 우선한다. 탑은 기존 CMS 층·최초 보상·진행·V3 연출을 유지하고 재등반·50% 감액·160층·반복 보상 초안을 적용하지 않는다. 용병·강화의 개방 상태는 이 지시로 변경하지 않는다. 기준은 `docs/cow-tower-live-on-2092-20260913.md`다.

- 2026-09-13 카우방 입장 지시: PVE 토벌·소탕의 완료 전투마다 일반 PVE 2%, 아포칼립스 3%로 서버가 포탈을 판정한다. “미지의 젖소방 포탈이 열렸습니다. 입장하시겠습니까?” 팝업으로 안내하고 포탈 1개당 1회 입장한다. 새로고침·재시도로 재추첨하거나 중복 사용하지 않는다. 아포칼립스 소탕 금지와 공동 출시 OFF를 유지한다. 기준은 `docs/cow-room-portal-entry-20260913.md`다.
- 같은 날 후속 카우방 보상 확정: 클리어 5억 코인·일일 보상 상한 30억 코인·일일 입장 6회다. CMS 클리어 보상과 일일 코인 상한은 각각 최대 100억까지 입력할 수 있다. 100억을 현재 지급값으로 혼동하지 않는다. 공동 출시 OFF와 기존 포탈 규칙은 유지하며 기준은 `docs/cow-room-economy-approved-20260913.md`다.

- 사용자가 해당 작업에서 명시적으로 커밋 또는 배포를 금지하지 않는 한, 구현을 완료한 뒤 관련 검증을 통과시키고 작업 범위 파일만 커밋한 다음 운영 배포까지 마무리한다.
- 운영 배포 전에는 항상 `npm run release:gate`를 통과시키고, 배포는 `npm run deploy:production`만 사용한다. `npx wrangler pages deploy .` 직접 호출은 금지한다.
- 콘텐츠별 출시 게이트, 기능 OFF 유지, 사용자 최종 승인 대기처럼 더 구체적인 고정 규칙이 있으면 해당 규칙을 우선한다.

## 장비 강화·용병 전투력 승인 기준

- 2026-09-13 후속 `개편된 V3 개편안 전체 다 라이브로 반영해라 ,CMS포함 해서`는 전체 코드·CMS 운영 배포 지시다. 미정 수량·확률을 검수용 값으로 자동 승인하지 않는다. 최신 범위는 `docs/v3-live-cms-2089-20260913.md`를 따른다.
- 같은 날 후속 지시: 용병은 확정된 등급별 고정 전투력을 쓴다. HP·공격력·방어력·속도는 공통 전투의 기본 환산식을 사용하고 개별 스탯·성장 초안은 적용하지 않는다. 업그레이드는 **차후 동일 용병 중복 카드 + 마스터의 별**이다. 현재 EXP 훈련·코인 레벨업·레벨당 전투력 성장은 사용하지 않고 중복 집계를 보존한다.
- 장비 강화 +1~+10의 비용 재료는 **코인 + 마스터의 별(`MASTER_STAR`)**로 고정한다. 단계별 두 수량을 CMS에서 설정하고 한 거래로 차감한다. 비어 있는 수량·확률과 보호·복구 정책은 미정 상태를 유지한다.

- 2026-09-12 사용자 `그래 그렇게 잡아` 승인: 장비 +1~+8은 기본 전투력의 단계당 +6%, +9는 누적 +80%, +10은 누적 +120%다. 일반 무기·상의·하의·신발·장신구에 적용하고 PVE 90%·PVP 10%, 배틀슈트 별도 처리를 유지한다.
- 용병의 성장·장비·스킬 적용 전 기본 전투력은 C 10,000 / B 20,000 / A 40,000 / S 70,000 / SS 120,000 / SSS 180,000이다. 비교 기준은 2026-09-12 슈퍼스타 +13의 자체 전투력 93,200이며 SS는 약 28.8% 높다.
- 단일 수치 기준은 `shared/equipment-mercenary-power-v1.mjs`, 승인 범위는 `docs/equipment-mercenary-power-approved-20260912.md`다. 전투력 기준 승인과 기능 활성화는 구분한다. 미정 성장량·역할별 능력치 환산·확률·비용은 자동 확정하지 않으며 공동 출시 전 실행 OFF를 유지한다. 저장된 용병 등급·스킬 배정은 덮어쓰지 않는다.

## PROJECT V와 용병 카드

- 2026-09-12 공동 출시 고정: V3 개편·용병·장비 강화/보호/복구는 같은 운영 회차에 공개한다. 무한의탑도 V3 공동 준비 범위로 관리한다. 기준은 `docs/v3-mercenary-equipment-joint-release-20260912.md`다.
- 같은 날 후속 `용병관련 전체 항목 CMS 등록 라이브로 배포해` 지시로 **용병 CMS만 먼저 운영 배포**한다. 원화·SD 43종, 독립 스킬 17종, 등급·포지션·직접 배정·획득·성장 초안을 DB에 등록한다. 이는 획득·편성·전투 활성화 승인이 아니며 모두 OFF를 유지한다. 기준은 `docs/project-v-mercenary-cms-live-20260912.md`다.
- 같은 날 후속 `장비강화 라이브 배포,유저공개만 해놓고 강화는 ON되야 가능하게`, `방어구도 되야한다` 지시로 승인 V2 기반 강화 센터를 **유저 선공개, 강화·복구 OFF** 상태로 배포한다. 무기·상의·하의·신발·장신구의 실제 보유 인스턴스 조회와 OWNER CMS를 연결한다. 운영 확률·비용·원자 처리·전투력 연결이 준비되지 않았으므로 ON 저장과 실행 API를 서버에서도 차단한다. 공동 게임플레이 활성화 조건은 유지하며 기준은 `docs/equipment-forge-public-v2084.md`다.
- 용병 CMS 저장은 OWNER 전용이며, 등록/저장 기록과 버전 충돌 검사를 유지한다. 재배포·초기화로 운영자가 저장한 초안을 덮어쓰지 않는다. 오메가-X SSS만 고정하고 나머지 등급은 사용자 입력 전까지 미정, 스킬은 자동 배정하지 않는다.
- 2026-09-13 사용자 확정: 용병카드 개봉은 저장된 등급별 확률을 사용하고, 같은 등급의 모든 카드를 보유·중복 여부와 무관하게 균등 추첨한다. 미보유 우대·보유 카드 제외·중복 재추첨·개별 카드 가중치를 적용하지 않는다. 첫 획득은 중복 0장, 이후 같은 카드 당첨마다 중복 +1로 집계하며 동일 요청 재시도는 한 번만 반영한다. 개봉 OFF·공동 출시 조건은 유지한다. 기준은 `docs/project-v-mercenary-draw-cms-20260912.md`다.

- PROJECT V는 별도 게임이 아니라 숲켓몬 내부의 용병·전투 캐릭터 및 전투 연출 체계다.
- 숲켓몬 기존 멤버 카드는 방송 멤버 사진 기반 카드다. PROJECT V 용병 카드는 독자 캐릭터 일러스트 기반 신규 카드군이다. 둘을 혼동하거나 서로의 이미지를 작화 분석 표본으로 사용하지 않는다.
- 사용자가 `용병 카드`, `용병 원화`, `PROJECT V 카드 작화`를 말하면 기존 `assets/cards`, `assets/NEWCARD`, `assets/pre`, `assets/cards/ZENITH`의 멤버 사진을 분석 대상으로 삼지 않는다.
- 용병 카드 관련 작업 전에는 반드시 `docs/project-v-mercenary-card-art-standard.md`를 읽고 따른다.

## 공식 작화 기준

- 작화 명칭은 `하이엔드 캐릭터 중심 세미리얼 판타지 스플래시 아트`다.
- 2026-09-07 사용자 지시에 따라 신규 용병의 최우선 작화 기준은 베스페라(V-004), `assets/ui/project-v/mercenaries/female-office-sniper-red-v1.png`다. SHA-256은 `629564D768A4BCEFCD0BE746E744DA49A64F1CF2BE7D048E7485FC6CB14FF874`다.
- 베스페라에서 계승하는 것은 얼굴의 가독성·표현력, 회화적 채색, 의상·무기 재질의 완성도다. 베스페라의 얼굴 골격, 검은 단발, 안경, 붉은 정장, 세워 든 소총과 포즈를 복제하지 않는다.
- 모든 인간형 신규 용병은 누구나 뽑고 싶을 만한 미녀·미남 성인 캐릭터를 목표로 한다. 개성 때문에 얼굴을 거칠게 만들거나 과한 주근깨·주름·흉터·투박한 골격을 추가하지 않는다. 다양성은 미형을 유지한 얼굴 인상·헤어·표정·의상·무기·구도로 만든다.
- 2026-09-07 후속 승인된 미형·채색 톤은 `preview/mercenary-vespera-beauty-v1/assets/female-beauty-v1.png`와 `male-beauty-v1.png`다. 사용자의 `좋다`, `이런 느낌으로` 승인은 작화 방향에 대한 것이며, 기존 도감 교체나 신규 카드 등급·전투 연결 승인이 아니다.
- 여성 의상은 제복·전술복·갑옷만 반복하지 않고 묶음 중 간간이 1~2종은 노출감 있는 드레스·전투복도 포함한다. 해당 캐릭터도 성인 미형과 역할·무기·의상의 일관성을 유지하고 다른 여성 용병과 같은 재단을 반복하지 않는다.
- 같은 날 후속 `여자도 총든거 많이`, `총스킨은 판타지 느낌`, `노출 많이` 지시에 따른 여성 총기 확장 시안은 노출형 비중과 강도를 높인다. 기본 다양성 묶음의 1~2종 규칙을 이 추가 묶음의 고정 상한으로 적용하지 않는다. 총 스킨은 보석만 붙이지 않고 총 전체에 통일된 판타지 외장을 설계하며 자연스러운 손·손목·손잡이 접점을 반드시 검수한다.
- 작화는 베스페라처럼 세련된 2D 게임 일러스트 톤을 유지한다. 실사 배우·서양 실사 RPG·3D 렌더처럼 보이는 피부 질감, 과도한 금속 마모·잔장식·대형 장갑과 기계 부품을 피한다. 장비의 완성도는 장식량이 아니라 정돈된 실루엣과 일관된 재질·형태로 확보한다.
- 기존 자수정 K2 앵커 `female-style-anchor-amethyst-k2-officer-v1.png`와 원본 `short-bob-k2-amethyst-officer-mercenary-source-art-v1.png`는 보조 기준으로 보존한다. SHA-256 `4B2F7D8F35E85E88B61AAAFFC3127817F61BFCA667075DEC64AEF4D1EC9B02CF`는 변경하지 않는다. 남성·중갑 보조 기준은 `male-style-anchor-silver-paladin-v1.png`다.
- 과거 V-001~V-012 시안 전체를 공식 작화 기준으로 취급하지 않는다.
- 공식 앵커 파일은 수정, 덮어쓰기, 재압축하지 않는다.
- 모든 신규 용병을 작은 얼굴의 8등신 전신·정면 3/4 자세로 고정하지 않는다. 카드마다 얼굴형·눈·코·입의 구조, 헤어 실루엣, 의상 재단, 무기 계열, 자세, 카메라 거리·각도를 별도로 설계한다. 머리색·옷색만 바꾸는 변주는 불합격이다.
- 현실 화기 기반 무기는 원형 실루엣만 남긴 단순 전술 튜닝으로 끝내지 않는다. 총 전체에 연결되는 장갑 외피, 결정 코어, 장식 문법과 광원 체계를 적용해 완성형 게임 스킨으로 만든다.
- 베스페라의 루비 팔레트·천문 도시와 과거 앵커의 자수정 팔레트·관측소 배경을 복제하지 않는다. 신규 용병마다 기존 로스터와 겹치지 않는 주색, 시간대와 배경 장소를 설계한다.
- 망토, 리본, 허리 천과 코트 자락처럼 펄럭이는 장식은 캐릭터당 주 흐름 1~2개로 절제한다. 얼굴·손·무기·다리 실루엣을 가리거나 화면 전역을 여러 갈래로 뒤덮지 않는다.
- 연속 여성 용병을 모두 노출형 흉부 장갑, 코르셋, 하이컷 하의와 허벅지 장비의 같은 골격으로 만들지 않는다. 현대복·전술복·정장 재단·마도복·경갑·중갑의 비중과 노출 위치를 바꾸고, 역할에 맞는 구조적 갑옷 덩어리를 배치한다.
- 카드 원화 `sourceArt`와 전투 SD `battleSprite`를 구분한다. SD는 원화의 파생물이며 도감·덱·상점·상세·스킬 컷인에서 원화를 대체하지 않는다.
- 신규 원화 최소 규격은 1024×1536 RGB PNG, 정확한 2:3이다. 원화 내부에 이름, 랭크, 시리얼, 로고, 워터마크, 카드 프레임과 UI를 넣지 않는다.

## 리소스 탐색 주의

- 2026-08-23 기준 용병 원화·SD·프리뷰는 `C:\Users\User\.codex\worktrees\1023\cnine-card`의 `codex/limited-zenith-evolution` 작업 트리에서 발견되었고 Git 미추적 상태였다.
- 현재 작업 트리에서 용병 리소스가 보이지 않는다는 이유만으로 없다고 결론 내리지 않는다. `assets/ui/project-v/mercenaries`, `assets/ui/project-v/characters/mercenary`, `preview/mercenary-cards-*`와 활성 작업 트리를 확인한다.
- 해당 리소스가 `main`으로 안전하게 통합되기 전에는 원본 작업 트리를 정리하거나 삭제하지 않는다.
- 2026-08-24 이전에 생성한 미드나이트·에메랄드 K2 제복 용병 시안은 `output/rejected/project-v-mercenary-drafts/`의 폐기본이며 작화 기준이나 생성 입력으로 사용하지 않는다.

## 승인 용병 카드 로스터

- 2026-08-24 승인 카드 V-013~V-020의 단일 기준 데이터는 `assets/ui/project-v/mercenaries/mercenary-card-roster-approved-v1.json`이다.
- 통합 카드 프리뷰는 `preview/mercenary-cards-approved-v1/index.html`이며 `assets/ui/card-frames/mercenary-contract-frame-premium-v2.png`를 별도 계층으로 사용한다.
- V-013~V-020은 카드 원화가 승인됐고, 2026-08-31 별도 투명 전투 SD가 `TECH_QA_COMPLETE_USER_REVIEW_PENDING` 상태로 준비됐다. 원화를 전투 스프라이트로 임시 사용하지 않는다.

## 용병 편성 고정 규칙

- 2026-09-13 용병 공격 누락 재현 기준: 전투력 1천~1만 일반 카드만으로 용병 연결 검사를 끝내지 않는다. 카드당 100만·2천만·1억, PVE·PVP 양쪽, 스킬 미배정 기본 공격과 연속 몬스터 교체를 확인한다. 등급별 고정 능력치는 유지하고 실제 서버 공격 이벤트와 V3 재생을 함께 검증한다. 수정과 행동 기회 규칙은 `docs/mercenary-v3-attack-fix-2099.md`를 따른다.
- 2026-09-13 후속 성능 기준: 용병 편성 전후를 같은 덱·상대·난수와 공격/방어 양쪽에서 비교한다. 용병의 추가 행동은 일반 카드의 행동 한도·순서·보호막 몫을 소모하지 않는다. 최신 수정은 `docs/mercenary-performance-effects-v2100.md`다. 사용자가 말한 특성별 타격은 **수호·돌격·기습·사격·저격·제압·지원** 7개 용병 역할이며, 일반 카드의 4계열과 혼동하지 않는다.

- 용병 카드는 기존 일반 덱 5장에 포함하지 않는다. 일반 덱은 계속 정확히 5장으로 저장·검증한다.
- 용병은 별도 전용 슬롯에 최대 1장 배치한다. 용병을 배치한 실제 전투 편성은 `일반 카드 5장 + 용병 1장 = 최대 6장`이다.
- 저장/API 계약에서도 일반 카드 ID 배열에 용병을 여섯 번째 원소로 끼워 넣지 않는다. `cardIds` 5장과 `mercenaryCode` 1개를 분리한다.
- 용병 슬롯은 선택 사항이며 비어 있으면 기존 5장 편성으로 동작한다. 일반 카드는 용병 슬롯에, 용병 카드는 일반 덱 슬롯에 배치할 수 없다.
- 용병 등급은 사용자가 통합 프리뷰를 검수한 뒤 새로 확정한다. 과거 매니페스트의 임시 등급과 V-013~V-020 초기 프리뷰의 등급을 신규 시스템에 자동 승계하지 않는다.
- 2026-09-11 사용자 지정 등급 체계는 **C → B → A → S → SS → SSS**다. 최상위 용병 **오메가-X(V-021)는 SSS**로 배정하고 별도 전용 스킬을 설계한다. 나머지 42종은 자동 배정하지 않으며, CMS 초안의 등급 선택은 사용자 최종 확정이나 운영 전투 활성화가 아니다. 기준 기록은 `docs/project-v-mercenary-omega-ranks-v1.md`다.
- 같은 날 후속 `스킬 목록을 따로 뽑아놓고 어떤 용병에 적용할지는 내가 정한다` 지시에 따라 **스킬 목록과 용병 배정은 독립**이다. 종언의 사건지평선 포함 모든 자동 제안 배정·전용 잠금을 해제한다. 원래 제작 참조·과거 code·등급으로 소유권을 재생성하지 않으며, 사용자가 별도 배정 화면에서 직접 체크한 연결만 초안에 저장한다. 시연용 용병 선택은 배정이 아니다. 최신 기준은 `docs/project-v-mercenary-skill-catalog-v1.md`다.
- 단일 준비 기준은 `assets/ui/project-v/mercenaries/mercenary-system-roster-v1.json`, 편성 계약 문서는 `docs/project-v-mercenary-system-standard.md`, 검수 화면은 `preview/project-v-mercenary-system-v1/`이다.
- 등급 확정 및 사용자의 연결 지시 전에는 준비 로스터와 프리뷰를 라이브 덱·전투 API에 연결하지 않는다.
- 2026-09-07 승인 신규 원화 16종은 V-022~V-037로 읽기 전용 라이브 용병도감에 추가한다. 전체 원화 37종·전투 SD 21종 준비·신규 SD 16종 제작 대기다. V-024는 검은 드레스의 판타지 SKS 수정본을 사용하며, 신규 이름·칭호는 가칭이다. 승인 기록은 `assets/ui/project-v/mercenaries/mercenary-art-approval-20260907.json`이다. 도감 공개는 등급·획득·편성·전투 활성화 승인이 아니다.
- 같은 날 V-022~V-037 전투 SD 16종을 제작한 뒤, 후속 `연결해` 승인으로 여성 의상·무기 원화 4종을 V-038~V-041로 도감에 추가했다. 현재 집계는 원화 41종·전투 SD 37종·신규 SD 제작 대기 4종이다. 조합은 오피스룩×건틀릿, 가터벨트 치마×체인소드, 비키니룩×활, 핫팬츠룩×대검이며 승인 기록은 `assets/ui/project-v/mercenaries/mercenary-four-looks-approval-20260907.json`이다. 신규 4종 역시 가칭·등급 미정·읽기 전용 도감으로 제한한다.
- 후속 `경찰 조은으로 이름 변경하고 라이브서버 연결해` 승인으로 원화 시안을 **경찰 조은(V-042)**이라는 사용자 지정 이름으로 도감에 추가한다. 승인 원화는 무가공 보존하고, 현재 집계는 원화 42종·SD 37종·SD 제작 대기 5종이다. 승인 기록은 `assets/ui/project-v/mercenaries/mercenary-police-joeun-approval-20260907.json`이며 등급·획득·편성·전투 활성화는 포함하지 않는다.
- 후속 `디임 용병사진도 라이브 배포하고` 승인으로 최종 게임 일러스트 V4를 **동탄 디임(V-043)**이라는 사용자 지정 이름으로 읽기 전용 도감에 추가한다. 원본 SHA-256은 `81FE7FB2BACEBF340FD53F730668BA079831DA7E5F34C396D4285DACECFA8B92`이며 무가공 보존한다. 현재 원화 43종·SD 37종·SD 제작 대기 6종이고 승인 기록은 `assets/ui/project-v/mercenaries/mercenary-dongtan-diim-approval-20260907.json`이다. 등급·획득·편성·전투 활성화는 포함하지 않는다.

## 대형 대화 기록 연속성

- 고정 작업 `몬스터공성 40회 초과 보상 적용`의 스레드 ID는 `019fdda2-6258-7902-a400-31dfdf72c278`이다.
- 2026-08-27에 9.777GiB 전체 원본을 `C:\Users\User\Documents\Codex-Session-Backups\2026-08-27\rollout-2026-08-08T04-10-34-019fdda2-6258-7902-a400-31dfdf72c278.full.jsonl`로 이동해 보존했다.
- Codex 안에는 당시 기준 최근 72시간의 작업 경계와 사용자·어시스턴트 대화 레코드만 남긴 42.52MiB 축소본이 있다.
- 사용자가 이전 대화나 이어서 하던 작업을 언급하면 없다고 답하거나 전체 백업을 한꺼번에 열지 않는다. 먼저 사용자 문구·기능명·날짜를 기준으로 전체 백업을 읽기 전용 스트리밍 검색하고, 관련 사용자·어시스턴트 메시지 주변만 추출해 현재 작업에 이어 붙인다.
- 전체 백업은 복구 기준이므로 사용자의 명시적 요청 없이 수정·이동·삭제·재압축하지 않는다.
- 백업 색인은 `C:\Users\User\Documents\Codex-Session-Backups\INDEX.md`에 있다.

## PROJECT V 배틀슈트 원본 총기 합성 고정 규칙

- 배틀슈트 리소스 신규 제작·무기 교체·비율 수정·V3 연결 전에는 반드시 `docs/project-v-account-battle-suit-standard.md`, `preview/battle-suit-prestige-v1/README.md`, `preview/battle-suit-prestige-v1/exact-weapon-fit.mjs`를 먼저 읽고 승인본·원본 해시·무기별 설정을 확인한다. 요청한 무기만 재작업하며, 다른 승인본까지 재생성하거나 공통 설정으로 덮어쓰지 않는다.
- 2026-09-08 사용자가 승인한 방식은 포스터가 아니라 실제 V3 투명 전신 PNG·사격 아틀라스를 만드는 방식이다. 길고 날씬한 사람이 입은 슈트 비율을 유지하며, 선택한 백색 판금·주황 코어·트윈 안테나 외형 한 종만 작업한다. 취소된 날개형 두 번째 슈트를 다시 만들지 않는다.
- 총기는 기존 투명 PNG 원본을 그대로 합성한다. 이미지 생성으로 총기 디자인을 다시 그리거나 총열만 늘리기, 비균일 축소·확대, 메시 변형, 공통 프록시 상자에 맞춘 일괄 축소를 하지 않는다. 총 전체의 균일 배율·방향 반전·조준각 보정만 허용한다.
- 무기를 기존 손 위치에 맞춰 줄이지 않는다. 몸 대비 무기 길이를 먼저 정하고 그 크기에 맞춰 어깨·팔·손을 보정한다. 개머리판은 어깨에, 주 손은 실제 손잡이·방아쇠에, 받치는 손은 탄창 앞 총열덮개 아래에 둔다. 총구는 전방 수평 조준으로 검수한다.
- 이미지 생성은 고정된 원본 무기 실루엣의 녹색 프록시 주변 몸·팔·손 보정에만 사용한다. 이후 사용자 승인된 기존 스크립트로 배경·프록시를 제거하고 원본 무기 PNG와 전경 손가락을 합성한다. 실제 알파, 원본 무기 해시·픽셀, 총구·발바닥 기준점과 V3 사격을 검증한다.
- 예외적으로 이미 승인된 M200 일체형 원본은 그대로 고정한다. 재생성·무기 재교체·총기만 축소하지 않는다. 승인된 SKS V6·금룡 대물저격총 V6·확대된 금룡 돌격소총 V7도 후속 변경 요청이 없는 한 보존한다.
- 기준 파일과 수치는 `docs/project-v-account-battle-suit-standard.md` 및 `preview/battle-suit-prestige-v1/`에 남긴다. 프리뷰 승인과 라이브 연결 승인은 구분하며, 이번 백색 외형은 후속 H-BODY·슈트 코어 4 등록 및 라이브 연결 지시를 받았다. 미정 전투력·제작비·확률은 임의로 확정하지 않는다.

## PROJECT V 스킬 이펙트 PixiJS·GSAP 고정 기준

- 2026-09-08 사용자 지시: 전투 스킬 이펙트는 **PixiJS로 표현하고 GSAP으로 타임라인·트윈을 제어**한다. 신규 제작·수정 전 반드시 `docs/project-v-skill-effects-standard.md`를 읽는다.
- 기존 V3의 PixiJS 렌더러·효과 레이어와 GSAP 재생 제어를 재사용한다. 별도 렌더러나 중복 라이브러리를 만들지 않으며, 아틀라스 프레임·충돌·배속·일시정지가 서로 다른 시계로 어긋나지 않게 한다. 서버가 확정한 피해·승패는 연출이 다시 계산하지 않는다.
- 작업마다 실제 PixiJS·GSAP 사용 파일, 잠금 버전, 자산·타임라인·충돌 기준점과 재생/취소 검수 결과를 해당 프리뷰 README 또는 매니페스트에 기록한다. 단순 라이브러리 설치를 실제 이펙트 구현으로 보고하지 않는다. 기록·시안 승인만으로 미승인 이펙트나 사운드를 라이브에 연결하지 않는다.
- 2026-09-11 사용자 지적: **스킬별로 고품질 개별 연속 스프라이트를 제작한다.** 정지 이미지 한 장의 이동·확대·회전으로 스킬 제작을 완료하지 않는다. 준비·방출·충돌·소멸의 형태가 프레임마다 실제로 달라져야 하며, 같은 효과의 색상 변경이나 한 장에 모은 아이콘을 개별 제작으로 세지 않는다.
- 기존 용병 스킬 V1의 16개 정지 이미지 트윈 연출은 사용자 반려다. 승인된 배틀슈트 로켓런처·헬기폭격을 실제 재생 비교 기준으로 삼고, 각 스킬의 연속 프레임·무기 궤적·충돌 기준점·파편/연기/지면광·잔향을 한 종씩 검수한다. 기능 테스트 통과는 시각 품질 승인과 별개이며, 상세 기준은 `docs/project-v-skill-effects-standard.md`에 기록한다.

## PROJECT V 전투 사운드 고정 기준

- 전투 이펙트 사운드에 사인파·오실레이터·절차적 노이즈로 만든 테스트 합성음, UI 알림음, 삑음, 단일 신스톤을 사용하지 않는다.
- 검격·금속 충돌·공기 파열·폭발·마법처럼 동작의 물성을 들려주는 실제 녹음·폴리·라이선스 원음을 사용한다.
- 주요 전투 이벤트는 최소한 이동·예고, 충돌, 잔향의 계층을 분리해 설계하고 가장 강한 충돌 피크를 시각 이펙트 충돌 프레임의 ±20ms 이내에 맞춘다.
- 원음 출처, 라이선스, 자산 ID, 해시와 가공 방식을 프리뷰 내부 매니페스트와 출처 문서에 남긴다.
- 승인 전 신규 이펙트와 사운드는 독립 프리뷰에서만 검증하며 사용자가 연결을 지시하기 전에는 게임 런타임에 연결하지 않는다.
- `scripts/generate-v3-event-sfx-preview-v1.py`의 과거 절차적 합성 방식은 폐기 기준이며 복원하거나 신규 사운드의 입력·참고로 사용하지 않는다.
- 현재 승인 대기 기준 프리뷰는 `preview/project-v-v3-event-fx-v1/`의 Combat SFX V4이며 치명타 V3는 과도한 금속 고역과 피크로 폐기됐다. 치명타는 저고역·저피크 V4, 사용자 검수를 통과한 2~6번은 V2 파일을 고정한다. 재생성 기준은 `scripts/generate-v3-event-sfx-preview-v2.py`이고 치명타만 다시 만들 때는 `--effect critical`을 사용한다.

## 신규 레이드의 PROJECT V V3 통합 규칙

- 2026-09-12 사용자 요청으로 코어 동조(중앙 정지)·회로 복원(드래그/터치 연결)·차폐 구역 이동 기믹 3종을 `preview/core-raid-mechanics-v1/`에 준비한다. 신규 기믹은 검수용이며 운영 타임라인 배정·난이도·서버 판정 연결은 별도 검수 대상이다. 같은 요청의 모바일 방향 입력 수정은 공용 QTE의 큰 터치 버튼·짧은 스와이프·포인터 추적 개선으로 반영한다. 계정 재화와 출시 플래그는 변경하지 않으며 기록은 `docs/core-raid-mechanics-mobile-v2085.md`다.

- 코어 프로토콜을 포함한 신규 레이드는 라이브 PROJECT V V3의 캐릭터 진형, 로스터, 카드 프레임, 전투 렌더러와 아트 어댑터를 그대로 사용한다. 이를 별도 구현으로 복제·교체·재배치하지 않는다.
- 신규 레이드가 추가할 수 있는 것은 입장·작전 화면, 서버 타임라인 기믹, QTE와 결과 오버레이뿐이다. 신규 CSS도 기존 V3 카드 프레임·로스터·Pixi 진형을 덮어쓰지 않는다.
- 카드 원화 `image`/`sourceArt`와 전투 SD `battleSprite`는 항상 분리한다. SD 경로로 카드 원화를 덮어쓰지 않는다.
- 전투 하단 카드 도크는 라이브 V3의 `rosterCardHtml`, 공용 `card.css`, `battle-v3-live.css`와 등급별 프레임 자산을 그대로 사용한다. 카드 이미지는 도감의 카드 원본만 허용하며 전투 SD를 표시하지 않는다. ZENITH·SUPERSTAR·Faker 전용 프레임 이미지 계층을 반드시 유지하고, 하단 카드 마크업·비율·프레임·배치의 신규 레이드별 변형은 금지한다.
- 독립 프리뷰도 라이브와 동일한 V3 아트 어댑터 체인을 로드하며, 가짜 덱·가짜 프레임 유도 ID·SD를 원화로 쓴 샘플을 사용하지 않는다.
- 상세 기준은 `docs/core-protocol-raid-v3-integration-standard.md`를 단일 기준 문서로 사용한다.

## 블랙미라클·신규 레이드 공동 출시 게이트

- 블랙미라클과 코어 프로토콜 신규 레이드는 하나의 대규모 업데이트 묶음으로 함께 공개한다. 둘 중 하나만 먼저 독립적으로 활성화하거나 정식 출시하지 않는다.
- 공통 선행 조건은 `고유특성 개편 완료 및 검수 통과`다. 이 조건과 사용자의 최종 배포 지시가 모두 충족되기 전에는 라이브 공개·활성화를 진행하지 않는다.
- 대기 기간에는 블랙미라클 사용 상태를 `OFF`로 유지하고, 신규 레이드는 프리뷰·검수 범위에만 둔다.
- 블랙미라클 선배포 상태에서는 `BLACK_MIRACLE_INVENTORY_USE_RELEASE_ENABLED=false`와 인벤토리 `usable=0`을 유지한다. 운영 DB에 과거 `enabled:true`가 남아 있어도 개봉이 열리면 안 된다.
- 향후 개봉을 ON으로 전환하기 전에 장비·차량 보상 `INSERT ... SELECT`가 0행인 경우 팩 차감과 `COMPLETED` 처리가 일어나지 않도록 지급 행수까지 원자적으로 검증하는 회귀 수정이 필수다.
- 선행 조건이 충족되면 두 콘텐츠의 회귀 테스트, 기능 플래그, 캐시 버전과 공지 시점을 한 번에 점검한 뒤 같은 배포 회차로 올린다.
- 선행 조건 전에는 운영 `index.html`, `js/app.js`, `functions/api/[[path]].js`, `js/pve-command-v2-live.js`에 코어 레이드 UI·스크립트·API 라우트를 연결하지 않는다. 코어 레이드 파일은 독립 프리뷰에서만 사용한다.
- 운영 레이드 진입은 `switchPveMode('raid')`에서 기존 `loadRaidView()`를 직접 호출해야 한다. 선택적 프리뷰 모듈의 존재 여부나 반환값이 월드 레이드 진입을 가로채게 하지 않는다.
- 운영 배포 전에는 반드시 `npm run release:gate`를 통과한다. `npx wrangler pages deploy .` 직접 호출은 금지하고 `npm run deploy:production`만 사용한다.
- dirty 작업 트리의 운영 배포는 원칙적으로 차단한다. 긴급 복구 때만 `ALLOW_DIRTY_PRODUCTION_DEPLOY=1`과 20자 이상의 `PRODUCTION_DEPLOY_REASON`을 함께 기록하며, 회귀 테스트와 Core 라이브 단절 검사는 이 경우에도 우회할 수 없다.

## 몬스터공성 고정 전황 규칙

- 몬스터공성은 단일 보스 레이드가 아니라 영토전과 같은 `지도 기반 진영전`이다. 화면과 서버 상태 모두 전황 지도, 점령 거점, 현재 전선, 진영 체력을 중심으로 구성한다.
- 전투에서 감소하는 값은 개별 몬스터 HP가 아니라 현재 전선의 `숲켓몬 연합 진영 체력`과 `몬스터 군단 진영 체력`이다. 개별 몬스터를 진영 체력의 주체나 보스 HP 대상으로 표시하지 않는다.
- 몬스터 AI는 `돌격대`와 `방어대`를 별도로 운용한다. 돌격대는 유저 점령지 공격과 거점 탈환을 담당하고, 방어대는 현재 몬스터 거점에 주둔해 유저 공격을 막는다. 유저에게 별도의 돌격대·방어대 편성을 요구하지 않는다.
- 지도에는 거점별 점령 상태와 현재 교전지, 몬스터 방어대의 주둔 배치, 돌격대의 이동 경로와 다음 공격 목표를 동시에 표시한다. 유저가 방어대를 격파하면 전선이 전진하고, 몬스터 돌격대가 연합 진영 체력을 소진시키면 전선이 후퇴·탈환된다.
- 몬스터 원화와 SD 부대는 역할을 구분한다. 대형 원화는 전선 배경·지휘관 정보용이고, 지도와 진형에 배치하는 개별 유닛은 승인된 투명 SD 전신 리소스를 사용한다.
- 몬스터공성 UI에는 마름모, 회전된 정사각형, 다이아 형태의 아이콘·노드·장식·게이지를 사용하지 않는다. 거점과 정보 요소는 원형 또는 회전 없는 직사각형으로만 만든다.
- 상세 설계 기준은 `docs/monster-siege-territory-war-standard.md`를 단일 기준 문서로 사용한다.

## 블랙미라클 보상 수동 편입

- 2026-09-09 사용자 지시: 새 신화장비는 블랙미라클에 자동 추가하지 않는다. CMS에서 운영자가 직접 포함 체크 후 저장한 항목만 보상 후보로 사용한다.
- 장비·이동수단의 활성화, 유저 공개, MYTHIC 등급 지정만으로 보상 풀에 들어가면 안 된다. 확률 계산 모드와 무관하게 명시적인 `enabled:true` 선택이 필요하며, LEGACY 개봉도 동일하게 검증한다.
- 신규 항목은 CMS에서 기본 미체크 상태로 표시한다. 선택하지 않은 항목은 기존 선택 항목의 자동 확률 계산에도 영향을 주지 않는다.
- 2026-09-09 운영 목록의 기존 장비 22종·이동수단 16종과 개별 확률은 일회성 운영 작업으로 고정했다. 엠퍼러 4종은 제외 상태를 유지하며, 이 스냅샷 작업을 새 카탈로그마다 재실행하지 않는다.
- 관련 기준과 교환 작업 기록은 `docs/black-miracle-manual-pool-20260909.md`다.
## V3 연속 PVE 개편 통합 출시

- 2026-09-09 사용자 지시: **폐차장만 따로 올리지 않고 이번 개편안 전체를 한 번에 운영 배포한다.** 무한의탑은 재기획 대기이므로 이 배포 묶음에서 제외한다.
- 공통 V3 다중 몬스터·연속 전투, 실제 계정 스냅샷과 원정 복구, 폐차장 3구역·몬스터 6종, 입장/진행/결과 UI, CMS, 모바일·경제 검수를 한 묶음으로 관리한다. 작업표는 `docs/pve-continuous-overhaul-v1.md`다.
- 기능 하나의 구현·로컬 커밋·프리뷰 검수 완료는 단독 운영 배포 승인이 아니다. 전체 완료 전에는 운영 메뉴/API·CMS·계정 재화를 변경하거나 기능을 활성화하지 않는다.
- 추가 진격·회수 목표·누적 확정 보상은 아직 수량·확률을 확정하지 않았다. 프리뷰 수치를 운영 보상 정책으로 자동 적용하지 않는다.


## 2026-09-13 S·SS 스킬 제작·배정 위임

- 현재 운영 CMS S 6명·SS 8명에 대해 사용자가 원화 컨셉에 맞춘 스킬 제작·배정을 명시적으로 지시했다. 이 범위는 2026-09-11의 사용자 직접 배정 원칙에 대한 명시적 후속 위임이다. 독립 스킬 목록과 기존 사용자 연결은 보존하고, 다른 등급을 자동 배정하지 않는다.
- 작업 기준과 감사 가능한 연결 목록은 `docs/mercenary-s-ss-skills-20260913.md`, `preview/project-v-mercenary-system-v1/skill-s-ss-plan-v3.json`이다. 개별 16프레임의 실제 재질 변화·투명 경계·V3 재생·사운드·모바일을 검수한다. 기술 통과를 사용자 시각 승인으로 바꾸지 않는다.

## 2026-09-13 실게임 연결·유저 개방 OFF

- 사용자 후속 지시로 S·SS 컨셉 스킬, 용병카드 하이퍼팩 개봉, 폐차장과 카우방은 실제 계정·운영 CMS·전투·보상·복구 경로에 연결한다. 검수 프리뷰로 사용자를 보내는 것으로 대체하지 않는다.
- 하이퍼팩의 실제 개봉은 용병 개봉 확률 CMS 한 곳을 사용한다. 같은 등급 카드의 중복 여부와 관계없이 균등 추첨하며, 결과는 서버 영수증의 실제 원화·등급·수량·중복 누적 값으로 표시한다. 기존 상점 가격은 1회 5억, 10회 50억이다.
- **유저 개방은 OFF로 유지한다.** 코드 연결과 운영 설정 저장을 기능 ON 또는 미정 스킬 수치 승인으로 취급하지 않는다. 공동 출시 게이트와 감사 가능한 정책 변경 기록은 `docs/v3-live-connections-2091-20260913.md`를 따른다.
