# 고유특성 V2 + 전술 전직 설계서 (rev.2)

작성 2026-08-30 · 기준 코드 v1902 · 모든 수치는 실제 `_battle_v2_preview.js` 하네스 측정값

---

## 0. 세 줄 요약

1. **고유특성이 문제가 아니다.** 계열(공격·방어·속도·생명) 선택만으로 PVP 승률이 이미 0% 또는 100%로 갈린다. 편차 78%p.
2. **원인은 스탯 두 개가 죽어 있기 때문이다.** 방어 스탯은 감소 곡선 상수가 안 맞아 무효, 생명 스탯은 부활 규칙이 봉인돼 무효. 남은 건 공격력과 행동 빈도뿐이라 `속도 > 공격 > 방어 > 생명` 하드 서열이 나온다.
3. **준비된 이펙트 6종이 곧 해법이다.** CRITICAL·COUNTER·DODGE·REVIVE·ULTIMATE·BOSS ULT. 는 전부 엔진에 이미 있는 판정이다. 각 계열에 판정을 하나씩 소유시키면 신규 메커니즘 없이 상성이 생긴다.

---

## 1. 실측 진단

### 1-0. 계열은 카드가 아니라 고유효과가 정한다

`normalizeType()`은 `uniqueAbility.dominantType`을 먼저 보고, 없으면 `card.power_type`을 본다.

그런데 **`power_type`의 실제 값은 `NORMAL` / `HIGH` / `TOP` / `FIXED`**다. v985에서 도입된 *전투력 등급*(클라이언트에서 ⚡ / ⚡⚡ / ⚡⚡⚡으로 표시)이지 계열이 아니다. `STAT_PROFILES`에는 이 키들이 없으므로 전부 `NONE`(균형형)으로 떨어진다.

> **고유효과가 없는 카드는 전원 균형형이다. 계열은 `card_unique_effects`의 4스탯 중 최고값(`dominantType`) 하나로만 결정된다.**

즉 지금의 `힐방방공속` 메타는 카드 풀이 만든 게 아니라 **CMS에 입력된 고유효과 수치가 만든 것**이다. OWNER가 `hp_percent`를 가장 높게 준 카드가 곧 힐형이 된다. 좋은 소식은 메타를 바꾸는 가장 빠른 레버가 이미 CMS 안에 있다는 것이고, 나쁜 소식은 **한 카드가 계열 하나에만 속할 수 있어 하이브리드가 원천적으로 불가능**하다는 것이다.

### 1-1. 현행 PVP 계열 매치업 (동종 5장, 카드 12만 × 5 + 장비 50만, 매치업당 120시드)

| 공격측 \ 수비측 | 공격 | 방어 | 속도 | 생명 | 평균 |
|---|---:|---:|---:|---:|---:|
| **공격** | 43 | 100 | 0 | 100 | 61 |
| **방어** | 0 | 51 | 0 | 100 | 38 |
| **속도** | 100 | 100 | 53 | 100 | **88** |
| **생명** | 0 | 0 | 0 | 44 | **11** |

순환이 아니라 **일렬 서열**이다. 속도형이 모든 계열을 이기고 생명형이 모든 계열에 진다. 미러전만 44~53%로 정상.

균형덱 `[공,방,속,생,공]` 기준 한 장 교체 민감도:

| 5번째 카드 | 승률 |
|---|---:|
| 공격 | 53% |
| 방어 | 39% |
| **속도** | **79%** |
| 생명 | 0% |

**카드 한 장 바꾸는 것만으로 26%p가 움직인다.** 이 상태에서 고유특성에 ±6% 상성 보정을 얹는 건 의미가 없다.

### 1-2. 원인 분해 — 판정 계층은 범인이 아니다

각 판정을 계열 중립으로 만들고 다시 측정했다.

| 조치 | 공격 | 방어 | 속도 | 생명 | 편차 |
|---|---:|---:|---:|---:|---:|
| 현행 | 62 | 37 | 88 | 11 | 78%p |
| 회피 차등 제거 | 62 | 37 | 85 | 11 | 75%p |
| 치명타 차등 제거 | 61 | 37 | 88 | 11 | 78%p |
| 관통 차등 완화 | 62 | 37 | 88 | 11 | 78%p |
| 위 셋 전부 | 61 | 37 | 86 | 11 | 75%p |
| **방어 감소 곡선 부활** | 63 | **53** | **75** | 11 | **63%p** |

회피·치명타·관통을 전부 껐는데 편차가 3%p밖에 안 줄었다. 유일하게 움직인 건 방어 곡선이다.

### 1-3. 죽어 있는 스탯 두 개

**방어** — `clamp(D_eff/(D_eff+600), 0, 0.65)`. 상수 600이 전투력 스케일에 안 맞아 전투력 1만 이상은 전원 정확히 65%. 방어 스탯도 관통 수치도 실제로는 아무것도 안 한다.

**생명** — 생명형은 세 겹으로 봉인돼 있다.

- `healerDuplicatePenalty`: 2장부터 회복 60%, 5장 90% 감소
- `SURVIVE`(불굴의 생존, 최대HP 12% 부활): `teamHealerCount < 2` 조건 — **2장부터 부활 자체가 사라진다**
- `suddenDeathAfter: 64`: 연장전 진입 시 지속 회복 전면 차단. 생명형은 딜이 약해 64행동 안에 못 끝내고, 연장전에 들어가면 회복이 다 꺼져 그냥 맞아 죽는다

측정: 생명형 5장 vs 공격형 5장 = **21행동 만에 전멸, 승률 0%**. 회복량을 4%→7%로 올려도, 부활 장수 제한을 풀어도 **승률 변화 0%p**. 회복은 생명형의 문제가 아니었다.

### 1-4. 고유효과 2계층 이중 적용

패시브 층(`buildCardUniqueDeckState`)이 `card.power`를 부풀리고, 엔진(`buildFighter`)이 같은 %를 한 번 더 곱한다. 게다가 `buildFighter`는 HP·공격·방어·속도를 전부 `power`에서 파생하므로 **공격 특성이 HP와 방어까지 올린다.**

| 고유 공격% | 실제 공격 배율 | 부수 효과 | 의도 |
|---:|---:|---|---:|
| 40% | 2.04x | HP 1.40x, 방어 1.40x | 1.40x |
| 80% (FUR +13) | 3.46x | HP 1.80x | 1.80x |
| 1000% (CMS 상한 × FUR+13) | **74.3x** | HP 11.0x | 11x |

적용 경로가 콘텐츠마다 다르다.

| 콘텐츠 | 패시브 | 발동 보너스 | 승패 판정 |
|---|---|---|---|
| PVE 몬스터 · 방치자동 | **이중** | 표시만 | V2 엔진 |
| 공성 · 호송 | **이중** | 없음 | V2 엔진 |
| PVP 랭크 · 영토전 | 단일 | 표시만 | V2 엔진 |
| 무한의탑 | 단일 | **승패 반영** | 전투력 비교 |
| 리프트 · 레이드 · 방치던전 | 단일 | 없음 | 전투력 비교 |
| 봉인전 · 폐차장 | 단일 | 승패 반영 | 전투력 비교 |

`resolveUniqueBattleRuntime`의 발동 보너스는 **엔진이 켜진 경로에서 승패에 전혀 반영되지 않는다.** 화면의 발동 연출은 순수 UI다. 반대로 탑·리프트·봉인전은 그 숫자만으로 승패가 갈린다.

### 1-5. 죽은 CMS 컬럼

`card_unique_effects.effect_type`, `trigger_type` — CMS가 쓰고 저장하지만 어떤 전투 경로도 읽지 않는다.
`max_activations` — `if(maxActivations < 1) continue` 게이트뿐. 감소시키는 코드가 없다.
`dominantType` — 4스탯 중 최고값 하나만 발동에 쓰고 나머지 3개는 버린다.

---

## 2. 설계 원칙

1. **버그 수정과 밸런스 개편을 같은 패치에 담지 않는다.** 이중 적용 제거만으로 PVE 실효 공격력이 30~70% 변한다. 상성 개편과 섞으면 원인 분리가 영영 불가능해진다.
2. **새 상성을 만들지 않는다.** 이미 있는 극단 상성을 완만하게 만든다.
3. **계열의 정체성은 스탯 배분이 아니라 판정 소유로 준다.** 스탯은 선형으로만 기여하지만 판정은 곱으로 기여한다. 프로필 숫자만 조정해서는 균형이 안 잡힌다(측정 확인).
4. **전직은 계열 균형이 잡힌 뒤에만 튜닝할 수 있다.** 기동형이 이미 승률 100% 천장에 붙어 있으면 어떤 대가를 물려도 승률이 안 내려간다(측정 확인).
5. **전 콘텐츠가 하나의 해석기를 정확히 한 번 통과한다.**

---

## 3. S0 — 정합성 복구 (밸런스 개편 아님)

### 작업

| # | 내용 | 파일 |
|---|---|---|
| S0-1 | 패시브 이중 적용 제거. PVE·공성·호송이 `unique.cards`(부스트본) 대신 원본 카드를 엔진에 넣고, 고유 %는 `buildFighter`에서만 적용 | `api/[[path]].js:5921`, `_siege.js:1547`, `_escort_operation.js:315` |
| S0-2 | `scaleUniqueEffect` 뒤 재클램프 추가 (±500 / 속도 300) | `_magic.js:230` |
| S0-3 | 발동 보너스를 엔진 안으로 이동. 덱 전투력에 더하는 대신 해당 카드의 `attack`/`shield`/`hp`에 적용 | `_magic.js:332` |
| S0-4 | 리프트·레이드·방치던전에 발동 층 연결 | `api/[[path]].js:5781`, `_idle_dungeon.js` |
| S0-5 | 덱 전투력 공식에서 속도 항 제거 (`× (1 + speedPercent/200)` 삭제). 속도는 엔진 안에서만 의미를 갖게 한다 | `_magic.js:258` |

### 예상 영향 (측정)

PVE 보스전, 카드 12만 × 5 + 장비 50만:

| 고유 공격% | 승률 (전/후) | 행동 수 (전/후) |
|---:|---|---|
| 20% | 100% → 100% | 41 → 45 |
| 40% | 100% → 100% | 32 → 38 |
| 80% | 100% → 100% | 21 → 29 |

**승패는 전 구간 변화 없음. 전투가 19~41% 길어질 뿐이며, v1902의 63행동 상한 안에 들어온다.** 유저 체감 손실은 "전투가 몇 초 길어졌다"가 전부다.

### 참고: PVE는 현재 구조적으로 지지 않는다

`MONSTER_MIN_DAMAGE_PERCENT = 0.016` 때문에 플레이어 63행동이면 몬스터 전투력과 무관하게 죽는다. 몬스터 전투력을 4000만까지 올려도 승률 100%가 유지된다. PVE 난이도는 "이기냐 지냐"가 아니라 "몇 장 죽느냐"로만 존재한다. 난이도를 올리려면 `pve_attack_percent` / 보스 공격 계수를 써야 하며, 전투력 숫자로는 조절되지 않는다.

---

## 4. S1 — 계열 V2

### 4-1. 판정 소유 구조

각 계열이 준비된 이펙트 하나를 소유한다. **여섯 개 전부 엔진에 이미 있는 판정이라 신규 메커니즘은 0개다.**

| 계열 | 소유 판정 | 이펙트 | 엔진 위치 | 현재 상태 |
|---|---|---|---|---|
| **파쇄** (공격) | 치명타 | `critical` | `hitResult` 치명 확률 16% / 배율 1.50 | 있음, 연출 없음 |
| **수호** (방어) | 반격 | `counter` | `COUNTER` 이벤트, 확률 25% | **있음, 이벤트까지 일치** |
| **기동** (속도) | 회피 | `dodge` | `hitResult` 회피 10~24% | 있음, 연출 없음 |
| **생명** (HP) | 부활 | `revive` | `SURVIVE` / `INDOMITABLE` 이벤트 | 있음, 2장부터 봉인 |
| 전 계열 · 전직 발동 | 개전 궁극기 | `ultimate` | `openingPlayerUltimateDamage` | 있음 |
| 보스 | 보스 궁극기 | `boss-ultimate` | `openingBossUltimatePercent` | v1902에서 수정 |

### 4-2. 상성은 판정끼리 부딪혀서 자연 발생한다

- 회피는 치명타를 무효화한다 → **기동이 파쇄를 이긴다**
- 반격은 회피 판정을 건너뛴다(`if(!counter && random() < dodgeChance)`) → **수호가 기동을 이긴다**
- 처형 배율(`execute`)은 공격형만 갖는다 → **파쇄가 수호를 이긴다**
- 부활은 순간 폭딜을 한 번 무효화한다 → **생명이 파쇄를 버틴다**

곱하기 보정표를 새로 만들 필요가 없다. 이미 있는 판정의 강도만 조절하면 된다.

### 4-3. 수치 (측정으로 확정)

**스탯 프로필**

| 계열 | HP | 공격 | 방어 | 속도 | 변경 |
|---|---:|---:|---:|---:|---|
| 파쇄 | .37 | .34 | .15 | .14 | HP +.03, 공격 −.04, 방어 +.01 |
| 수호 | .40 | .25 | .25 | .10 | HP −.03, 공격 +.03 |
| 기동 | .39 | .27 | .16 | **.18** | **속도 .27 → .18**, HP +.05 |
| 생명 | .44 | .27 | .17 | .12 | HP −.08, 공격 +.07, 속도 +.02 |
| 균형 | .40 | .28 | .18 | .14 | 유지 |

**판정 상수**

| 항목 | 현행 | V2 | 근거 |
|---|---|---|---|
| 방어 감소 분모 | 고정 600 | `공격자 공격력 × 0.9` | 상수 600은 전투력 1만 이상에서 전원 65% 상한. 유일하게 효과가 확인된 레버 |
| 생명 부활량 | 최대HP 12% | **42%** | 12~50% 스윕 결과. 생명형 평균 승률 11% → 35% |
| 부활 장수 제한 | 1장까지만 | 제한 없음 | `teamHealerCount < 2` 조건 삭제 |
| 긴급 회복 | 18% | 30% | |
| 연장전 지속 회복 | 완전 차단 | 50% 유지 | 연장전 = 생명형 자동 패배 구조 해소 |
| 지속 회복 | 4% | 5.5% | |
| 반격 확률 | 25% | 45% | |
| 반격 배율 | 0.55 | 0.68 | |

**결과 (매치업당 150시드)**

| 공격측 \ 수비측 | 파쇄 | 수호 | 기동 | 생명 | 평균 |
|---|---:|---:|---:|---:|---:|
| **파쇄** | 51 | 97 | 0 | 45 | 48 |
| **수호** | 3 | 43 | 100 | 66 | 53 |
| **기동** | 100 | 0 | 43 | 100 | 61 |
| **생명** | 55 | 35 | 0 | 49 | 35 |

계열 편차 **78%p → 26%p**. `파쇄 → 수호 → 기동 → 파쇄` 3순환이 성립하고, 생명은 파쇄에 강하고 기동에 약한 네 번째 축이 된다.

> 이 표는 **동종 5장 몰빵 덱** 기준이라 극단값이 나온다. 몰빵 덱이 극단인 것은 의도된 결과다(덱 구성이 의미를 가져야 한다). 혼합 덱 밴드는 S1 적용 후 별도 측정한다.
>
> ⚠️ 기동형이 아직 평균 61%로 남아 있다. 속도 프로필을 .18 아래로 더 내리는 것과 회피 상한을 24%→18%로 내리는 것을 S1 후속에서 측정해야 한다. **기동형이 천장에 붙어 있는 한 기동 계열 전직은 튜닝이 불가능하다**(§6 참조).

### 4-4. 고유특성별 효과 — `effect_type` 8종

`dominantType`(최고값 하나만 발동)을 폐기하고 4스탯을 전부 유효화한다. 그 위에 `effect_type`을 실제로 읽어서 특성마다 다른 일을 하게 만든다. **8종 중 4종은 현행 메타 코어를 직접 겨냥한다.**

| `effect_type` | 이름 | 효과 | 이펙트 | 무엇을 깨나 |
|---|---|---|---|---|
| `REVIVE_SEAL` | 봉인 | 대상 팀의 부활 예산 1 감소 (다음 부활 1회 무효) | `revive` 역재생 | **방어형 불굴 · 생명형 생존 = 탱커 코어 직격** |
| `HEAL_BLOCK` | 쇠약 | 4행동 동안 대상 팀 회복 −70% | `critical` | **힐 오라 = 힐 코어 직격** |
| `SHIELD_PIERCE` | 관통 | 실드 무시, 방어 감소 −40%p | `critical` | 불굴 실드 · 수호 결계 |
| `EXECUTE` | 처형 | HP 35% 이하 대상 피해 +30% | `critical` | 부활 직후 저HP 마무리 |
| `GAUGE_DRAIN` | 교란 | 적 게이지 −25, 자신 +15 | `dodge` | 속도 편중 선공 |
| `COUNTER_STANCE` | 반격 | 받은 피해 25% 반사 | `counter` | 공격 몰빵 |
| `AEGIS` | 수호 | 팀 최대HP 8% 실드, 1회 | `counter` | — |
| `REBIRTH` | 재생 | 아군 1장 부활 (최대HP 30%) | `revive` | — |

**`REVIVE_SEAL` 검증** — 상대가 `힐방방공속`일 때, 내 덱이 봉인 특성을 몇 장 들고 있느냐에 따른 승률 (200시드):

| 내 덱 | 봉인 0장 | 1장 | 2장 | 3장 |
|---|---:|---:|---:|---:|
| 힐방방공속 (미러) | 51.0% | 53.5% | **71.0%** | **82.5%** |
| 방힐공공속 | 82.5% | 86.5% | 95.5% | 98.0% |

미러전에서 **20~31%p가 움직인다.** 구조 수정(§4-6)이 탱힐 코어의 *이득*을 줄인다면, 봉인·쇠약 특성은 그 코어를 *도박*으로 만든다. 둘이 같이 있어야 메타가 굳지 않는다.

`trigger_type`도 고정 규격으로 관리한다. 자유 입력을 제거한다.

`BATTLE_START` / `BEFORE_ATTACK` / `AFTER_ATTACK` / `BEFORE_DAMAGE` / `AFTER_DAMAGE` / `HP_BELOW_40` / `ON_ALLY_TRAIT` / `ON_ENEMY_TRAIT` / `ON_KILL` / `ON_REVIVE`

`max_activations`를 실제로 감소시키는 코드를 넣는다. 발동 횟수는 서버가 카운트한다.

### 4-5. 발동 방식 — 게이지 확정

PVP·PVE 모두 게이지 확정 발동으로 통일한다. 발동 규칙을 둘로 나누면 CMS·엔진·검증 하네스가 전부 두 배가 되고, 지금 겪는 문제가 정확히 "모드마다 다르게 동작한다"이다. PVE 난이도는 게이지 충전 속도로 조절한다.

- 카드마다 원형 특성 게이지
- 조건 수행 시 게이지 상승, 100에서 발동
- 발동 횟수는 서버가 카운트

### 4-6. 메타 파괴 — `힐방방공속`이 굳은 이유와 해체

#### 왜 이 조합인가 (측정)

`힐 1장 + 방어 2장` 코어는 취향이 아니라 **규칙이 강제한 유일해**다.

| 규칙 | 내용 | 결과 |
|---|---|---|
| `singleHealerBonus` | 힐러가 **정확히 1장일 때만** 켜짐. 팀 최대HP +8%, 회복 4회, 위기 회복 16% | 힐 2장 = 통째로 꺼짐 |
| `healerDuplicatePenalty` | 2장 60% · 3장 75% · 4장 85% · 5장 90% 회복 감소 | 힐 2장 = 추가 페널티 |
| `SURVIVE` | `teamHealerCount < 2` 조건 | 힐 2장 = 부활 소멸 |
| `INDOMITABLE` | 방어형 카드당 1회 무료 부활 + 실드. **팀 총량 제한 없음** | 방어형을 쌓을수록 선형 이득 |

힐은 **1장에서 절벽**, 방어는 **장수에 선형 비례**. 두 규칙이 만나면 `힐1 + 방N`이 답이 된다.

측정: `singleHealerBonus`를 끄면 힐방방공속의 리그 평균 승률이 **80.5% → 72.4%**로 떨어진다(9위 → 15위). 이 조합의 동력 대부분이 이 한 규칙이다.

#### 수치 조정으로는 안 깨진다

계열 중첩 체감(스탯 감쇠)만 넣으면 힐방방공속이 오히려 **80.5% → 84.9%로 올라간다.** 스탯은 깎이지만 불굴·오라·부활 같은 **능력**은 안 깎이기 때문이다. 능력이 장수에 비례하는 한 쌓는 게 계속 이득이다.

#### 3단 레버

| 레버 | 내용 |
|---|---|
| **L1 · 절벽 → 곡선** | `singleHealerBonus`를 힐러 장수 비례 연속 함수로. 배율 `[1, 0.65, 0.4, 0.25, 0.15]`. `SURVIVE`의 `teamHealerCount < 2` 조건 삭제. `healerDuplicatePenalty` `[0, 25, 45, 62, 75]`로 완화 |
| **L2 · 능력 총량제** | 부활은 **팀당 종류별 1회**. `INDOMITABLE` 팀 1회, `SURVIVE` 팀 1회. 방어형을 3장 넣어도 무료 부활은 1회 |
| **L3 · 계열 중첩 체감** | 같은 계열 k번째 카드의 프로필 편차를 `[1, 0.72, 0.45, 0.28, 0.28]`로 감쇠. 균형형 프로필로 수렴시킨다 |

#### 결과 (56개 조합 전수 리그, 조합당 20~28시드)

| 상태 | 1위 조합 | 힐방방공속 | 1~56위 폭 | 경쟁권(45~65%) | Top10 중 3장 몰빵 |
|---|---|---:|---:|---:|---:|
| 현행 | 방방속속속 95.9% | 80.5% | 96%p | 15개 | 4개 |
| S1 계열 V2 | 방방힐공공 91.4% | 84.0% | 88%p | 18개 | 1개 |
| + L1 | 방방힐공공 84.9% | 74.0% | 80%p | 17개 | 1개 |
| + L3 | 방방힐공공 80.4% | 70.5% | 79%p | 20개 | 1개 |
| **+ L2 (최종)** | **방힐공공속 86.6%** | **77.1% (7위)** | 81%p | 17개 | **1개** |

최종안 상위 10과 하위 10:

| 순위 | 조합 | 승률 | | 순위 | 조합 | 승률 |
|---:|---|---:|---|---:|---|---:|
| 1 | 방힐공공속 | 86.6% | | 47 | 속속속속속 | 25.6% |
| 2 | 방힐속속속 | 82.9% | | 48 | 공공속속속 | 25.5% |
| 3 | 방힐공속속 | 82.6% | | 50 | 공속속속속 | 21.6% |
| 4 | 방방힐공공 | 80.1% | | 52 | 힐힐힐힐공 | 20.0% |
| 5 | 방방힐속속 | 79.7% | | 53 | 방방방방방 | 16.9% |
| 6 | 방방공공속 | 78.4% | | 55 | 힐힐힐힐힐 | 9.9% |
| 7 | 방방힐공속 | 77.1% | | 56 | 공공공공공 | 5.7% |

**바뀐 것** — 현행에서는 딜러 자리가 사실상 속도형 전용이었다. 최종안에서는 상위 10 안에 `공공속` · `속속속` · `공속속` · `힐속속` · `공공공`이 전부 들어온다. 딜러 구성이 5가지로 갈라진다.

**남은 것** — 탱커 1~2장 + 힐러 1장 코어는 여전히 상위권이다. 5장 파티 게임에서 이건 자연스러운 결과이며, 목표는 코어를 없애는 게 아니라 **유일해에서 여러 해 중 하나로 바꾸는 것**이다. 나머지는 §4-4의 카운터 특성이 담당한다 — 봉인 2장이면 미러전이 51% → 71%로 뒤집힌다.

**적용 주의** — L1·L2는 PVP·영토전의 매치업을 직접 바꾸므로 반드시 시즌 종료 시점에 적용한다. PVE는 먼저 열어도 된다.

---

---

## 5. S2 — 이펙트 6종 결선

밸런스와 독립이므로 S0과 **병렬 진행 가능**하다. 신규 판정이 없어 위험도 0이다.

### 결선 대상

| 이펙트 | 바인딩 대상 | 트리거 |
|---|---|---|
| `critical` | 치명타 | `TURN` 이벤트 `critical: true` |
| `dodge` | 회피 | `TURN` 이벤트 `dodge: true` |
| `counter` | 방어형 반격 | `COUNTER` 이벤트 |
| `revive` | 부활 | `SURVIVE` · `INDOMITABLE` · `MAGIC_CARD{revived:true}` |
| `ultimate` | 개전 궁극기 · 전직 발동 | `openingPlayerUltimateDamage` |
| `boss-ultimate` | 보스 궁극기 | `openingBossUltimatePercent` |

### 규격

- 12프레임 512×455, 4×3 아틀라스 2048×1365, RGBA8888 PNG + PixiJS v8 JSON
- 24fps = 500ms, 충돌 프레임 6 (250ms), 블렌드 `screen`
- 사운드 48kHz 스테레오 MP3 256kbps, 충돌 동기점 250ms

### 결선 규칙

- 이벤트 타입 필터에 주의: 일반 공격 이벤트 타입은 `ATTACK`이 아니라 **`TURN`**이다
- 치명타·회피는 판정당 최대 1회. 연속 발생 시 마지막 것만 재생
- 동시 발생 우선순위: `boss-ultimate` > `revive` > `ultimate` > `counter` > `critical` > `dodge`
- 카드 위에 정보를 계속 덧붙이지 않는다. 0.5초 이펙트 + 게이지 링만 쓴다
- **CSS 대각선 광선·빛기둥 장식은 쓰지 않는다.** 마름모·원형 룬·별도 쓰지 않는다(생성 단계에서 이미 배제됨)

---

## 6. S3 — 전술 전직 4종

### 6-1. 계열 하나에 전직 하나

계열이 `공격 · 방어 · 속도 · 생명` 네 개이므로 전직도 네 개다. 각 전직은 그 계열이 소유한 판정을 강화하고, 준비된 스프라이트가 그 판정에 붙는다.

| 계열 | 전직 | 소유 판정 | 스프라이트 | 핵심 효과 | 대가 |
|---|---|---|---|---|---|
| 공격 | **파쇄자** | 치명타 | `critical` | 방어 관통 +30%p, 치명 확률 +6%p | 최대HP −7% |
| 방어 | **반격자** | 반격 | `counter` | 반격 확률 +34%p | 가하는 피해 −14% |
| 속도 | **잔영자** | 회피 | `dodge` | 회피 +14%p, 관통 +14%p | 최대HP −(S1 후 재측정) |
| 생명 | **불멸자** | 부활 | `revive` | 부활량 +14%p | 가하는 피해 −16% |
| 공통 | 특성 각성 (+13) | 개전 발동 | `ultimate` | 고유특성 발동 1회 추가 | — |
| — | 보스 | 개전 궁극기 | `boss-ultimate` | (기존) | — |

**스프라이트 6종이 정확히 맞아떨어진다.** 전직 4개 + 각성 1개 + 보스 1개. 남거나 모자라는 게 없고, 여섯 개 전부 엔진에 이미 있는 판정이라 신규 메커니즘이 0개다.

### 6-2. "선택"은 전직이 아니라 덱 편성이 담당한다

계열당 전직이 하나면 전직은 분기 선택이 아니라 **계열 특화**가 된다. 이건 단점이 아니다. 선택은 이미 한 단계 앞, 어느 계열을 몇 장 넣느냐에서 일어나고(§4-6), 전직은 그 선택의 결과를 선명하게 만드는 역할을 한다. 전직 8종은 밸런싱해야 할 축이 두 배가 되는데, 계열 균형조차 아직 안 잡힌 상태에서 그걸 먼저 벌리면 원인 분리가 불가능해진다.

깊이가 필요해지면 나중에 `+12` 노드에서 2택을 여는 게 안전하다. 그때는 이미 계열 균형이 잡혀 있고 전직 4종의 실측값도 쌓여 있다.

### 6-3. 작동하는 레버와 작동하지 않는 레버 (측정)

| 레버 | 승률 영향 | 판정 |
|---|---|---|
| 부활량 | 최대 44%p | ✅ 가장 강력 |
| 처형 배율 | 18%p | ✅ 강력 |
| 반격 확률·배율 | 18%p | ✅ 유효 |
| 관통 | 14%p | ✅ 유효 |
| 게이지·속도 | 미러전 한정 22%p | ⚠ 편중 |
| 치명타 확률 | 3%p 미만 | ❌ 무의미 |
| 회피 확률 | 1%p | ❌ 무의미 |
| 회복량 | 0%p | ❌ 무의미 |

> 치명타·회피가 단독으로는 무의미하다는 점이 중요하다. **파쇄자는 치명타가 아니라 관통이 본체이고, 잔영자도 회피가 아니라 관통이 본체다.** 스프라이트는 치명타·회피에 붙지만, 승률을 만드는 건 관통이다. 연출과 수치를 분리해서 설계해야 한다.

### 6-4. 대가 수치 (자동 튜닝, 매치업당 200시드)

"평균 승률 변화 0%p"를 목표로 대가를 이분 탐색해 얻은 값이다.

| 전직 | vs공격 | vs방어 | vs속도 | vs생명 | 평균 | 분산 | 판정 |
|---|---:|---:|---:|---:|---:|---:|---|
| 파쇄자 | 0 | −12 | 0 | +14 | +0.5%p | 25%p | ✅ |
| 불멸자 | +17 | −17 | 0 | −4 | −0.8%p | 33%p | ✅ |
| 반격자 | −3 | +6 | 0 | −5 | −0.4%p | 11%p | ⚠ 분산 부족 |
| 잔영자 | 0 | 0 | +23 | 0 | +5.6%p | 23%p | ❌ S1 후 재측정 |

- 파쇄자·불멸자는 그대로 쓸 수 있다. 매치업마다 부호가 갈려 옆방향 성장이 성립한다.
- 반격자는 효과·대가를 같이 키워야 고를 이유가 생긴다.
- **잔영자는 지금 튜닝이 불가능하다.** 기동형이 vs공격 100% · vs생명 100%로 승률 천장에 붙어 있어 최대HP를 30% 깎아도 평균이 올라간다. S1에서 기동형을 내린 뒤 다시 잡는다.

### 6-5. 성장 경로

| 단계 | 조건 | 개방 |
|---|---|---|
| 전직 개방 | 카드 +10 & 특성 숙련도 100 | 계열 전직 확정 |
| 노드 I | +11 | 전직 효과 1단 |
| 노드 II | +12 | 전직 효과 2단 |
| 특성 각성 | +13 | 고유특성 발동 1회 추가 (`ultimate` 연출) |

- 기존 +11~13 카드는 투자 손실 없이 해당 노드를 즉시 개방
- 고등급 재뽑기 시 단계·숙련도 보존, 새 계열 전직으로 자동 승계
- **초기 버전은 계정 내 같은 카드 ID가 하나의 전직을 공유한다.** 계열이 카드의 고유효과로 정해지므로 애초에 카드당 전직은 하나뿐이고, 인스턴스 구조 변경이 필요 없다.

### 6-6. 재화

미스틱 에너지는 제작 기대 비용이 높으므로 1차 전직 재료로 쓰지 않는다.

| 단계 | 재료 |
|---|---|
| 전직 개방 · 노드 I · II | 코인 + 카드 조각 + 전직 인장 |
| +13 특성 각성 | 미스틱 에너지 1개 |

## 7. 화면

### 카드 상세 3탭

`고유 특성 | 성장·강화 | 전직`

전직 탭 구성:

- 상단 — 계열, 현재 전직, 발동 조건, 남은 발동 횟수
- 중앙 — 원형 숙련도 게이지
- 하단 — 전직 2종 비교, 변경되는 효과와 대가

**회전하지 않은 직사각형 카드만 쓴다. 마름모 장식·대각선 광선·빛기둥은 쓰지 않는다.**

### 나머지

- 덱 편성 — 계열 분포와 상대 덱 상성 예상만 간결하게
- 전투 — 라이브 V3 카드 원본·제니스·슈퍼스타 프레임 유지
- 발동 연출 — 0.5초 이펙트 + 게이지 링. 카드 위 정보 누적 금지
- CMS — 기존 고유효과 표에 열을 추가하지 않고 별도 `전직 설계` 화면 + 4×4 상성 시뮬레이터

### 화면은 마지막이다

신규 화면이 들어가면 밸런스 검증이 UI 완성을 기다리게 된다. 전직은 CMS 값 + 기존 상세 화면 하단 섹션으로 먼저 돌리고, 화면은 수치가 굳은 뒤에 만든다.

---

## 8. 적용 순서

| 단계 | 내용 | 밸런스 변화 | 선행 조건 |
|---|---|---|---|
| **S0** | 이중 적용 제거, 재클램프, 발동 보너스 엔진 편입, 누락 콘텐츠 연결, 속도 항 제거 | 승패 0, 전투 길이 +19~41% | 없음 |
| **S2** | 이펙트 6종 결선 | 없음 | 없음 (S0과 병렬) |
| **S1a** | 방어 곡선 부활 + 생명 부활 규칙 해제 + 프로필 조정 | 계열 편차 78 → 26%p | S0 |
| **S1b** | 기동형 재조정, 혼합 덱 밴드 측정 | 기동 61% → 목표 55% 이하 | S1a |
| **S1c** | 메타 파괴 L1·L2·L3 (힐 보너스 연속화, 부활 팀 총량제, 계열 중첩 체감) | 조합 폭 96 → 81%p | S1b |
| **S1d** | 고유특성 4스탯 유효화, `effect_type` 8종·`trigger_type` 10종 규격화, 게이지 확정 발동 | 특성 재설계 + 카운터 도입 | S1c |
| **S3** | 전직 4종 (계열당 1개) | 전직당 평균 0%p 목표 | S1d |

각 단계는 **비공개 비교 모드**(기존 결과와 신규 결과를 동시에 계산해 로그만 남김)로 먼저 돌리고, OWNER 테스트 → PVE 우선 적용 → 시즌 종료 시점에 PVP·영토전 순으로 연다.

---

## 9. 검증 하네스

`_battle_v2_preview.js`를 `.mjs`로 복사하면 DB 없이 그대로 import된다.

1. `build.mjs` — 원본에서 **밸런스 상수만** `globalThis.__T.*`로 치환한 사본 생성
   - 원본이 CRLF이므로 다중행 치환 전에 `.replace(/\r\n/g,'\n')` 필요
   - `defenseLineBreached` 같은 분기 상수는 **별도 키로 분리**해야 한다. `0.25 × 0.48`처럼 계산으로 유도하면 부동소수점 때문에 패리티가 깨진다
2. **기본값 패리티를 먼저 확인한다** — 원본 상수로 되돌렸을 때 승패·잔여 HP·종료 사유가 완전히 일치해야 한다 (960/960 확인 완료)
   - 패리티 비교는 `result.final` 전체가 아니라 `[hp, alive]` + `reason`으로 한다. 필드를 추가하면 JSON 비교가 무의미하게 깨진다
3. ESM 모듈 캐시 회피: `import('./tunable.mjs?v=' + Math.random())`
4. 기준 덱: 카드 12만 × 5 + 장비 50만. 조합당 40시드, PVP 매치업당 120~200시드
5. 무작위 600판 회귀 + 호송작전 100판 원본 일치 검사로 마무리

---

## 10. 코덱스 안과의 차이

| 항목 | 코덱스 안 | 본 설계 | 근거 |
|---|---|---|---|
| 진단 | 고유특성 계산식이 문제 | 계열 스탯 두 개가 죽은 것이 문제 | 판정 계층 ablation에서 편차 3%p만 이동 |
| 상성 | `±6%/−4%` 보정을 새로 얹음 | 기존 극단 상성을 완만하게 | 현행 매치업이 0/100이라 ±6%는 무의미 |
| 순환 방향 | 파쇄→수호→기동→생명→파쇄 | 파쇄→수호→기동→파쇄 3순환 + 생명은 별도 축 | 엔진 판정에서 유도. 생명형 4순환은 구현 비용이 큼 |
| 속도형 | 새 역할 부여 | 덱 전투력 공식에서 속도 항만 제거 | 선제권·연속 행동은 이미 엔진에 있음 |
| PVE 발동 | 확률 유지 | 전 모드 게이지 확정 통일 | 이원화 시 CMS·엔진·하네스 2배 |
| 전직 순서 | 특성 통일 후 바로 전직 | S0 → S1 → 재측정 → S3 | 계열이 천장이면 전직 대가가 작동하지 않음(측정) |
| 전직 개수 | 계열당 2개 = 8종 | **계열당 1개 = 4종** | 스프라이트 6종(전직4+각성+보스)과 정확히 일치. 계열 균형 전에 8종은 밸런싱 축이 두 배 |
| 메타 파괴 | 언급 없음 | L1·L2·L3 + 카운터 특성 4종 | 힐 1장 절벽과 불굴 무제한이 유일해를 강제 |
| 계열 중첩 체감 | 100/60/30/0% | 100/72/45/28% + **능력 총량제** | 스탯만 깎으면 오히려 +4.4%p (측정) |
| 미스틱 에너지 | 1차 제외 | 동일 | — |
| 카드 인스턴스 | 초기엔 카드 ID 공유 | 동일 | — |

---

## 부록 — 재현 스크립트

```
/tmp/uv/
  build.mjs      원본 → tunable.mjs 생성 (밸런스 상수 치환)
  parity.mjs     기본값 패리티 검사 (960/960)
  harness.mjs    덱 생성 · 매치업 매트릭스
  ablate.mjs     판정 계층 분해
  s0.mjs         이중 적용 제거 영향
  final.mjs      계열 V2 튜닝
  tune.mjs       전직 8종 자동 대가 튜닝
```

측정 결과 원본: `final-tuning.json`, `awaken-tuned.json`
