# 8방향 유도탄 검수 · 2026-09-24

## 관련 회귀

`tests/battle-suit-octaseeker-preview-v1.test.mjs`: 최초 12개 통과. 공개 주소 경로 회귀 1개를 추가해 최종 배포에서는 13개를 확인한다.

- 서로 다른 45도 출발 방향 8개, 동일 도착점 1개, 방사/선회 접점 위치·속도 연속성.
- 모든 시간/각도/프레임 유한값, 추진·폭발 각각 24개 실제 다른 프레임, 실제 RGBA·경계 알파·무손실 가시 픽셀·512px 칩.
- PC·모바일 8발 표시, 폭발 발끝 고정, 다른 적으로 재조준하지 않음, 사망/재바인딩 이후 미명중탄 중단.
- HP 무변경. 역방향 시킹 동일 장면. 50회 재생/시킹 후 스프라이트 풀 및 GSAP 시간축 1개 유지. 끝/중지/파괴 시 효과 정리 및 공용 텍스처 보존.
- 0.25/0.5/1/2배속에서 시각 충돌시점 불변, 원음 8개 충돌 예약의 출력 보정 오차 <1ms, 정지 시 예약 음원 제거.
- 기존 V3 엔진/카드 원화/아트 어댑터/프레임 재사용. 신규 코드가 운영 카탈로그/메인/장비/API에 연결되지 않았음을 검사.

## 실제 화면

Codex 브라우저에서 데스크톱 1440×1000과 모바일 390×844를 확인했다. 데스크톱 스크롤바 제외 실제 폭 1425px, 모바일 375px였으며 `scrollWidth === clientWidth`다.

- PC: 방사 0.34초, 선회 0.72초, 집중 폭발 1.48초를 확인. 8발과 추진 꼬리가 구분되고 폭발은 동일 적 발끝에 모인다.
- 모바일: 원본 V3 세로 진형·카드 5장과 등급 프레임 유지. 전장 자동 높이 612px. 가로 넘침 없음. 상단 칩/제목, 스크롤 및 조작 버튼 확인.
- 정상 속도 끝까지 재생, 0.25배속 일반 사격 병행, 1.92초 일시정지 후 시간이 유지되는 것, 2배속 변경/재개 직후 중지→0초 복귀 확인.
- 공유 V3 엔진의 비사용 보스 아틀라스 캐시 경고와, 페이지 파일/줄 번호 없이 보고되는 브라우저 `MutationObserver` 오류는 관찰됐다. 임시 전장 오류 수집기에는 전달되지 않았으며 이번 전장의 준비·자동 높이·재생·조작은 정상이다. 관련 없는 엔진/브라우저 범위로 수정 확장하지 않았다. 따라서 전체 콘솔 무오류라고 주장하지 않는다.
- 실제 스피커 루프백 및 사용자 시각/음원 승인은 미완료다.

## 한정 배포 기록

- 분류: 신규 독립 연출 프리뷰 한 곳. 서버·경제·공유 엔진/라이브 번들·인증·DB/의존성 변경 없음.
- 직전 실제 운영 배포: `7c6953d19ad57b757e7f3bcd49f50d3759b1befb`, Pages `81817970-8cd1-44ed-a08c-d6ad8fd62b47` (Wrangler 배포 목록에서 확인).
- 함께 따라오는 원격 변경은 문서 커밋뿐이며 유지했다.
- 지정 명령: `npm run deploy:production -- --scoped`.
- 선택 검사: `tests/battle-suit-octaseeker-preview-v1.test.mjs`; 추가 광범위 검사 없음. 최종 HTML 정리 이후 배포 과정에서 최종 회귀를 확인한다.
- 이유: 추가 파일은 프리뷰·자산·기록·관련 테스트에만 한정. 운영 스킬칩과 PVE/PVP 동작이 무변경이므로 전체 게임 검사나 별도의 실계정 거래 검수를 반복하지 않는다.
- 공개 후에는 프리뷰 정적 파일과 준비/재생 경로만 짧게 확인한다. 정적 프리뷰 공개와 실전 기능 활성화를 구분한다.

## 공개 주소 복구

첫 게시 `7a9953c08dcf4c0a049cd137f0f499f247189f0d` / `a4caf726.cnine-card.pages.dev`에서 파일 해시는 일치했으나 Pages가 `battle.html`을 `battle`로 리다이렉트했다. Pixi 경로 결합이 확장자 없는 페이지를 디렉터리로 판단해 공용 상대 배경이 `/preview/assets/`로 요청되어 프리뷰 준비가 실패했다.

공용 엔진은 변경하지 않고 새 프리뷰의 `<base href="/preview/battle-suit-octaseeker-v1/">`를 고정했다. 실제 Pixi 경로 결합으로 기존 오류와 정상 사이트 루트 해석을 함께 회귀 검사한다. 복구 배포의 직전 실제 운영 SHA는 위 `7a9953c08dcf4c0a049cd137f0f499f247189f0d`, 선택 검사는 동일 파일의 13개이며 원인과 인접 프리뷰 준비 경로만 확인한다.
