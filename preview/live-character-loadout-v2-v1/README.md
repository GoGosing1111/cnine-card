# 장비창 강화 테두리 — 2026-09-23

공유 운영 렌더러 `js/character-loadout-v2.js`와 `css/character-loadout-v2.css`를 그대로 사용한다.

- 확인 주소: `/preview/live-character-loadout-v2-v1/?enhancementGlow=1`
- +8: 청록색 맥동 테두리.
- +9: 보라색 테두리와 5초 주기 순환광.
- +10: 금빛 이중 순환광(3.8초)과 모서리 잔광.
- 일반 장비의 장착 슬롯·보유 장비 이미지 테두리에만 적용한다. +0~+7, 그 밖의 단계, 배틀슈트·칭호·이동수단·스킬칩 및 다른 화면에는 적용하지 않는다.
- 장비 원화, 등급 프레임, 전투력, 강화 비용/확률, 서버 저장은 변경하지 않는다.
- 광원과 강화 배지는 클릭을 가로채지 않으며, 동작 줄이기 설정에서는 정지 테두리를 유지한다.

## 확인

- 1440×1000 및 390×844에서 세 단계 색상, 이미지/수량/강화 표기, 가로 넘침 없음 확인.
- 공유 프리뷰의 PC·모바일 장착 버튼 조작과 슬롯 광원 갱신 확인. 운영 계정 재화/장비 변경 없음.
- 회귀: `tests/equipment-enhancement-glow-20260923.test.mjs` — 정확한 단계/화면 범위, 장착·해제, 능력치 불변, 접근성, 캐시 연결.
- 소규모 장비 UI 수정이므로 직전 운영 커밋 `562de9800b738d53544e66856392d544bc5ffa8e`를 기준으로 위 테스트만 선택하여 `npm run deploy:production -- --scoped`를 사용한다. 중간 규칙 전용 커밋도 포함하며 전체 게임 검사는 반복하지 않는다.

쿼리 옵션은 프리뷰의 가상 장비에만 강화 수치를 넣는다. 운영 API 응답이나 실제 보유 장비는 바꾸지 않는다.
