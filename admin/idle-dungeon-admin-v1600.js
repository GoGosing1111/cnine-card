(()=>{function install(){const nav=document.getElementById('nav');if(!nav||nav.querySelector('[data-view="idledungeon"]'))return;const b=document.createElement('button');b.dataset.view='idledungeon';b.innerHTML='방치형 원정 <span class="buildBadge">NEW</span>';nav.querySelector('[data-view="tower"]')?.after(b);const view=document.createElement('section');view.id='view-idledungeon';view.className='view';view.hidden=true;view.innerHTML='<div class="panel"><h2>방치형 원정 관리</h2><div id="idleAdminBody">설정을 불러오는 중...</div></div>';document.querySelector('main')?.appendChild(view);b.onclick=async()=>{document.querySelectorAll('.view').forEach(x=>x.hidden=x!==view);document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x===b));document.getElementById('pageTitle').textContent='방치형 원정 관리';const d=await api('admin/idle-dungeon'),s=d.settings,root=document.getElementById('idleAdminBody');root.innerHTML=`<div class="formgrid"><label class="field"><span>운영 상태</span><select id="idleEnabled"><option value="1" ${s.enabled?'selected':''}>ON</option><option value="0" ${!s.enabled?'selected':''}>OFF</option></select></label><label class="field"><span>최대 누적 시간</span><input id="idleHours" type="number" min="1" max="24" value="${s.maxOfflineHours}"></label><label class="field"><span>층당 진행 시간(초)</span><input id="idleFloorSeconds" type="number" min="5" max="300" value="${s.floorSeconds}"></label></div>${s.difficulties.map((x,i)=>`<div class="panel" data-idle-row="${i}"><h3>${x.name}</h3><div class="formgrid"><label class="field"><span>난이도명</span><input data-k="name" value="${x.name}"></label><label class="field"><span>최종층</span><input data-k="maxFloor" type="number" value="${x.maxFloor}"></label><label class="field"><span>시작 배율</span><input data-k="powerStart" type="number" step="0.01" value="${x.powerStart}"></label><label class="field"><span>최종 배율</span><input data-k="powerEnd" type="number" step="0.01" value="${x.powerEnd}"></label><label class="field"><span>6시간 코인 상한</span><input data-k="dailyCap" type="number" value="${x.dailyCap}"></label></div></div>`).join('')}<button id="saveIdleSettings">방치형 설정 저장</button>`;document.getElementById('saveIdleSettings').onclick=async()=>{const difficulties=s.difficulties.map((x,i)=>{const out={...x},row=document.querySelector(`[data-idle-row="${i}"]`);row.querySelectorAll('[data-k]').forEach(el=>out[el.dataset.k]=el.dataset.k==='name'?el.value:Number(el.value));return out});await api('admin/idle-dungeon',{method:'POST',body:JSON.stringify({enabled:document.getElementById('idleEnabled').value==='1',maxOfflineHours:Number(document.getElementById('idleHours').value),floorSeconds:Number(document.getElementById('idleFloorSeconds').value),difficulties})});alert('방치형 원정 설정을 저장했습니다.')}}}new MutationObserver(install).observe(document.documentElement,{childList:true,subtree:true});addEventListener('DOMContentLoaded',install)})();
