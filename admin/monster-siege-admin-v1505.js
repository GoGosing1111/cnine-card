(()=>{
  const $=s=>document.querySelector(s),token=()=>localStorage.getItem('cnine_admin_token')||'';let last=null;
  async function api(path,options={}){const r=await fetch('/api/'+path,{...options,headers:{'content-type':'application/json','authorization':`Bearer ${token()}`,...(options.headers||{})}}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'요청 실패');return d}
  function mount(){const nav=$('#nav');if(nav&&!nav.querySelector('[data-view="monstersiege"]')){const b=document.createElement('button');b.dataset.view='monstersiege';b.innerHTML='몬스터 공성전 <span class="buildBadge">6H</span>';nav.querySelector('[data-view="territorywar"]')?.after(b);b.onclick=open}const cms=$('#cms');if(cms&&!$('#view-monstersiege')){const s=document.createElement('section');s.className='view';s.id='view-monstersiege';s.hidden=true;s.innerHTML=`<div class="sectionIntro"><div><small>ALLIANCE MONSTER SIEGE</small><h2>6시간 몬스터 공성전</h2><p>영토전과 독립된 서버 협동 공성전의 단계·체력·보상을 관리합니다.</p></div><button class="ghost" id="msaReload">새로고침</button></div><div id="msaRoot" class="panel">공성전 설정을 불러오는 중입니다.</div>`;cms.appendChild(s);$('#msaReload').onclick=load}}
  function open(){mount();document.querySelectorAll('.view').forEach(x=>x.hidden=x.id!=='view-monstersiege');document.querySelectorAll('#nav button').forEach(x=>x.classList.toggle('active',x.dataset.view==='monstersiege'));if($('#pageTitle'))$('#pageTitle').textContent='몬스터 공성전';load().catch(e=>alert(e.message))}
  function render(d){last=d;const cfg=d.settings,event=d.state?.event,root=$('#msaRoot');root.innerHTML=`<div class="maintenanceHead"><div><small>CASTLE OPERATION CONTROL</small><h2>${event?`${event.name} · 진행 중`:'공성전 대기'}</h2><p>${event?`종료 ${event.endsAt} · 누적 피해 ${Number(event.totalDamage||0).toLocaleString()}`:'설정을 저장한 뒤 수동 시작할 수 있습니다.'}</p></div></div><div class="formgrid"><label class="field"><span>운영 모드</span><select id="msaMode"><option ${cfg.mode==='OFF'?'selected':''}>OFF</option><option ${cfg.mode==='TEST'?'selected':''}>TEST</option><option ${cfg.mode==='ON'?'selected':''}>ON</option></select></label><label class="field"><span>공성전 이름</span><input id="msaName" value="${cfg.name}"></label><label class="field"><span>진행 시간</span><input value="6시간" disabled></label><label class="field"><span>공격 재사용(초)</span><input id="msaCooldown" type="number" min="2" value="${cfg.attackCooldownSeconds}"></label><label class="field"><span>승리 시 전선 HP 감소(%)</span><input id="msaVictoryDamage" type="number" min="26" max="100" value="${cfg.victoryDamagePercent}"></label><label class="field"><span>패배 시 전선 HP 감소</span><input value="25% 고정" disabled></label><label class="field"><span>최소 보상 공격 횟수</span><input id="msaMin" type="number" min="1" value="${cfg.minAttacks}"></label><label class="field"><span>성공 보상 코인</span><input id="msaCoin" type="number" min="0" value="${cfg.rewardCoin}"></label><label class="field"><span>성공 보상 카드 조각</span><input id="msaShards" type="number" min="0" value="${cfg.rewardShards}"></label></div><h3>공략 단계 HP · V2 몬스터 난이도</h3><div class="formgrid">${cfg.phases.map((p,i)=>`<label class="field"><span>${i+1}. ${p.name}</span><input class="msaPhaseHp" data-i="${i}" type="number" min="1000" value="${p.hp}"><input class="msaPhasePower" data-i="${i}" type="number" min="80" max="500" value="${p.powerPercent}"><small>${p.monsterName} · 내 PVE 전투력의 ${p.powerPercent}%</small></label>`).join('')}</div><div class="bar"><button id="msaSave">설정 저장</button><button id="msaStart" ${event?'disabled':''}>6시간 공성전 시작</button><button id="msaSuccess" class="warn" ${event?'':'disabled'}>성공 종료</button><button id="msaFail" class="danger" ${event?'':'disabled'}>실패 종료</button></div>`;$('#msaSave').onclick=save;$('#msaStart').onclick=()=>operate('start');$('#msaSuccess').onclick=()=>operate('finish',{success:true});$('#msaFail').onclick=()=>operate('finish',{success:false})}
  async function load(){render(await api('admin/siege/settings'))}
  async function save(){const phases=last.settings.phases.map((p,i)=>({...p,hp:Number(document.querySelector(`.msaPhaseHp[data-i="${i}"]`).value),powerPercent:Number(document.querySelector(`.msaPhasePower[data-i="${i}"]`).value)})),body={...last.settings,mode:$('#msaMode').value,name:$('#msaName').value,attackCooldownSeconds:Number($('#msaCooldown').value),victoryDamagePercent:Number($('#msaVictoryDamage').value),minAttacks:Number($('#msaMin').value),rewardCoin:Number($('#msaCoin').value),rewardShards:Number($('#msaShards').value),phases};await api('admin/siege/settings',{method:'POST',body:JSON.stringify(body)});await load();alert('공성전 설정이 저장되었습니다.')}
  async function operate(action,body={}){if(!confirm(action==='start'?'6시간 공성전을 지금 시작할까요?':'현재 공성전을 즉시 정산할까요?'))return;await api(`admin/siege/${action}`,{method:'POST',body:JSON.stringify(body)});await load()}
  addEventListener('load',mount);new MutationObserver(mount).observe(document.documentElement,{childList:true,subtree:true});mount();
})();
