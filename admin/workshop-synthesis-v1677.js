(()=>{
  'use strict';
  const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
  const asset=value=>`/${String(value||'').trim().replace(/\\/g,'/').replace(/^\/+/, '').replace(/ /g,'%20')}`;
  const auth=()=>localStorage.getItem('cnine_admin_token')||sessionStorage.getItem('cnine_admin_token')||'';
  const request=async body=>{const response=await fetch('/api/admin/workshop',{method:body?'POST':'GET',credentials:'include',cache:'no-store',headers:{'content-type':'application/json',...(auth()?{authorization:`Bearer ${auth()}`}:{})},...(body?{body:JSON.stringify(body)}:{})});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||'장비 합성 레시피 요청에 실패했습니다.');return data};
  let snapshot=null,selected=0,mounted=false;
  const current=()=>snapshot?.synthesisRecipes?.find(row=>Number(row.id)===Number(selected))||snapshot?.synthesisRecipes?.[0]||null;
  const option=(row,value)=>`<option value="${row.id}" ${Number(row.id)===Number(value)?'selected':''}>${esc(row.name)} · ${esc(row.slot)} · ${esc(row.rarity)}</option>`;
  function mount(){const host=document.getElementById('view-workshop');if(!host||document.getElementById('workshopSynthesisCmsV1677'))return false;const panel=document.createElement('section');panel.id='workshopSynthesisCmsV1677';panel.className='panel ws77-cms';panel.innerHTML='<div class="inlineNotice">장비 합성 계보를 불러오는 중입니다.</div>';host.insertBefore(panel,host.children[2]||null);mounted=true;load();return true}
  async function load(){const root=document.getElementById('workshopSynthesisCmsV1677');if(!root)return;try{snapshot=await request();if(!snapshot.synthesisRecipes?.some(row=>Number(row.id)===Number(selected)))selected=Number(snapshot.synthesisRecipes?.[0]?.id||0);render()}catch(error){root.innerHTML=`<div class="inlineNotice">${esc(error.message)}</div>`}}
  function render(){const root=document.getElementById('workshopSynthesisCmsV1677'),recipe=current(),equipment=snapshot?.equipment||[];if(!root)return;root.innerHTML=`<div class="ws77-cms-head"><div><small>EQUIPMENT LINEAGE RECIPES</small><h2>장비 합성 계보</h2><p>동일 입력 장비 3개를 CMS에 지정한 결과 장비 1개로 정확히 변환합니다.</p></div><div><button type="button" id="ws77CmsNew" class="ghost">+ 새 계보</button><button type="button" id="ws77CmsReload">새로고침</button></div></div><div class="ws77-cms-layout"><aside>${(snapshot.synthesisRecipes||[]).map(row=>`<button type="button" data-ws77-recipe="${row.id}" class="${Number(row.id)===Number(selected)?'active':''}"><span><b>${esc(row.input_name)}</b><i>3 → 1</i><strong>${esc(row.output_name)}</strong></span><small>${esc(row.code)}</small></button>`).join('')||'<p>등록된 계보가 없습니다.</p>'}</aside><main>${recipe?editor(recipe,equipment):'<div class="inlineNotice">새 계보를 추가하세요.</div>'}</main></div>`;root.querySelectorAll('[data-ws77-recipe]').forEach(button=>button.onclick=()=>{selected=Number(button.dataset.ws77Recipe);render()});root.querySelector('#ws77CmsNew').onclick=()=>{const base=equipment[0];snapshot.synthesisRecipes.unshift({id:0,code:`SYNTH_NEW_${Date.now()}`,name:'새 장비 합성 계보',description:'동일 장비 3개를 지정 결과 장비 1개로 합성합니다.',input_equipment_id:base?.id,output_equipment_id:base?.id,is_active:1,is_public:0,owner_test_only:1,sort_order:100});selected=0;render()};root.querySelector('#ws77CmsReload').onclick=load;root.querySelector('#ws77CmsSave')?.addEventListener('click',save);root.querySelectorAll('[data-ws77-select]').forEach(select=>select.onchange=updatePreview)}
  function editor(recipe,equipment){const input=equipment.find(row=>Number(row.id)===Number(recipe.input_equipment_id))||equipment[0],output=equipment.find(row=>Number(row.id)===Number(recipe.output_equipment_id))||equipment[0];return `<div class="ws77-cms-preview"><figure><span>${esc(input?.rarity)}</span><img id="ws77InputImage" src="${esc(asset(input?.image_url))}" alt=""></figure><b><em>3</em><i>→</i><em>1</em></b><figure class="result"><span>${esc(output?.rarity)}</span><img id="ws77OutputImage" src="${esc(asset(output?.image_url))}" alt=""></figure></div><div class="formgrid"><label class="field"><span>계보 코드</span><input id="ws77RecipeCode" value="${esc(recipe.code)}"></label><label class="field"><span>계보 이름</span><input id="ws77RecipeName" value="${esc(recipe.name)}"></label><label class="field full"><span>설명</span><input id="ws77RecipeDescription" value="${esc(recipe.description||'')}"></label><label class="field"><span>입력 장비 · 3개 소모</span><select id="ws77RecipeInput" data-ws77-select>${equipment.map(row=>option(row,input?.id)).join('')}</select></label><label class="field"><span>결과 장비 · 1개 지급</span><select id="ws77RecipeOutput" data-ws77-select>${equipment.map(row=>option(row,output?.id)).join('')}</select></label><label class="field"><span>정렬</span><input id="ws77RecipeSort" type="number" value="${Number(recipe.sort_order||0)}"></label><label class="field"><span>노출</span><select id="ws77RecipePublic"><option value="1" ${Number(recipe.is_public)?'selected':''}>유저 공개</option><option value="0" ${!Number(recipe.is_public)?'selected':''}>숨김</option></select></label><label class="field"><span>상태</span><select id="ws77RecipeActive"><option value="1" ${Number(recipe.is_active)?'selected':''}>활성</option><option value="0" ${!Number(recipe.is_active)?'selected':''}>중지</option></select></label><label class="field"><span>접근</span><select id="ws77RecipeOwner"><option value="0" ${!Number(recipe.owner_test_only)?'selected':''}>전체</option><option value="1" ${Number(recipe.owner_test_only)?'selected':''}>OWNER 테스트</option></select></label></div><button type="button" id="ws77CmsSave">계보 레시피 저장</button>`}
  function updatePreview(){const equipment=snapshot?.equipment||[],input=equipment.find(row=>Number(row.id)===Number(document.getElementById('ws77RecipeInput').value)),output=equipment.find(row=>Number(row.id)===Number(document.getElementById('ws77RecipeOutput').value));document.getElementById('ws77InputImage').src=asset(input?.image_url);document.getElementById('ws77OutputImage').src=asset(output?.image_url)}
  async function save(event){event.currentTarget.disabled=true;try{const recipe=current();const result=await request({action:'SAVE_SYNTHESIS_RECIPE',recipe:{id:Number(recipe?.id||0),code:document.getElementById('ws77RecipeCode').value,name:document.getElementById('ws77RecipeName').value,description:document.getElementById('ws77RecipeDescription').value,inputEquipmentId:Number(document.getElementById('ws77RecipeInput').value),outputEquipmentId:Number(document.getElementById('ws77RecipeOutput').value),sortOrder:Number(document.getElementById('ws77RecipeSort').value),isPublic:Number(document.getElementById('ws77RecipePublic').value),isActive:Number(document.getElementById('ws77RecipeActive').value),ownerTestOnly:Number(document.getElementById('ws77RecipeOwner').value)}});snapshot=result.snapshot;selected=Number(result.recipeId);render();alert('장비 합성 계보를 저장했습니다.')}catch(error){alert(error.message)}finally{if(event.currentTarget?.isConnected)event.currentTarget.disabled=false}}
  const observer=new MutationObserver(()=>{if(!mounted)mount()});observer.observe(document.documentElement,{childList:true,subtree:true});document.readyState==='loading'?document.addEventListener('DOMContentLoaded',mount):mount();
})();
