(()=>{const num=n=>Number(n||0).toLocaleString(),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const typeLabel=type=>String(type||'SSR_TO_MA')==='MA_TO_PRESTIGE'?'MA +13 → PRESTIGE':'SSR +10 → MA';
async function load(){const state=document.getElementById('evolutionCmsState');try{const d=await api('admin/evolution/settings'),s=d.settings;document.getElementById('evolutionEnabled').value=s.enabled?'ON':'OFF';document.getElementById('evolutionSsrStarCost').value=s.ssrToMaMasterStarCost;document.getElementById('evolutionPrestigeStarCost').value=s.maToPrestigeMasterStarCost;state.textContent=s.enabled?'운영 중':'중지';state.className='statusPill '+(s.enabled?'on':'off');const logs=document.getElementById('evolutionCmsLogs');logs.innerHTML=d.logs?.length?d.logs.map(x=>`<div class="evolutionCmsLog ${x.is_success?'success':''}"><div><b>${esc(x.nickname)}</b><span>${esc(x.source_title)} · ${typeLabel(x.evolution_type)}</span></div><strong>${x.is_success?`완료 · ${esc(x.reward_title||'결과 미확인')}${x.reward_duplicate?' (중복)':''}`:'처리 실패'}</strong><small>마스터의 별 ${num(x.master_star_cost)}개 · 원본 카드 ${x.source_consumed?'소모':'유지'} · ${new Date(x.created_at+'Z').toLocaleString()}</small></div>`).join(''):'<div class="muted">아직 진화 기록이 없습니다.</div>'}catch(e){state.textContent=e.message;state.className='statusPill off'}}
function mount(){const view=document.getElementById('view-cards');if(!view||document.getElementById('evolutionCmsPanel'))return;const panel=document.createElement('div');panel.id='evolutionCmsPanel';panel.className='panel evolutionCmsPanelV1157';panel.innerHTML=`<div class="maintenanceHead"><div><small>MASTER STAR EVOLUTION</small><h2>카드 진화 설정</h2><p>진화에 소모되는 마스터의 별 수량을 경로별로 관리합니다. 원본 카드 소모와 결과 지급은 서버에서 함께 처리됩니다.</p></div><span id="evolutionCmsState" class="statusPill off">불러오는 중</span></div><div class="formgrid evolutionCmsGrid"><label class="field"><span>진화 운영</span><select id="evolutionEnabled"><option value="ON">ON · 사용</option><option value="OFF">OFF · 중지</option></select></label><label class="field"><span>SSR +10 → MA</span><input id="evolutionSsrStarCost" type="number" min="1" max="9999" step="1"><small>1회 진화에 필요한 마스터의 별</small></label><label class="field"><span>MA +13 → PRESTIGE</span><input id="evolutionPrestigeStarCost" type="number" min="1" max="9999" step="1"><small>1회 진화에 필요한 마스터의 별</small></label></div><div class="evolutionCmsNotice"><b>운영 기준</b><span>진화 결과는 활성화된 공개 상위 등급 카드 중 랜덤 1장입니다. 카드 또는 결과 지급에 문제가 생기면 원본 카드와 마스터의 별은 소모되지 않습니다.</span></div><button id="saveEvolutionCms">진화 설정 저장</button><div class="maintenanceHead evolutionLogHead"><div><h3>최근 진화 기록</h3><p>최근 50건의 진화 경로와 마스터의 별 사용량입니다.</p></div><button type="button" class="ghost" id="refreshEvolutionLogs">새로고침</button></div><div id="evolutionCmsLogs"></div>`;view.insertBefore(panel,view.firstElementChild);document.getElementById('saveEvolutionCms').onclick=async()=>{const payload={enabled:document.getElementById('evolutionEnabled').value==='ON',ssrToMaMasterStarCost:Number(document.getElementById('evolutionSsrStarCost').value),maToPrestigeMasterStarCost:Number(document.getElementById('evolutionPrestigeStarCost').value)};if(payload.ssrToMaMasterStarCost<1||payload.maToPrestigeMasterStarCost<1)return alert('마스터의 별 소모량은 1개 이상이어야 합니다.');try{await api('admin/evolution/settings',{method:'PATCH',body:JSON.stringify(payload)});alert('진화 설정이 저장되었습니다.');load()}catch(e){alert(e.message)}};document.getElementById('refreshEvolutionLogs').onclick=load;load()}
new MutationObserver(mount).observe(document.documentElement,{childList:true,subtree:true});mount();})();
