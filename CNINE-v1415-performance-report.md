# CNINE v1415 성능 최적화 패치

## 적용 내용

- 카드·팩·이벤트·뽑기 핫패스에서 전체 런타임 마이그레이션 게이트를 제거했습니다.
- 기능 내부의 영수증 및 필수 테이블 안전장치는 유지했습니다.
- 사용자별 `shell/summary` 캐시를 제거해 이전 인벤토리 값이 다시 표시되지 않게 했습니다.
- 공개 카탈로그 캐시는 최대 5초의 isolate-local burst cache로 축소했습니다.
- 이벤트와 레이드 폴링 주기를 완화하고 jitter를 적용해 동시 요청 쏠림을 방지했습니다.
- 수동 뽑기는 45초 대신 12초, 자동 뽑기는 60초 대신 20초 후 idempotent 영수증 복구로 전환합니다.
- JS/CSS 장기 캐시, HTML no-cache, 교체 가능한 이미지의 단기 캐시를 분리했습니다.
- 스크립트에 `defer`와 신규 버전 키를 적용했습니다.
- 동시접속용 DB 인덱스 마이그레이션 `0033_v1415_hotpath_indexes.sql`을 추가했습니다.

## 배포 전 필수 작업

프로덕션 코드를 먼저 올리지 말고 마이그레이션을 먼저 적용하십시오.

```powershell
npx wrangler d1 migrations apply cnine-card-db --remote
```

성공을 확인한 뒤 Pages 프로젝트를 배포하십시오.

## 권장 검증

1. 테스트 계정으로 1회·10회·자동 뽑기를 수행합니다.
2. 네트워크 지연 상태에서 동일 `requestId` 재전송 시 코인이 한 번만 차감되는지 확인합니다.
3. 뽑기 직후 코인, 인벤토리, 도감 수량이 즉시 일치하는지 확인합니다.
4. CMS 설정 변경이 사용자 화면에 5초 이내 반영되는지 확인합니다.
5. 레이드 HP와 참가자 순위가 정상적으로 갱신되는지 확인합니다.
6. `/api/draw`, `/api/raid/status`, `/api/burning-event/status`의 p95와 D1 rows read를 비교합니다.

## 롤백

문제 발생 시 이전 Pages 배포로 롤백할 수 있습니다. 추가 인덱스는 데이터 값을 변경하지 않으므로 남겨도 무방합니다.

## 남은 2차 최적화

- 카드 목록 가상 스크롤
- 콘텐츠별 JS/CSS 동적 로딩
- 카드 랭킹 사전 집계
- 레이드 Durable Object 또는 SSE 전환
- 대형 GIF/PNG/MP4 재인코딩
- 실부하 테스트 및 p95/p99 계측
